# クルージングモード（ローカル、2026-09-12）

依頼: スライダー付きは自動で終点まで進め3秒後に次へ。スライダーなしは5秒おきに約5 POIを表示して次へ。全展示をループ。

実装: src/exploration/map-cruise.js に巡回の時計とUIを追加。PCは上部、スマホは操作メニュー。現在の展示の先頭から開始し、01〜71を既存の展示選択経路で巡る。スライダーのデータ更新は既存inputハンドラーを使用し、つまみの表示のみ小数値で連続移動。停止時には元のstepと記録位置に戻す。既存の単独自動表示とは排他制御。
POIは収録地点から最大5か所を分散して選び、既存のカメラ・プレビュー／地点選択処理で表示。読み込み、ガイド、非表示タブ、スマホ操作パネル中は時計を止める。停止ボタン・手動操作・地図退出で解除。初期状態はオフ。

検証:

- node scripts/check-map-cruise.mjs 合格。時計を注入して71展示の一周、終点+3秒、5地点×5秒、読込／非表示中の停止、退出を検証。
- check-map-cruise-browser.mjs: Chrome 1440×900で全71展示の切替と71→01を確認。390×900で2・9・17・21・33・69・71を確認。データと実画面は実物、巡回時計だけを加速。全時間を実時間で放置した試験ではない。
- 上記後につまみの整数丸めを検出して修正。最終版は1・7・9・12・17・21・33・71を1440×900 / 390×900で再検証。
- check-map-cruise-lifecycle-browser.mjs: 実時間で02の5 POIの文言が5秒ずつ変わり03へ進むこと、停止／再開、ズームによる解除、スマホ操作パネルでの時計停止を確認。09は終点への到達だけ時計を加速し、終点の表示と3秒待機は実時間で確認。つまみ10サンプルの連続増加も検査。
- 変更JSのnode --checkとgit diff --check合格。PCのPOI表示とスマホ操作メニューを目視確認。

証跡: artifacts/map-cruise/all-71-before-smooth-fix.json、同results.json（最終代表系統）、artifacts/map-cruise-lifecycle/results.jsonとPNG。
外部HTTPSを遮断した保存データでの確認。実ライブ配信、物理スマホ、全展示の実時間通し放置は未実施。コミット・プッシュ・デプロイは今回実行していない。前回公開依頼の承認待ちは継続。
