# 接続状態・観測種別を見出し直下へ移動（2026-09-08）

## 変更

基点 `b236255db4dc60f748aecfe4327d45d39f8ad33b` に、前回までの紹介画面・下部クレジットのローカル変更を保持した作業ツリーが対象。

- `realtime-exhibits.css`：幅1800px以上で右列にあった「保存観測を表示・ライブ未接続」等の接続状態と観測種別を、「リアルタイム展示」の直下・同じ左端へ移動した。時刻はその右側に維持。
- 見出し行は2列をまたがせ、バッジを見出しの隣に配置。フルHDでもバッジと時刻が重ならないようにした。
- 1800px未満のPCは既存の縦積み配置を維持。重複見出しを省いたスマホHUDも変更していない。データ判定、観測時刻、出典クレジット、取得処理は変更なし。
- `gaia-mode-loader.js` と `index.html` の対応キャッシュ識別子を更新。

## 実画面検証

ローカル `http://127.0.0.1:4447`、Windows Chrome。外部APIは保存データ・試験用応答で検証した。

`scripts/check-realtime-exhibits-browser.mjs` に、見出しと状態の座標・左端揃え、要素同士の非重複、広い画面での保存観測・参考値・時刻遅れの検査と記録を追加した。

1. 変更前再現：`REALTIME_SIZES=3840x2088` で `--before-state-layout` を指定し、`artifacts/realtime-state-below/before/` に記録。展示01〜05と保存観測・参考値・時刻遅れで、状態が見出しの右側にあることを確認。保存観測の実出力も目視確認した。
   - このオプションは該当レイアウトだけを旧配置としてブラウザーへ供給する。最終CSSから復元した旧配置CSSのSHA-256が、変更前の記録と一致することを確認した。
2. 最終変更の回帰試験：`node scripts/check-realtime-exhibits-browser.mjs http://127.0.0.1:4447 artifacts/realtime-state-below/after-final`
   - 1440×900、1920×1080、2560×1392、3840×2088、1024×768、390×844、320×568の7サイズで展示01〜05の35ケース合格。幅1024以外の6サイズで保存観測・参考値・時刻遅れの18ケース合格。ページエラー0。
   - 最後の844×390はページ読込が30秒でタイムアウトしたため、この一括実行のレポート自体は `failed` のまま保存している。既に完了した上記ケースは再利用した。
3. 未完了条件を再実行：`REALTIME_SIZES=844x390` で同じスクリプトを実行し、`artifacts/realtime-state-below/after-landscape-final/` に記録。展示01〜05の5ケース合格、ページエラー0。
   - 最終実装で合計8サイズ・40展示ケースと18フォールバックケースが合格。既存の時刻、ライブバッジ、凡例開閉、操作の到達性、表示範囲、展示06でリアルタイム表示が消える動作も確認した。
   - 1920・2560・3840幅の保存観測の最終スクリーンショットを開き、見出し直下の状態・観測種別、時刻とバッジの非重複を目視確認。途中案の `after/` は採用せず、最終採用証跡は上記2つの `after-final/` と `after-landscape-final/`。
   - 両最終レポートの対象3ランタイムファイルのSHA-256と作業ツリーの一致を確認。

`node --check`（ローダー・変更したブラウザーテスト）、`check-mode-loader-prefetch.mjs`、`check-realtime-exhibits.mjs`、`build-browser-security.mjs --check`、`git diff --check` は合格。

## 範囲と公開状態

ローカルChromeと画面サイズ・タッチ入力エミュレーションによる確認。実API、本番配信、物理スマホ、Safariは未確認。保存・書き出し仕様や配布ZIPの変更はない。今回はコミット・プッシュ・デプロイをしていない。
