# スマホ地点カードの重なり・上揃え

2026-09-14 コミット・プッシュの追加指示を受領。今回の8ファイルのみを開発ブランチへ送信し、デプロイは行わない。再確認時はローカルサーバーが停止していたため再起動して試験。未採用画像、チラシ、thumbnail.png、別用途のパッケージスクリプトは対象外。

## 2026-09-14 方針変更

スマホの常時クレジット帯を非表示。出典パネルと地点カードの折りたたみは保持。OSMタイル（#japan-tiles が非hidden）を使用する場合のみ短い © OpenStreetMap contributors リンクを別表示。公式タイル利用条件 https://operations.osmfoundation.org/policies/tiles/ は地図上の可視表記を要求するため、OSM表記は出典ボタンだけに隠さない。Natural Earthは https://www.naturalearthdata.com/about/terms-of-use/ でpublic domain。これは他の全提供元条件の包括承認ではない。

地点の閉じるボタンは44px操作領域・18pxアイコンに縮小、外枠をなくし見出し右余白を48pxへ。390/628pxで寸法・実際の閉鎖・帯非表示・折りたたみ展開・復帰を確認。

## 追加修正

クレジット一覧はdetailsで初期折りたたみ。「地図のクレジット」を押した時だけ展開し、カードを開き直すと閉じる。Open-Meteoの上端見切れは地点カードとは別で、外側のクレジット枠の下揃えが原因。390pxで子の上端519.31px / 枠の上端524.31px（5px見切れ）を実測。justify-content:flex-startに変更後は528.31px / 524.31px（4px内側）。下にあふれる内容は従来のスクロール領域に残す。

390/628pxの上端・折りたたみ・展開・閉鎖後復帰、および320×568/844×390/1280×720の地図タップによる回帰に合格。Open-Meteo欄の画像を目視。キャッシュ識別子を更新し、通常再読み込みで修正が適用されるようにした。以前の「上揃え完了」は地点カードの確認であり、このOpen-Meteo欄の確認ではなかった。

ローカル修正。スマホで開いている地点カードの末尾に共通クレジットを移し、元データリンクとの間に16pxの余白を確保。閉じる・PC幅に戻す場合は元の配置へ戻す。カードは見出しの下から上揃え、初期スクロール0。下に収まらない説明・クレジットはカード内スクロール。

検証: check-mobile-poi-credits.mjs は390/628pxの実DOMに可視カードの制御条件を設定し、配置・閉鎖後復帰に合格。check-poi-card-layout-browser.mjs は320×568、844×390、1280×720の地図を実際にタップして4指標と元データリンクの可視性・クリック可能性に合格。上流値は制御API入力/保存値で、LIVE実通信ではない。横画面で観測ドロワーがリンクに重なる症状を再現し、カードの表示順調整後に同操作で合格。確定スクリーンショットは artifacts/mobile-poi-credits-20260913/real。

check-source-collapse.mjs 合格（共通出典・配布保留の関連回帰）。node --check map-responsive-layout.js / git diff --check 合格。実機Safariは未確認。今回コミット・プッシュ・デプロイは依頼されていないため未実施。
