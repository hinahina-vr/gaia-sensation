# キャラクター紹介の左端・文章校正

## 依頼と対象版

2026-09-10、公開URL `https://gaia-senseware.pages.dev/#character` と添付画像に対して「文字の位置下のキャラの左位置に揃えて　いまガチャってる」、および以下の文章校正を依頼された。見出し・副見出し・リード文の左端を、先頭の丸いキャラクター画像の左端に揃える依頼と解釈した。

- リード: 四つの瞳に映る、たったひとつの地球。
- 「はじめまして」: 海風の抜ける通りで、ふたりと出会った。／柔らかな秋の光のなか、物語が静かに動き出す。
- 「夕暮れの帰り道」: 展示を終えて、夕焼けに染まる海沿いを歩く。／今日の終わりは、新しい始まり。
- コンセプト案内見出し: 放課後の窓辺から。
- コンセプト案内本文: 放課後の静けさの向こうに広がる、地球の気配。『惑星の放課後～GAIA SENSATION～』が生まれた背景と思想を綴りました。

基準コミット `888c26b623151205f6b8ecf9f77951dbc9a425ae`。先行するBGM・プロローグ・エンディング遷移の未公開変更を保持したローカル作業ツリーが検証対象。コミット・push・デプロイは行っていない。

## 変更と再現

`character-mode.css` で、ボタン内に中央配置された丸い画像の余白を文字にも共用した。副見出し独自の左マージンを解消し、ボタンのパディングを明示。PC・4K・スマホの既存サイズをカスタムプロパティで共通管理する。画像のサイズ・顔の位置・選択時の挙動は維持。

`index.html` のリードとコンセプト案内、`character-mode.js` の2場面の詩文を指定原文どおり変更。台本・音源・画像・コンセプト遷移先の本文は変更しない。`gaia-mode-loader.js` とそのHTML読み込みキーを更新。既存の文章契約・ブラウザ試験の期待値を合わせ、新規 `check:character-copy:browser` を追加した。

証跡ルートは `artifacts/character-copy-alignment-20260910/`。インストール済みChrome、分離ブラウザ、localhost:4492、本番と同じCSPを使用。

- `before/report.json`: PCとスマホ幅で再現。見出しとリードの左端は画像より6px左、副見出しも2.64px／3.84px左だった。
- `after-final/report.json`: 1440×900、3840×2160、980×1000、390×844、320×568、844×390の6ケース合格。3種類の文字のボックス左端と画像左端の差は全件0px、横方向のはみ出しなし。通常モーションと320px幅の動き低減設定を含む。
- 同試験で4人の選択、2場面の一覧と実クリック／タップで開いた拡大表示のタイトル・2行の全文、画像ロード、Escapeで閉じる、案内見出し・本文・リンク先を検査。エラー・HTTP 400以上・CSP違反は0。
- `concept-navigation/report.json`: PC・390・320・844px幅の4ケース合格。新しい案内の全文と表示領域、キーボード／タップでのコンセプトページ遷移、ブラウザで戻る、紹介画面を閉じるところまで確認。
- `selector-regression/report.json`: 4K・PC・390・320px幅の4ケース合格。既存の3人の顔の大きさ・顎の高さ・左右位置、選択・hover・キーボードフォーカス時に位置が変わらないことを確認。青猫は輪郭表示のため顔の比率比較から除外。
- `npm-check.log`: `npm run check` 全体合格。`git diff --check` 合格。ブラウザレポートの4対象ファイルのSHA-256と完了時の作業ツリーを照合。
- PC・スマホ幅の紹介上部、PCの夕暮れ拡大表示、スマホ幅の出会い拡大表示・コンセプト案内を実画像で目視確認。

## 試験上の修正・未確認範囲

新規試験の初回はCGカードの見出しを `h4` と誤指定して待機終了した。実DOMの `strong` に修正して6ケースを再実行し合格。失敗記録 `after/` は保持。公開URLの読み取りはブラウザで `ERR_NETWORK_ACCESS_DENIED` となったため、本番画面を今回直接検証したとは扱わない（`public-before/`）。添付画像とローカルで症状を照合した。

物理端末・Safari/Firefox・本番反映は未確認。紹介ページには保存や書き出し機能の変更がなく、新規ZIP等の配布物もない。既知の本編通常会話の短い横画面での顔と会話領域の重なりは今回の紹介ページとは別の実装にあり、未対応のまま保持。別途そのレイアウト調整と再検証が必要。
