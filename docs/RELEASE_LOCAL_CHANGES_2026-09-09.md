# 2026-09-09 ローカル変更の公開

## 指示と対象

製作者の今回の明示指示「コミット・プッシュ・デプロイ」に基づく。公開先は既存の `https://gaia-senseware.pages.dev`。公開基点 `aaa9153116c704f14d82f48969a396b907388451` からの、このタスクで依頼・実装されたローカル変更をまとめる。実装コミットは `6df0a40`。新規サービス、課金プラン変更、DB変更、シークレット変更は行わない。

- コンセプト本文・構想図・作者紹介・神託3原則、タイトルとキャラクター紹介からの導線。
- 物語ログ修正、終幕の描画待機・話者フェード、展示切替の保持時間と操作音。
- センサーのイラスト付き案内、公開地図上の操作説明。
- 地図UI・海域CODの表示と選択改善、日本のpH・DO・気象12展示の追加（計43展示）。

各変更の再現・修正確認・実画面証拠は同日の `QA_*.md` および2026-09-08のコンセプト等の記録を参照。過去のQAはその記載時点の版に対する証拠であり、後続版の全体合格に読み替えない。

## 素材範囲の照合

基点との機械照合により、既存297素材の全台帳項目、既存出典6件、`data/gaia-signals.json`、`data/gbif-rights.json`、`docs/THIRD_PARTY_INVENTORY.json`、両lockfileが不変。追加は依頼されたOpenAI Imagegen素材7件で、制作記録・加工履歴・SHA-256・初回コミット日を台帳へ収録。

旧scope `ccc12a0d1fbbc28e1349d8b0a6331704f030fe4b48dd0cf2e09a414748440908` → 新scope `5c397fc2edacd956f1d4aa677c5b65cbec8b3f09275b226f177308e31a29eab1`。既存の製作者判断の日時・理由・提供元個別許諾の確認状態は維持。提供元の個別許諾や全条件適合を新たに確認したという意味ではない。

追加の環境省・気象庁資料は、`data/sources/japan-sensor-open/README.md` に取得元・期間・換算・欠測・利用条件確認を別記。出典、利用条件へのリンク、「GAIA SENSEWAREが抽出・加工」の表示を確認済み。官公庁が制作・承認したと表示しない。既存の未確認権利項目に関する了承を、新たな未確認リスクへ拡張しない。

## 公開前検査

全体検査で、保持時間延長前の字幕テスト3条件と追加展示の字幕取得方法に対する旧期待値が残っていたため、現在の実装と既存ブラウザ回帰記録に合わせた。演出時間を戻したり検査を削除したりしていない。

`b610676cc4d7bc385f0f2b904592e95b5384ca36` を新しいディレクトリ `artifacts/release-b610676-clean/` へGitのblobそのままで展開し、1,313ファイルのコミット照合に合格。公開前の新規展開版と公開後のHTTP・実Chrome証拠の保存先は `artifacts/release-20260909/`。

合格した検査：

- 元作業ツリーの `npm run build:release`（権利台帳、precheck/check/postcheck、公開ゲート21フィクスチャ、既知形式の秘密値840ファイル走査、991,209-byte Pages Workerの再現ビルド）。ビルド後にWorkerの内容変更はなく、`check:pages-worker` も合格。
- `npm run check:contest`（初期画面815,147 bytesで1 MB以内、提出情報と素材台帳）とsensor-platformのTypeScript検査。
- ローカルPagesの22検査。実WorkerのAPI/静的配信/Range/CSP/非公開パス/分離した匿名セッション試験。ライブ提供元はフィクスチャ・無効化条件であり、本番確認とは別。
- 新規展開版の全体 `npm run check`。ただしこのディレクトリはGit作業ツリーではないため秘密値走査は0ファイルとなる。秘密値チェックの証拠には元作業ツリーの840ファイル結果を使用する。
- 新規展開版で、原資料再ビルドの一致と独立照合83,044環境省レコード＋1,140気象庁レコード。
- 新規展開版のChrome 1440×900・390×844：実HTTP配信を使い、タイトル→コンセプト→戻る、作者紹介、4講義、神託3原則、構想図の拡大、4キャラクターとCG、キャラクター→コンセプト→戻る、全43展示の登録、追加12展示の地点・年度・元の数値と単位、出典、5年の分析グラフ、43→01の前後移動。レスポンスのモックなし、JSエラー・資産失敗・CSP違反0。`local-browser-verified/browser-report.json`。
- 新規展開版のキャラクターリンク専用回帰4サイズ（1440/390/320/844）、字幕保持時間4条件（PC/スマホ×通常/動きを減らす）、終幕グリント4条件。
- 新規展開版の物語ログ回帰2サイズ：修正4台詞、保存・読込、旧版セーブ、再読込、544件ログ、Markdownファイルの実ダウンロード。`story-regression/report.json`。

新規展開版でWorker自体を再ビルドする試みは、そこへnpm依存を導入していないため `geotiff` を解決できず未完了。配布Workerは元作業ツリーで再現ビルド・Pages実行テストを済ませた同一blobを展開し、変更せず配信する。

## 未確認範囲

実機スマホ・Safari/Firefox、ESP32実機の接続・校正、外部AIへの実送信・課金、実アカウントによる認証変更・データ登録・削除は今回の公開確認に含めない。ブラウザの端末エミュレーションとローカルAPI試験を実機・実アカウント試験とは表現しない。
