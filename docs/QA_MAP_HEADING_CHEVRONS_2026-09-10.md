# 地図タイトルの左右矢印 — 2026-09-10

## 依頼と対応

ユーザー原文: 「左右の＜＞　デザインダサいんでもっと頑張って　添付のやつとかでいいよ」。展示66のタイトル左右と、細身の右向き矢印が参考画像。

解釈: 対象は地図上部タイトルに付く共通の前後ボタン。全角文字「＜ ＞」を、参考画像に近い縦長・細身のSVGシェブロンへ置換。24pxのアイコン枠、2.25pxの丸端ストローク、淡い青緑色。左右は鏡像。ボタンは44×44pxのまま、読み上げラベル・既存の前後移動・キーボードフォーカス枠を維持。前回撤去した展示見出しの下線は戻していない。下部操作バーや他の矢印は今回変更していない。

変更ファイル: `map-heading-navigation.js` / `.css`、キャッシュキー更新の `gaia-mode-loader.js` / `index.html`、回帰テスト `scripts/check-map-heading-chevrons-browser.mjs`、実行コマンド追加の `package.json`。

## 検証対象

- 親HEAD `cdc82c8ffd4fee607bd2d838ec3157b7095952d3` のローカル作業ツリー。既存の未コミット変更は維持。
- `http://127.0.0.1:4492/?exhibit=66#world` の案内から展示を開始。
- 修正前の1440×900で実際の「＜ ＞」表示を確認し、スクリーンショットを保存。
- 修正後のソースSHA256は `artifacts/map-heading-chevrons-2026-09-10/after/report.json` に記録し、検証終了時に実ファイルとの一致を確認。

## 合格した検証

`npm run check:heading-chevrons:browser` 相当のスクリプトをインストール済みChromeで実行。

- 1440×900、3840×2160、768×1024、390×844、320×568、844×390の6画面サイズ。
- 各幅で12回、計72回のレイアウト・矢印形状チェック。展示66の初期状態、67→66の前後移動、64・71・01・04・06・15・21の各描画系統、71→01→71の循環移動を対象とした。
- 全ケースで左右の鏡像パス、24pxアイコン枠、読み上げから隠した装飾SVG、44px操作領域、実際のヒット判定、タイトルとの非重複、横はみ出しなしを確認。
- マウスクリック／タッチエミュレーションで前後移動。PCではEnter・Spaceによる移動、フォーカス保持と枠表示も確認。
- 強制カラーモードでも矢印が表示されることを確認。
- 修正前後のPCタイトル、4K・タブレット・スマホ縦横・最小320幅、フォーカス状態、強制カラーの実スクリーンショットを目視確認。
- ページ例外・CSP違反なし。関連JS構文確認、`git -c core.safecrlf=false diff --check` 合格。

## 証跡・限界

`artifacts/map-heading-chevrons-2026-09-10/{before,after}/` にレポートと画面を保存。

ローカル実データと本番CSPを使用。NOAA／FIRMSは保存応答、USGSは空応答、天気APIは通信失敗を固定したため、実配信データの検証ではない。タッチ・画面サイズはエミュレーションで物理端末未確認。

表示アイコンとスタイルのみの変更で保存・読込・書き出し処理は未変更、今回その経路は再試験していない。ZIP配布物なし。コミット・push・本番公開は未実施。公開指示は受けていない。
