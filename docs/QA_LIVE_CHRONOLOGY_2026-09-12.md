# 15〜20 過去から現在への再生

live-exhibits.js: 各展示の初回再生は最古の時刻から開始。時刻を昇順にし、未来の時刻は自動再生に含めない。4秒ごとに進み、最後の保存時刻の次に最新スロット(null)を表示する。次の周回で最古へ戻る。一時停止・再開は位置を維持し、展示の退出時に開始状態をリセット。

検証: QA_NUMBERS=15,16,17,18,19,20 node scripts/check-map-default-play-browser.mjs。ローカルChrome1440/390幅。最古から開始、時刻が前進、最後の保存時刻から最新へ遷移すること、スライダーの実値変化、手動停止を検査。終端は最後の時刻へテストから移動して検証（全時間を待った試験ではない）。

外部API遮断の保存モデル値による検証。実機・ライブ更新・本番未検証。未公開。証跡artifacts/map-default-play-20260912/report.json。
