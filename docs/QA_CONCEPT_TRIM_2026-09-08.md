# コンセプトページ・指定10箇所の削除

2026-09-08、ローカル候補 `concept-13-trim`。画像で指定された次の10箇所を `concept/index.html` から要素ごと削除しました。

1. 「PROJECT NOTE / 作品の入口」全体。
2. 「データを調べる。変化を読む。／そして、共につくる人々の物語へ。」
3. 「海流 / 光 / 動き」
4. 「地図 / 時系列 / 公開データ」
5. 「オンライン大学 / 学園祭 / 共同制作」
6. スクリーンショット・イラスト・装飾についての2行の補足。
7. 「大学がひらく可能性を、物語として描く。」
8. 「私を記録する。別の私に、聞いてみる。」から「最後の選択は、機械に渡さない。」までの説明。
9. 「ORIGIN / LIFELOG」の説明ブロック全体（見出し、本文、資料リンクを含む）。
10. 「非決定性・神託・本人の選択」はLifeLogから着想した応答である旨の補足。

ほかの本文・見出し・画像・リンクが変わっていないことを、変更直前のDOMから指定10要素だけを取り除いた期待値と比較して確認しています。大学の公式リンク9件、図と4段階の説明、別の位置にある現行実装の制約説明、末尾の1行見出し、ロゴからトップページへ戻るリンクは維持しています。CSS・JavaScript・素材ファイルは今回変更していません。既存のローカル変更を保持。push・デプロイなし。

## 回帰検証

- `node scripts/check-concept-trim-browser.mjs --before`：1440×900・390×844で削除前の10要素を再現し、それぞれのスクリーンショットを保存。
- `npm run check:concept-trim:browser`と同じスクリプトの直接実行：同じPC・スマホ幅で10要素の削除、その他の本文・画像・リンク・見出しの完全一致、横溢れなし、図の拡大・Escape・フォーカス復帰、末尾見出しの1行表示を確認。ページエラー・CSP違反・保存領域への書込なし。`artifacts/concept-trim-after/report.json`。
- `npm run check:concept`：構文・静的契約に合格。削除対象を復活させない検査を追加し、旧補足の存在を前提にした期待値を更新。既存の画像種別・出典リンク等の検査は維持。
- PCの冒頭・構想見出し、スマホの3つの体験紹介を実際のスクリーンショットで目視確認。
- `git diff --check`：合格。既存の改行コード警告のみ。

`npm run check:concept:browser`：1440×900、390×844、320×568、768×1024、3840×2160、JavaScript無効、既存本編入口の計7ケースに合格。残した本文の実描画フォント・文字サイズ・コントラスト、図の読込・拡大・スクロール・閉じる・フォーカス、アンカーと配色切替、動きを減らす設定、印刷メディアでの内容表示を確認。ページエラーなし。`artifacts/concept-page-v13/report.json`。削除を反映した最終HTML/CSS/JSとレポートのハッシュ一致を確認しました。

ローカルChromeのPC／モバイル画面エミュレーションです。実機・本番配信ではありません。大学リンクのURLと文言は維持しており、公式サイトの再巡回はしていません。本編全編、保存・書き出し、外部API・センサー連携は変更せず今回の再検証対象外。配布ZIPなし。

変更前HTMLは `artifacts/concept-trim-before/index.html` に保存してあり、削除した内容は復元可能です。既存の制作記録や素材も削除していません。

## 最終ファイルSHA-256

- HTML：`25528fad222ea1d4b12c7518d0369d7ef1049dcf19ad817ec9df5fd358eb7220`
- CSS（今回不変）：`2ef004bbf8eff276a6abda6d50dba6ff70e306479639d41268ea3b730b0b62da`
- JavaScript（今回不変）：`4696cfa83134cef43cce1dcf47feb3eec05aeb00316d0927aae1bbc9c9eac78a`
