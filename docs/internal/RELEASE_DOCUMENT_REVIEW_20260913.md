# 公開文書導線の照合記録（内部用）

- 日付：2026-09-13
- 基点：cb5e44320392d76fc81b08c3e0c7f81f0f89765a + 既存の未コミット変更
- 依頼：README以外も実態と照合・修正し、内部向けメモは保持してもREADMEからたどる場所には置かない。
- 今回は文書・文書検査のみ。push・デプロイ・権利判断の変更は実施しない。

## 整理したもの

- README起点のローカルMarkdown到達範囲を28文書から11文書に整理。
- 公開索引から引継ぎ、障害運用、制作・画像生成、台本管理、提出残件メモへのリンクを除去。
- 出典説明を71展示・現在の年次データ・FAO追加系列に合わせ、旧番号・古いGBIF状態と削除素材行を公開索引から解消。個別利用条件と未確認の注意は保持。
- 審査ガイドから削除済みREADME見出しへのリンク、過去の作業報告・公開設定変更に関する作業メモを除去。
- アーキテクチャから旧ガイド版番号と台本管理・制作検証記録へのリンクを除去。
- プライバシー・セキュリティ説明から内部運用手順への導線を除去。利用者向け注意・削除方法は維持。
- 素材台帳の生成元も修正し、再生成で内部の権利判断記録へのリンクが復活しないようにした。
- 元の索引・審査ガイド・出典文書は `pre-release-20260913/` に保全。他の内部記録は削除していない。

## 検証

| 検査 | 結果 |
|---|---|
| `node scripts/check-public-docs.mjs` | 合格：11文書・169ローカルリンク、リンク先の存在・見出し・公開文書許可リスト・内部参照を検査 |
| `node scripts/check-contest-submission.mjs` | 合格：実装の主要な公開説明との契約、公開導線検査を含む |
| `npm run check:rights` | 合格：338素材・116出典。権利許諾やリリース承認の合格ではない |
| `node scripts/preview-public-docs.mjs` | 合格：実Chrome 1366×900、公開索引・審査ガイド・出典説明・台帳の簡易Markdown表示に横はみ出しなし |
| `git diff --check`（今回の公開文書） | 合格 |

簡易プレビューはGitHubの実レンダラーではない。画像は `artifacts/public-docs-20260913/` に保存。READMEのローカルMarkdownリンクを再帰追跡する検査であり、GitHubのファイル一覧、外部サイト経由、ソースコード内の文字列まで隠すアクセス制限ではない。内部ファイルはリポジトリに残る。

## リリース前の未解決事項

1. `npm run check:contest` は初期画面の保守的な未圧縮合計1,840,418 bytesで停止。検査の1MB上限を超過。今回、資産や上限は変更していない。
2. 当初 `node scripts/check-release-rights.mjs --validate` は対象ハッシュ不一致で停止。後続依頼で変更範囲を照合し、台帳更新を確認対象に反映して解消。`npm run check:release-rights` とゲート回帰21試験が合格。詳細は `RIGHTS_SCOPE_RECONCILIATION_20260913.md`。新しい提供元許諾を取得したという意味ではない。
3. 直前のAI・統計ラボUI変更は中断時点のローカル未完了・未検証状態。今回の文書検査で機能合格と扱わない。

全機能テスト、実機Safari、公開版との一致、デプロイ後スモークは今回実施していない。公開文書では過去の画面検証を現在版の全機能合格と表現しない。
