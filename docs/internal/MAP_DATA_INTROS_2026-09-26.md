# 全展示へのデータ解説追加 — 2026-09-26

## 依頼と実装

ユーザー原文：「残りの演出も同じように説明加えておいて」。
直前の指示「帯の高さは変わらないようにタイポグラフィ調整して」も維持する。

- 01の試作を全71展示へ拡張。各タイトルの後に、データの対象・性質、光や色との対応、短い出典を表示する。
- 元のタイトル帯を保持し、高さは従来の `clamp(180px, 24vh, 260px)` のまま。本文のみ切り替え、約7秒（フェードを含む）後に消える。地図の描画・再生を止めず、操作時はその操作を妨げずに解説を閉じる。
- `src/exploration/map-data-intro-catalog.js` に71件の文言と取得状態の説明を集約。JA・EN・zh-CN対応。探索の遅延ローダーで、コンテンツ定義の後・app.jsの前に読み込む。
- `app.js` の共通タイトル切り替えから、その時点の展示番号に対応する説明を読む。データの再取得で同じタイトルが更新されても再表示しない。素早い展示切り替え、閉じる操作、非表示タブで古い説明を残さない。
- モデル値・衛星観測・統計・年度記録・届出量を区別する。取得中・保存値・演出用の参考値・取得失敗の表示は各プロバイダーの状態に追従する。
- HTMLは固定の風の説明ではなく共通スロットを持つ。キャッシュ用URLを更新。packageのバージョン番号は変更していない。

文言は `app-content.js`、planet/FIRMS/live/e-Stat/marine/foodの各プロバイダーとカタログ、描画方法と照合した。例えば、pHは年度最小値、DOは年度平均、気圧は現地気圧、Oxは昼間の年平均、生物調査は個体数でなく分類群数として説明する。森林と雨など、比較から因果関係を断定しない。

## 対象版と合格した検証

対象：`3b8a0f12b8844094101dd8d578c30ef40778b01b` **＋未コミット変更あり**。以前のアイコン修正など、既存の変更も含む作業ツリー。主要ソースのSHA-256は下記JSONに記録している。

- `node scripts/check-map-data-intro-catalog.mjs`：71件の番号・必須フィールド・分類、137件の3言語メッセージ、プロバイダー別の取得状態、ロード順を確認。
- `node scripts/check-map-data-intros-browser.mjs`：インストール済みChromeで全71件を実際の展示メニューから選択。番号と表示本文の一致、古い本文が残らないこと、タイトルの終了、帯内の位置、横はみ出しを確認。網羅切り替え試験のみタイトルアニメーションを20倍速にした。
- 同試験の書体・折り返し検査：320×568、390×844、844×390の3寸法 × JA/EN/zh-CN × 71件、計639条件。カタログ本文と長い取得状態の注記を実DOMへ入れて測定し、全件が元の帯の高さに収まること、320pxでズーム操作欄にかからないことを確認。これはレイアウトを分離した試験であり、639回のプロバイダー入場試験ではない。
- 同試験の実入場：04・16・31・49・68・70へスマホ幅／横長画面の直接リンクで入場。こちらは通常速度でタイトル→解説を確認。解説表示中に操作シートをタップすると、解説が消えてシートが開く。合計77件の入場／切り替え試験。
- `node scripts/check-wind-data-intro-browser.mjs`：1440×900、390×844、844×390、320×568で既存の01を回帰確認。自然な7秒の表示、実フェード、タイトルから解説への帯の連続表示と高さ変化0、地図フレームの更新・再生継続、出典ボタン、操作シート、再入場、02→01→02の途中切り替え。320pxは動きの低減設定も検証。
- `node scripts/check-wind-data-intro-browser.mjs --languages`：英語320px、中国語390pxの自然な表示・参考値注記・操作を確認。
- `npm run check:mode-loader`：38項目のライフサイクル検査を含め合格。カタログ追加だけを基準に反映し、他の資産順序の固定検査は維持。
- `npm run check:security-policy`、locale bundleの生成内容同値チェック、app.js／新規カタログの構文、`git diff --check`：合格。
- ブラウザー試験でJS例外・CSP違反なし。04・31・70のPC画像、16・31・49・68のスマホ／横長画像などを目視し、説明文と帯・既存操作の位置を確認。

証跡：

- `artifacts/map-data-intros-2026-09-26/report.json` および各PNG。
- `artifacts/wind-data-intro-2026-09-26/all-exhibits-regression/report.json`、`handoff-frames.json`、各PNG。
- `artifacts/wind-data-intro-2026-09-26/all-exhibits-regression-languages/report.json` およびPNG。

Chromeはローカルサーバーに本番相当のCSPを適用。外部APIは遮断、風の試験の一部は明示的な気象レスポンスのモックを使用。実機スマホ・外部APIの実配信・本番公開の検証ではない。保存・書き出しの形式や配布ZIPは変更していない。

## 残件・公開状態

- 今回の解説追加はローカル検証まで完了。コミット・プッシュ・デプロイは未実施。
- 既知の1440px・06 CO₂での凡例と展示メニュー日本タブの重なりは未修正。解説の追加とは別の既存レイアウト問題で、今回の試験では地域タブにフォーカスしてEnterで操作した。詳細・次に必要な修正は `MAP_ACTION_ICONS_2026-09-26.md` に保持する。
- 以前の文書・素材などの未コミット変更は保持した。
- ローカル確認：`http://127.0.0.1:4492/#world-01`。展示を切り替えると、その展示のタイトルの後に解説が出る。

## 追記：否定形の注意書きを削除

ユーザー原文：「～ではありません　みたいなの　うぜえからいらねえ」。

- 全71件の冒頭解説を再点検し、「〜ではありません」「〜は判断できません」などの但し書きを削除。本文はデータの対象と可視化の読み方に絞った。JA・EN・zh-CNすべて同じ方針に合わせた。
- モデル値・推定値・年度最小値・重量ベースなど、データが何を表すかという諸元と出典は維持。参考値の表示は「演出用の参考値を表示しています。」に短縮し、取得失敗は「データ取得エラー」とした。出典パネルや実データの計算・描画には手を加えていない。
- 帯・書体・高さ・表示時間・操作は変更せず、カタログとローダーのキャッシュ参照を更新。
- 対象版は同じ完全SHA `3b8a0f12b8844094101dd8d578c30ef40778b01b` ＋未コミット変更あり。前節のJSONは変更前の証跡として保持する。
- カタログ検査に3言語の否定形の但し書きが再混入しない検査を追加。71件・136件の翻訳と状態判定に合格。
- `node scripts/check-map-data-intros-browser.mjs --copy-only`：変更前の全71件の入場証跡を再利用し、今回は代表10件の実メニュー切り替え・6件のスマホ直接入場、全71件×3画面幅×3言語の639レイアウトを再検証。16件の実入場と639条件に合格。PCの04、スマホの16・68などのスクリーンショットを目視確認。
- `node scripts/check-wind-data-intro-browser.mjs` および `--languages` を再実行。4画面幅と英語・中国語で、新しい参考値の文言、フェード・帯の高さ・再生継続・操作を確認。
- モードローダー38項目、CSP、構文、差分空白の検査に合格。
- 今回の証跡：`artifacts/map-data-intros-2026-09-26/concise-copy/`、`artifacts/wind-data-intro-2026-09-26/concise-copy/`、`artifacts/wind-data-intro-2026-09-26/concise-copy-languages/`。各report.jsonに主要ソースのSHA-256を記録。外部API遮断・気象モック・モバイルエミュレーションという検証範囲は前節と同じ。
- 未コミット・未プッシュ・未デプロイ。前節の既知のメニュー重なりと既存の変更はそのまま保持。
