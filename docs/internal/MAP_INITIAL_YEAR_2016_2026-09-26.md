# 年次展示の初期表示を2016年へ（2026-09-26）

## 依頼と対象版

原文: 「初期のPOI表示、2016年にすっか　スライダーも　最新にするといきなり次が最過去になり変であるため」

解釈: 年次の過去データ展示では、初回に表示するデータ・POI・スライダー・再生位置を2016年へ揃える。最新年の直後に最古年へ戻る初回の不連続を避ける。ライブの時間別再生や物語固有の開始年は変更しない。

- 対象: `3b8a0f12b8844094101dd8d578c30ef40778b01b` + 未コミット変更あり。
- 先行するアイコン・説明帯・下部パネル等の未コミット変更は保持。
- アプリのバージョン番号は1.0.0のまま。今回のコミット・プッシュ・デプロイは行っていない。

## 再現と修正

修正前をインストール済みChromeで再現（通常モーション、ローカル実データ）:

- 31: 初期2024年度・2,042地点 → 最初の自動送りで1971年度・314地点。スライダーも2024→1971。
- 70: 初期2023年度・スライダーindex63 → 最初の自動送りで1960年度・index0。
- 証跡: `artifacts/map-initial-year-2016-2026-09-26/before.json` と同階層の画面キャプチャ。

修正内容:

- 共通の初期年選択関数を追加。実際に収録された年から2016年に最も近い年を選ぶ。同距離なら過去年。欠測年の値を作らない。
- 06・10・11・14: 基本タイムラインの初期位置を2016年に設定。遅延読込完了時にも適用し、明示されたURL時刻・ユーザー操作・物語側の指定を上書きしない。
- 21〜30: 都道府県年次展示のスライダーと遷移元を2016年のindexに揃える。
- 31〜69: 年度ファイル自体を2016年で読み込む。65〜67（PRTR）の収録開始は2018年度のため、実在する最寄りの2018年度を使う。
- 70: 2016年開始。71: 原資料が3年平均なので、中心年2016に対応する「2015〜2017年」を選ぶ。
- 食料展示のセッション内の手動選択は維持。別の年度展示への切替は初期年から始める。
- ライブ展示の既存の時間別再生、物語内の1958年開始、年次ではないスライダー、データ取得元・元データ・統計計算・書き出し形式は変更していない。
- 変更したJSの読込URLを更新し、親ローダーまでキャッシュキーを更新。

変更ファイル: `app.js`、`src/exploration/{initial-observation-year,estat-exhibits,marine-cod-exhibit,food-exhibits,index}.js`、`gaia-mode-loader.js`、`index.html`。再実行用の3テスト・package.jsonのテストコマンド・本記録を追加。

## 検証

環境: ローカルHTTP（127.0.0.1:4492）、インストール済みChromeのheadless実行。1440×900と390×844のモバイル幅エミュレーション。ローカルに本番相当CSPを付与。HTTPS外部APIは遮断し、リポジトリ内の実際の保存データを使用（モック値で置換していない）。実機スマホ・本番配信・現在の外部ライブAPIは未検証。

合格:

- `npm run check:initial-observation-year`: 共通関数の空配列・不正値・順不同・同距離・最寄り年と、実データ58系列を検証。初期年の次に後続年が存在することを確認。
- `node scripts/check-initial-observation-year-browser.mjs`: 18ケース。デスクトップ06・10・11・14・21・24・31・38・54・63・65・68・69・70・71、モバイル幅31・70・71。初期データ・スライダー・年表示の一致、POI件数と元ファイルの座標・計測値の一致、食料の実数値を確認。
  - 21・31・70・71（モバイルは31・70・71）は通常モーションの実際の自動送りを待ち、2016→2017（71は2015〜2017→2016〜2018）を確認。その後、キーボードによるスライダー操作でさらに次へ進めることを確認。
  - 基本タイムライン4展示はreduced-motionで初期位置と描画年を固定して確認。
- `node scripts/check-initial-year-entry-paths-browser.mjs`: 8件。URLの明示的なtime=0、70の手動2018年選択の復元、展示切替後の初期年、15の既存の時間別再生開始位置、デスクトップ/モバイル幅の物語保存地点読込→1958年から再生→物語に戻る操作。
- `node scripts/check-initial-observation-year-browser.mjs --paint-only`: デスクトップ/モバイル幅の31・70・71。初期年のまま登場アニメーションの完了を待ち、POI/国別塗り分けが実際に描画されたこと、描画に使った年とスライダーの一致を確認。自動送りを停止して描画状態を採取する補足試験。
- JS構文チェック（app/3プロバイダー）、`npm run check:mode-loader`（38 lifecycle checks等）、`npm run check:security-policy`、`git diff --check`。

画面・状態・検証時のソースSHA-256: `artifacts/map-initial-year-2016-2026-09-26/{after,paint,entry-paths}/`。ブラウザ試験はページ例外・CSP違反なし。

## 状態と残件

- 今回はローカル反映・検証まで。公開未反映。全71展示の実機網羅や全書き出し経路の再検証はしていない（保存/書き出し処理は今回変更なし）。
- 先行の下部パネル・アイコン・説明帯の検証記録は同フォルダーの各記録を参照。既知の06上部メニューへの凡例の重なりは年次初期化では変わらず、今回の修正完了で解消した扱いにしない。必要な次対応は上部メニューの重なり順・当たり判定の修正と実ブラウザ回帰確認。

## 追記: ローカルコミット依頼

ユーザー原文: 「コミット」。2016年開始と、同じ作業ツリーに残る先行依頼のアイコン・説明帯・下部パネル統一、対応するテストと記録をローカルコミットする。プッシュ・デプロイの指示はない。

- コミット前に初期年58系列、解説71件/翻訳136件、モードローダー38項目を含む検査、CSP、locale bundle同値性、差分空白を再確認して合格。
- 初期年ブラウザ試験18件＋実描画6件＋入口/復元8件の合格記録を照合。記録対象の実装ファイルはSHA-256が現状と一致。直前の実ブラウザ試験をそのまま再利用し、コミット操作だけのために同じ操作を再試験していない。
- 先行検証のアイコン処理、解説カタログ、翻訳バンドル、統一ドックCSS、レスポンシブCSS、食料元データもそれぞれ対応する最終合格記録とハッシュ一致を確認。
- 別作業として残っていたREADME・提出資料検査、以前からの検査URL補正2件、出力素材・図のパッケージ処理・thumbnailは今回のコミットに含めず保持。
- コミット後の完全SHAと対象ファイルのGit blob照合は、ローカル証跡 `artifacts/map-initial-year-2016-2026-09-26/local-commit.json` に記録する。公開・本番確認は未実施。
