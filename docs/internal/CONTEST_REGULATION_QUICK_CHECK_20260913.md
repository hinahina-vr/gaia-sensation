# 応募規定の簡易チェック — 2026-09-13

対象：開発中のローカル作業ツリー（基点cb5e443、未コミット変更あり）。依頼どおり軽量な書面・コード照合のみ。全画面再試験や応募手続きは実施しない。最終提出物の合格認定ではない。

根拠：ユーザー添付の評価項目・応募規定画像、および当日確認した[公式要項](https://progedu.github.io/webappcontest/2026/summer/index.html)。締切は2026-09-13。終了時刻は今回確認できていない。

## 必須条件と残件

| 項目 | 簡易判定 |
|---|---|
| HTML/CSS/JavaScriptによる動きのある作品 | 画面側は該当。独自DOM・Canvas・WebGL実装 |
| 外部ライブラリ | 注意。ブラウザ側の確認範囲では禁止例のライブラリや外部CDN scriptは検出していない。ただしsensor-platformはgeotiff 3.0.5を実行依存に持ち、live-senseware.tsが静的import。提供元画像を読むサーバー側の用途でも、規定上の例外と明記されていないので無条件合格にしない。運営へ用途確認するか提出構成から実依存を除く対応が必要。フラグ無効だけでは依存の除去ではない |
| 提出用GitHub | Public必須。privateは準備段階のみ。開発側private・提出側publicで運用可能。新提出先未作成のため未達。審査用ブランチは推奨であり必須ではない |
| 動作するサイトURL・提出コードとの一致 | 現在のURLはあるが新提出版は未確定。固定コミットからの配信と一致確認が必要。GitHub Pages以外のホスティングも可 |
| PC・スマホのChrome | 過去のサイズ変更試験は存在するが、現在の未完了UIを含む最終候補の正常動作は未認定。提出直前に両方の主要導線を確認する |
| 著作権・利用条件 | 台帳・権利scope検査は直前の作業で合格。ただしowner-acceptedは提供元許諾確認済みではない。GOSAT等の条件付判断と生成素材の条件を維持。機械チェック合格を権利保証にしない |
| 応募資格・フォーム・公開維持 | 所属に合う応募フォーム、参加資格、提出後の公開維持は応募者側で確認。今回フォーム送信やアカウント確認は未実施 |

## 評価項目（必須条件と区別）

- 整理・コメント・テスト：srcによる分割、主要ファイルの概要コメント、scriptsの回帰試験、GitHub Actions設定を確認。未使用データ・内部メモの整理は提出用切り出し時の残件。
- GitHub活用：新リポジトリは履歴なしになるため、以後の本番更新・CI結果をそのリポジトリに残す。評価対策で制作履歴を捏造しない。
- 完成度・UI/UX・初見案内：案内とtourはある。開発中のため最終合格とはしない。
- アイデア：物語と地球データ探索・観測参加を組み合わせた構成は説明可能。新規性の採点や実利用者数は今回検証していない。

実行：`node scripts/check-contest-submission.mjs` 合格（公開文書11件・169リンクも合格）。HTML入口、主要コード、package依存と概要コメントを確認。`check:contest` 全体は今回再実行していない。既知の初期読込1MB超過はプロジェクト内部の検査上限で、今回確認した公式要項の容量制限ではない。

公開設定・geotiffの扱い・最終Chrome動作／公開コード一致が主な提出前チェック。準備用リポジトリや本番設定の変更、データ削除、コード修正は今回なし。
