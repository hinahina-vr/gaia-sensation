# 地図下部タイトル欄の内枠削除

## 依頼と変更

ユーザー原文：「06-15のデザイン、四角い枠みたいなの外してほしいんだけど」。添付は06の下部タイトル欄。

- `.map-command-dock::before` が描いていた内側の四角い枠・塗りを削除。
- タイトル欄 `.map-mode-bank` に旧パネル装飾として残っていた `::before` の上辺と `::after` の四角い背景も削除。
- 外側の丸いパネル、列間の区切り、数値欄、文字サイズ・位置、出典／分析ボタンは保持。タイトル欄から下側の展示メニューを開く操作も保持。
- 対象の内枠が表示されるのは06〜14の共通ドック。15のライブ用ドックは元からこの内枠を使っておらず、余計な変更をせずに表示と操作を照合した。
- 変更ファイル：`map-ui-grid-polish.css`。CSSとローダーのキャッシュ参照更新：`gaia-mode-loader.js`、`index.html`。版番号は据え置き。

## 対象版・検証

`3b8a0f12b8844094101dd8d578c30ef40778b01b` ＋未コミット変更あり。以前の解説・アイコン等の変更を含む。既存の変更は保持。

- インストール済みChromeで変更前の内枠を再現し、変更前の画面と位置・寸法を保存。
- `node scripts/check-map-dock-frame-browser.mjs`：1440×900で06〜15全件と隣接16、3840×2160で06／15、390×844で06／15、計15条件に合格。
- 全15条件で変更前後のタイトル／操作ボタン／ドックの位置・寸法が一致。外側のパネルの枠・角丸・背景も一致。
- 06〜14で内枠とタイトルの矩形装飾が生成されなくなったこと、15／16とスマホ側の既存装飾が変わらないことを実DOMで確認。
- PCでは実際にタイトルを押して、下側に展示メニューが開くことを確認。モバイルは直接リンクから入り、操作シートの開閉を確認。PCの地域タブ選択は、既知の凡例重なりを避けるためフォーカス＋Enterで実施。
- 1440pxの06の変更前後画像を目視確認。JS例外・CSP違反なし。
- `npm run check:mode-loader`（38項目を含む）、`npm run check:security-policy`、テスト構文、`git diff --check`：合格。
- 証跡：`artifacts/map-dock-frame-2026-09-26/{before,after}/report.json`、各PNG。レポートに対象ソースのSHA-256を記録。`--before` は修正前の再現用、通常実行は修正後の回帰試験。beforeの証跡があれば寸法も照合する。

外部APIは遮断しローカルの保存値を使用。CSPはローカルへ適用したもの。実機スマホや本番の確認ではない。保存・書き出し・配布形式の変更なし。

## 公開状態と残件

ローカルのみ。未コミット・未プッシュ・未デプロイ。
既知の06の凡例と上側メニューの日本タブの重なりは、内枠の装飾と異なる表示優先順位の問題で引き続き未修正。詳細と必要な対応は `MAP_ACTION_ICONS_2026-09-26.md` に保持。今回の下側メニューの開閉は合格。

確認URL：`http://127.0.0.1:4492/#world-06`。

## 追加依頼：05に合わせたデザインへの統一

ユーザー原文：「05とかと同じ感じにしてほしいんだけど」。枠だけを消す前段の対応では不足していたため、05を変更せず実ブラウザーで基準として比較した。

- 06〜14の内側のカード装飾を数値欄からも削除し、05と同じ一枚の下部パネルに統一。左右の余白、影・ぼかしを05に揃えた。
- 06〜15のカテゴリ／番号／タイトルの文字サイズ、行高、間隔、開始位置を05に統一。カテゴリ固有の色は保持。
- 15の飾り線・円形装飾も除去。観測地点・時間操作・数値表示など、各展示に必要な情報は保持。
- 1200px以下ではカテゴリと見出しを同じ下段に配置。旧指定による06〜11の行高ずれ、12〜14の上段配置、15の見出し領域の過大な高さを合わせた。1201〜1400pxでは05と同様に一段のパネル内に収める。
- スマホのCSSと05／16のデザインは変更しない。
- 実装：`map-ui-grid-polish.css`、`map-unified-dock.css`。資産参照：`gaia-mode-loader.js`、`index.html`。版番号は`1.0.0`のまま。

### 検証版・結果

`3b8a0f12b8844094101dd8d578c30ef40778b01b` ＋未コミット変更あり。以前の解説・アイコン等の変更を保持。

- `scripts/check-map-dock-reference-browser.mjs`：最終32条件に合格。1440×900で05〜16、1100×900で05〜15、1366×900・3840×2160・390×844で05／06／15。
- デスクトップでは05を基準に、実DOMの書体・文字サイズ・行高・各行の開始位置・高さ・パネルの背景／余白／影／ぼかしを照合。タイトルの矩形装飾がないことも検証。
- 下側メニューの開閉、出典を開いて閉じる操作を全デスクトップ条件で実施。06の統計分析はチャートの実描画と点数まで確認。06／15の時間スライダーをキーボード操作し、15では地点送りによる表示更新も確認。
- 05／16のデスクトップの見た目・位置は変更前と一致。スマホでは背景・タイポグラフィ・下端の位置が一致。ライブ取得状態の文言によるパネル高さの変動は、CSS変更と区別して扱った。
- 15の出典開閉→時間操作→地点送り→16への連続遷移も補足検証し、パネル位置を確認（`navigation-controls.json`）。出典パネルの閉じアニメーションが終わってから次の操作を行う。
- 1440pxの05／06／15、3840pxの06、1100pxの06／15、1366pxの06の画像を目視確認。横はみ出し・JS例外・CSP違反なし。
- `npm run check:mode-loader`（38項目を含む）、`npm run check:security-policy`、両ブラウザーテストの構文、`git diff --check`：合格。
- 証跡：`artifacts/map-dock-reference-2026-09-26/{before,after}/report.json`、各PNG、`navigation-controls.json`。最終レポートは29件のPC合格後に`--resume`でスマホ3件を完了。再開時に実装4ファイルのSHA-256とGit SHAの一致を検証しており、別の実装版の証跡は流用しない。
- 旧`check-map-dock-frame-browser.mjs`の寸法固定チェックは、今回意図的に変更した06〜15を除外し、スマホ／16の非変更部分に維持した。新しい05基準の回帰テストを優先する。

インストール済みChromeを使用。外部APIは遮断、保存値で検証。スマホはエミュレーションであり実機確認ではない。保存・書き出し・配布形式の変更なし。公開状態は引き続き未コミット・未プッシュ・未デプロイ。上記の既知の上側メニュー／凡例の重なりは今回の下部パネル修正とは別の未修正事項として保持。

## 再指摘：フォントの位置と大きさ

ユーザー原文：「フォントの位置と大きさ　05とかと合わせてほしいんだけど」。添付は15。ローカルか公開サイトかは質問済みで、作業時点では回答未受領。

- 新規起動のローカルChromeでは差を再現できなかった。1920×1080 CSS px／DPR 2／通常アニメーションで、05／06／15の文字要素だけでなくテキストの`Range`の座標・高さまで一致を確認した。
- この条件でカテゴリ11px、番号26px、見出し19.968px。カテゴリと番号の左位置はタイトル領域の左から50px。行高はカテゴリ15.4px、番号と見出し30px。表示幅に応じた既存の可変サイズを保持した。
- 一致済みのCSSをさらに別サイズへ変更せず、`gaia-mode-loader.js`のCSS URLと`index.html`のローダーURLの読み込み識別子を更新。古いページからも通常のページ読み込みが発生する確認URLを用意：`http://127.0.0.1:4492/?ui=chapter-type-20260926-2#world-15`。キャッシュ残留は可能性であり、利用中の画面の原因と断定しない。
- 回帰テスト`node scripts/check-map-chapter-typography-browser.mjs`を追加し、05／06／15の文字・行ボックス・テキストRangeの一致、新しいCSS URLの読み込み、JS例外／CSP違反なしに合格。
- `npm run check:mode-loader`、`npm run check:security-policy`、構文チェック、`git diff --check`に合格。CSS2ファイルのSHA-256は前項32条件の検証版と同一であり、その画面・操作の証跡も保持。
- 証跡：`artifacts/map-chapter-typography-2026-09-26/before.json`、`after.json`、`before-05.png`／`before-15.png`／`after-05.png`／`after-06.png`／`after-15.png`。対象は引き続き`3b8a0f12b8844094101dd8d578c30ef40778b01b`＋未コミット変更あり。

公開サイトへの反映は未実施。利用中の画面と検証画面の差の原因は、場所・読込版の確認が残る。ローカル一致をもって、添付の利用環境の不具合解消を確認したとは扱わない。

## 追加依頼：16以降の見出し／70・71のパネル

ユーザー原文：「16以降も同様に合わせて」「70と71はデザインそのものが違うので直して」。前者は下部のカテゴリ・番号・タイトルを05基準に拡張、後者は70・71の下部パネル全体も05系の表示へ統一する依頼として対応。地図本体の描画や食料データの定義は変更しない。

- `map-unified-dock.css`：ライブ、統計、観測記録、食料の各見出しを共通化。05と同じ書体・サイズ・行高・開始位置・上下の配置にした。カテゴリーの固有色は保持。
- 70・71は背景・影・ぼかし・境界線・列の配置を05基準に統一。国・品目／指標・期間の選択、数値、出典／分析ボタンを維持。1200px以下では操作を上段、見出し・値・アクションを下段に収める。
- 1366pxの実表示で31以降の旧1399pxブレークポイントと共通100px高の衝突を再現。見出しが下へ76px押し出されていたため、1201〜1399pxでは一段の配置へ揃えた。69の記録型に残っていた上下余白の違いも修正。
- `responsive-audit-fixes.css`に残っていた70・71の旧1399px以下の強制二段指定も削除。1366pxで161px高に膨らみ、上段へ見出しがずれることを実表示で再現した。食料パネルのサイズ指定は共通CSSの1200px境界へ一本化し、当該CSSのキャッシュ参照も更新。
- スマホと物語内の埋め込みMAPは共通化CSSの対象外。05の基準の文字サイズ・配置は維持。
- キャッシュ参照を`gaia-mode-loader.js`／`index.html`で更新。版番号据え置き。既存の未コミット変更は保持。

### 検証記録

対象：`3b8a0f12b8844094101dd8d578c30ef40778b01b`＋未コミット変更あり。変更前38条件の実DOM・画面を保存。

- 全体照合97条件に合格：1440×900で05／06／15および16〜71全件、1100×900・1366×900・3840×2160で05／06／15／16／21／24／31／63／69／70／71、390×844で16／21／31／70／71。
- その後の最終目視で、71の長い補足文が数値見出しを上に押し出す状態を発見。食料パネルの数値欄だけを固定3行のグリッドへ変更し、補足文は領域内のスクロールで全文を保持。見出し・数値の枠外への押し出しを防ぐ回帰判定を追加した。
- この最終変更後に`--quick --focus-food`で影響箇所と基準・隣接69・スマホを21条件再検証し、6つの食料操作経路も再実行して合格。97条件版と最終補足欄調整版の実装ハッシュは異なり、それぞれ`after`と`quick`に別記録を保持。最後に`quick`／`food-controls`の全ソースハッシュが作業ツリーと一致することを確認した。

- 回帰テスト：`scripts/check-map-remaining-headings-browser.mjs`。05を実表示して比較し、カテゴリ／番号／見出しの計算済み書体・サイズ・行高とテキストRangeの座標を照合。70・71ではパネル素材も比較。代表例では実際のタイトルクリック→下側メニュー→閉じる操作を検証。
- 操作テスト：`scripts/check-food-dock-controls-browser.mjs`。1440／1366／1100px、70／71の6経路で2種類の品目／指標、国選択、Home／Endによる期間変更、保存元JSONとの値照合、実際のクリック到達位置、出典開閉、統計チャートと行数、下側メニューを確認。日本の観測値がない指標で分析が無効になることを保ち、実測記録のある国に切り替えて分析を検証。
- `npm run check:mode-loader`（38項目を含む）、`npm run check:security-policy`、テスト構文チェックに合格。ローダーの重複読み込みテストは、古い比較基準のキャッシュURLではなく現行マニフェストのURLを既存DOMへ配置するよう修正した。資産パス・評価順序の基準照合は維持。
- `check-map-chapter-typography-browser.mjs`も最終資産参照で再実行し、1920×1080／DPR 2で05・06・15の書体、行ボックス、テキストRangeの一致を確認した。
- 1440pxの16／21／70、1366pxの31／69／70／71、1100pxの70／71、4Kの70、390pxの70を目視確認。最終版の70・71は数値見出しの押し出しがなく、出典・分析ボタン、選択欄、スライダーが操作可能。JS例外・CSP違反なし。`git diff --check`合格。
- 証跡：`artifacts/map-remaining-headings-2026-09-26/{before,after,quick,food-controls}/report.json`とPNG。Git SHA、未コミット状態、実装／資産参照ファイルのSHA-256を保持。

インストール済みChromeをヘッドレス実行。外部APIは遮断し、同一オリジンの実保存データを使用。スマホは390×844のエミュレーションで、実機・公開サイトの確認ではない。保存／書き出し／配布形式の変更なし。コミット・プッシュ・デプロイは行っていない。既知の06上側メニューと凡例の重なりは今回の下部パネルとは異なる優先順位の問題で、上記の未修正記録を維持。

確認URL：`http://127.0.0.1:4492/?ui=all-chapters-food-20260926#world-70`。
