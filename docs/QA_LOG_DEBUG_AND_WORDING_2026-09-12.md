# LOGデバッグ表示と指定セリフ — 2026-09-12

## 依頼への対応

- 通常のログでは添付された表示範囲タブ、コメント件数／削除／Markdown書き出し、IDコピー、修正コメント欄を非表示。本文・話者・閉じる操作は維持。内部IDとコメント済み強調も通常表示では隠す。
- ログを開いた状態で半角 `ruu` を続けて入力するとデバッグ表示へ切り替える。再度 `ruu` で通常表示へ戻り、全台本を開いていた場合も既読ログへ戻す。表示切替のみで保存済みコメントは削除しない。
- キー間隔は2秒以内。編集欄、IME変換、修飾キー、キーリピートは除外。ログを閉じている間は発火しない。デバッグ状態は永続化せず、再読込は通常表示から開始する。秘密の入力は認証・アクセス制御ではない。
- 再開依頼があった `circle_invitation_030` も完了。ユーザー指定の「それぞれの技術とこだわりをぶつけ合う場所です」へ差し替えた。話者、ID、他のセリフは変更なし。
- `story/log-wording-20260912.js` を生成時の明示的な差分として適用。過去の提出台本・LOG指示履歴は保持。現行実行データと現行統合台本を更新し、歴史的テストはこの1件を元に戻した入力で従来の完全一致契約を継続する。
- 通常のローカルChromeを `http://127.0.0.1:4492/#story` で起動。ゲームの保存データを検証用データで上書きしていない。

## 実ブラウザー回帰試験

`node scripts/check-log-debug-browser.mjs`（`npm run check:log-debug:browser`）。インストール済みChrome headless、1440×900／390×844、分離した保存領域と本番相当CSP。スマホ幅はエミュレーションで、実端末ではない。

- 通常ログで対象UIが不可視、既読本文が表示されること。
- `ruxuu` では切り替わらず、`ruu` で復帰。コメント欄に `ruu` を入力しても切替なし。
- 既存コメントの復帰、変更の保存、デバッグ→通常→再表示、再読込後の通常表示とコメント保持。
- IDコピー、ID＋本文コピーをChromeのClipboard APIで読み戻して照合。WindowsのCRLFはLFへ正規化して比較。
- Codex用Markdownと全台本Markdownを実際にダウンロードし、本文・コメントを確認。全台本表示は本編＋APEIRONCENEの544件。
- 指定セリフを通常の会話・既読LOG・全台本書き出しで確認。スマホ会話では分割ページを進め、連結した全文が指定文言と一致することを検査。
- 通常／デバッグのPC・スマホ幅スクリーンショットを目視確認。ページ例外・CSP違反0件。
- 証跡: `artifacts/log-debug-20260912/report.json`、各幅の `*-normal.png`／`*-debug.png`、ダウンロードされた `*-comments.md`／`*-script.md`。

## 検証版・静的チェック

基点 `bd6c1c3c95ab245507293acd7f2f3714714f2606` ＋既存の表情修正と今回の変更。未コミット・未公開。生成背景3案は組み込んでいない。

- novel-mode.js: `097bb055f07f1dbc745b87081eaeba06046b3269f69be10aa7833c6679fe4bf0`
- novel-mode.css: `7e6d8462bfc6781e4ddbc71ba1d6893c1f1ac87794fac4e13b35c10e7b3e6c78`
- novel-story-data.js: `e82287f925a0904e0cd4d392df482628dd97a8eb46b4683a87842b72af8d055f`
- gaia-mode-loader.js: `519d478a7d24fc7cf6c69ef3b0cc8fbae1d71423826bbb0fae526a549293a2ce`
- index.html: `a2c71e47b2d12da3600750ec2bd545a17df966d1249176c5a69955fa7fb41f21`

JS構文、380ステップ台本整合性と過去LOG契約、現行統合台本の生成結果、ローダーprefetch、CSPハッシュ、差分空白チェック合格。初期読込997,639／1,000,000 bytes。

外部HTTPSは遮断。実スマホ、他ブラウザー、本番配信、ZIP導入は未確認。通常Chromeを開く操作と、自動検証用Chromeの分離プロファイルは区別している。
