# MAP 12 → 13 の切り替え調査（2026-09-07）

## 報告と現在の状態

報告画像では「13 電気のつくり方」という見出しに変わった後も、MAP 12「街と森、そのあいだ」の地図・右パネルが残る。選択欄はハンガリー、比較カードと地図の選択はロシアという不一致も見られる。

現時点では以下の操作で同じ症状は再現していない。原因確定・修正完了とは扱わない。アプリ本体の変更は行っていない。再現操作を残すため `scripts/check-ecologies-mode-switch-browser.mjs` を追加した。

## 検証対象

- ローカル HEAD `63ec6ff`。既存の権利整理に関する未コミット変更はそのまま保持し、本調査では実行時ファイルを変更していない。
- Chrome 152、ヘッドレス実ブラウザー。4K 幅も含めて画像を保存し、切り替え後の描画を目視確認。
- ローカル検証サーバー `http://127.0.0.1:4447`。センサー API は試験用応答であり、センサー実機・本番認証の検証ではない。
- 本番 `https://gaia-senseware.pages.dev` も 1440 × 900 で読み取り・UI 操作のみ検証。公開内容・データ・設定は変更していない。
- オーロラ取得だけは、いずれの試験でも既存スナップショットに置換した。MAP 12 / 13 のデータ・描画処理は置換していない。

## 実施済み

| 条件 | 結果 | 証跡ディレクトリ（artifacts 配下） |
| --- | --- | --- |
| ローカル 1440、MAP 12 の初期状態から左下の次ボタン | 13 の地図に切り替わり、12 のパネル・レイアウトクラスが消える | ecologies-mode-switch-before |
| ローカル 1440、イラク選択 → 自動比較 → 次ボタン | 同上 | ecologies-mode-switch-interactive-before |
| ローカル 3840、自動比較後の次ボタン | 同上 | ecologies-mode-switch-4k-before |
| ローカル 3840、イラク → ロシア → ハンガリーを短間隔で選択 → 次ボタン | 同上 | ecologies-mode-switch-rapid-before |
| 本番 1440、同じ短間隔の国選択 → 次ボタン | 同上 | ecologies-mode-switch-production-before |
| ローカル 390、比較パネルを開く → 国選択 → 閉じる → 展示一覧から13 | 同上 | ecologies-mode-switch-mobile-shell-before |
| ローカル 3840、タブ・国・散布図・世界遺産・自動比較を40周操作後に次ボタン | 同上 | ecologies-mode-switch-stress-before |
| ローカル 3840、ネイティブselectの開閉・キー選択も追加して40周 | 同上 | ecologies-mode-switch-native-stress-before |
| ローカル 2560 × 1392 / DPR 1.5、25周操作。5周ごとに12→13→12、最後に13 | 同上 | ecologies-mode-switch-dpr-stress-before |
| 本番 同じ2560 / DPR 1.5、同じ25周と往復 | 同上 | ecologies-mode-switch-production-stress-before |

各ディレクトリに `report.json` と画面 JPEG を保存。上記試験の JavaScript ページ例外は 0 件。スクリプト構文検査、`git diff --check` も合格。

最初の 390 幅試験は、モバイル専用の「比較・関係図を開く」操作を省いたため非表示の select でタイムアウトした（ecologies-mode-switch-mobile-before）。切り替え不具合の再現ではなく、PC 用試験手順とモバイル UI の不一致。証跡を保持し、実際のモバイル操作経路へ試験を修正した。

## 未確認

利用者のブラウザーで症状が発生する直前の操作列・ページ状態は未取得。新規ブラウザーで再現しないことを理由に、報告された現象が存在しないとは結論しない。物理スマートフォンの動作確認、修正の本番反映は未実施。

利用者からの「ローカルで開いて」に対応し、MAP 12 のローカル URL をブラウザーで開いた。利用者が確認できるようローカルサーバーは稼働を継続している。

利用者から「左下の›」「国選択して右のUIを連続操作すると再現する」と追加情報を得た。上記連続操作でも未再現のため、`scripts/debug-ecologies-browser.mjs` で操作可能な専用Chromeを開いた。対象は `http://127.0.0.1:4447/?exhibit=12#world`。MAP 12 の操作とレンダリング状態・ページ例外だけを `artifacts/ecologies-interactive-debug/report.json` に記録する。他のブラウザーのタブ・履歴・入力は取得しない。原因捕捉用の調査スクリプトであり、本番の配信コードには組み込んでいない。

なお、公開表示番号12への直接リンクは `?exhibit=12` を用いる。`?mode=` は旧レンダラー番号1〜9用である。
