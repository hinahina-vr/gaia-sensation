# 出典パネルを覆うMAP凡例の重なり修正 — 2026-09-12

## 依頼と変更

- ユーザー原文: 「出典押したらかぶるUI下にやってね」。添付は38「気温が刻む一年」の出典本文に気温の凡例が重なる画像。
- 対応解釈: 出典を開いている間、凡例・地図操作UIを出典パネルより背面にする。通常の地図上での凡例の位置は変えない。
- 原因: PCでは出典パネルが `z-index: 100`、気温の凡例が `110`。ヘッダー等にも出典より大きい重なり順があり、本文や操作領域を覆っていた。
- `data-ledger.css`: 出典の背景を800、パネルを801に揃え、700までの通常MAP操作UIより手前、ガイドより背面に配置。スマホの240／241上書きを除去し、同じ順序を使う。
- `gaia-mode-loader.js` と `index.html`: 変更CSSとローダーの読込URLを更新。
- 新規回帰試験: `scripts/check-map-source-layer-browser.mjs`。`package.json` に `check:map-source-layer:browser` を登録。
- 既存の未コミット作業を保持。出典内容・保存データ・書き出し形式・再生ロジックの変更なし。コミット・push・デプロイは未実施。

## 対象版

- 基点HEAD: `9fbd901b68faebaf6148db6e18cdbcb50fa1c778`＋ローカル変更。
- `data-ledger.css` SHA-256: `85c72bc1f4cd1fb2949ce8455cfa34393aab3807fdf8eed85c3cda81f2dd7729`。
- `gaia-mode-loader.js` SHA-256: `695ac5aa86aa968616fae1e4a21ed6b0ffe8530a1155243edfb1e1cf493e75d8`。
- `index.html` SHA-256: `406338faf1b85eb8438ad0735b7604e3505f1a2d29ccdcbfa7d9f19c23688ac6`。
- ブラウザー試験の開始・終了時に上記3ファイルのハッシュ一致を検証。

## 再現・回帰確認

Windowsのインストール済みChrome、ローカル4492、本番相当CSP。幅1440×900、901×768、390×844。

- 修正前: 実際に38の「データの出典」を押し、PC2幅で凡例が本文を覆う症状を再現。出典内部の格子点ヒット判定で、1440pxは36点、901pxは35点に別UIが重なった。スマホは既存の重なり順で0点だった。
- 修正後: 01・02・06・15・21・31・35・38・40・65・68・70・71の13展示×3幅=39経路。クリック／タップで出典を開き、出典領域の格子点で最前面要素がパネル内にあることを確認。単なるCSS数値比較で済ませない。
- 各展示の番号・出典本文・元データリンクの存在、横スクロールなし、長文時のホイールスクロールを確認。
- 38では閉じるボタン、Escape、PCでは背景クリックで閉じる動作を確認。閉じた後は展示を維持し、凡例が再び読めてヒット可能であることを確認。
- 修正前後のスクリーンショットで本文が隠れないこと、通常MAPの凡例の位置を変更していないことを目視確認。
- 39経路すべて合格。別UIによる出典領域の遮蔽、ページ例外、404、CSP違反は0件。
- 証跡: `artifacts/map-source-layer-20260912/before/report.json`、`after/report.json` と各幅の `*-38-map.png`／`*-source.png`。

## ローカルチェックと限界

- 新規試験とローダーの `node --check`、`check-mode-loader-prefetch.mjs`、対象ファイルの `git diff --check`。既存のLF／CRLF変換警告のみ。
- 初期読込容量: `check-contest-entry-budget.mjs` 997,507／1,000,000 bytes。
- 外部HTTPSを遮断し、FIRMS APIはリポジトリ保存スナップショットを使用。実ライブ配信・本番・実スマホ端末での試験ではない。
- 保存・書き出し・外部アプリ連携は変更なしのため、今回の重なり修正では再テストしていない。配布ZIP作成・新規展開・導入先テストは未実施。
- ローカル反映のみ。公開には別途明示指示が必要。
