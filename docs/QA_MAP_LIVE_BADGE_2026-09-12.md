# 展示一覧のLIVEバッジ / 2026-09-12

ユーザー原文: 「LIVEは赤地に白抜きとかでかつ明滅するなどしてライブであることをアピールしてほしい」

添付画像の展示一覧にあるLIVE表示を対象とした。基点は `9fbd901b68faebaf6148db6e18cdbcb50fa1c778` 上のローカル作業ツリー。先行する固定矢印などの未公開変更を保持。今回のコミット・push・デプロイなし。

## 実装

- `map-exhibit-categories.css`: 赤い点＋薄赤文字から、赤地・白抜き文字・白い点のバッジへ変更。背景と光彩が1.8秒周期で明滅する。文字・点の不透明度は常に100%を保つ。
- 赤背景は暗い状態 `#a31830` と明るい状態 `#d52643` を往復。白文字とのコントラスト比は約7.69〜5.01。`prefers-reduced-motion: reduce` では静止した赤地 `#bd203b`（約6.12）にする。
- LIVEの上段スペースを確保し、展示記号の上にバッジが重ならないようにした。記号の文字サイズとタイルの幅・高さは変更しない。バッジ自体はポインターを遮らず、元の展示ボタンを操作できる。
- 対象は既存の01〜05・15〜20の11展示のまま。分類・番号・名前・データ・接続状態判定は変更していない。「LIVEは定期更新の展示（保存値を含む）」という既存の説明を保持。取得済みの保存値を新たにライブ接続と判定する変更ではない。
- `gaia-mode-loader.js` / `index.html`: CSSのキャッシュ識別子を更新。`scripts/check-map-live-badge-browser.mjs` を追加し、`package.json` に実行コマンドを追加。既存の周期表テストは、点ではなくバッジの背景を検証するよう更新した。

## 実画面の確認

ローカルChrome＋Playwright、本番相当CSP、外部HTTPSを遮断。同梱の観測データを使用し、FIRMS APIにはリポジトリの観測スナップショットを応答。タッチと画面幅のエミュレーションであり、物理端末・本番配信・実ライブ接続の確認ではない。

- 変更前: 1440×900・390×844の世界/日本、4条件で透明背景のLIVE表示を記録。
- 最終版: 1440×900・3840×2160・901×900・390×844・320×568・844×390、および動きを抑える設定の1440×900・390×844。世界/日本の計16条件すべて合格。
- 全条件で11展示のみにバッジを表示。白文字、背景のコントラスト、タイル内の収まり、横はみ出しなしを確認。ブラウザーの実際の疑似要素ボックスを取得して、展示記号とバッジが重ならないことも確認した。
- 通常設定は実時間の複数フレームで背景/光彩が変化し、文字の不透明度・タイル内位置・サイズが不変。抑制設定は背景/光彩とも静止。暗い/明るい状態それぞれの画面を保存し、1440pxと320pxの画像を目視確認。
- 各条件でバッジの座標を実マウス/タッチで直接押し、03・15への移動、メニューの終了・再表示を確認。画面切替が完了してからメニューを再度開くようテストを調整した。
- 1440pxと390pxの変更前後142タイルを比較し、名前・記号・記号の文字サイズ・タイルの幅と高さが同一であることを確認。
- 既存の `check-map-periodic-picker-browser.mjs --focused`: PC/スマホの世界/日本4条件合格。ガイド、タブのキーボード操作、PCの55タイルのヒット領域、スマホのスワイプ/カテゴリ移動、44/16/70の選択、退出後の次送りを確認。
- ブラウザー例外/CSP違反なし。検証前後の対象ファイルハッシュ一致。JS構文確認、セキュリティポリシー（6ハッシュ）、入口容量（997,472 / 1,000,000 bytes）、`git diff --check` 合格。

## 証跡と版

`artifacts/map-live-badge-20260912/before/` に変更前、`after/` に最終16条件と画像、`picker-regression/` に既存操作の4条件を記録。`edges/` は中間のテストであり最終合格版ではない。そこでの重なり判定は、提供元読込で移動中のポップアップの異なる時点の絶対座標を比較していたため、最終テストでは同じタイル内の相対座標で実ボックスを比較している。CSSの実装はその間変更していない。

| 最終ファイル | SHA-256 |
| --- | --- |
| map-exhibit-categories.css | 2a087a8b51c15fee9ef024948caec63de3b72b888d6f0bfaa7cc6b0736dbabf7 |
| gaia-mode-loader.js | 48eaf10698a2647da58b3921df05b4b06aea519e372bc28707967a6dcb2b6fe3 |
| index.html | b3f3f3a23699d2ae56bdadd1ef58559751be8da5bd5861e9f6fc25022dfa49f2 |

ローカル実装・確認まで完了。保存/書き出し形式・配布物の変更なし。この表示変更では保存/書き出しの全経路は再試験していない。固定矢印のJS/CSSは前回合格版と同一。先行する未公開作業・既知の残件は各QA記録を維持し、今回の表示調整で解決したとは扱わない。
