# 「見どころへ」のデザイン / 2026-09-12

ユーザー原文: 「見どころへ　ダサいんでもっとこだわって」

添付画像はMAPのタイトル画面にある「見どころへ」ボタン。基点は `9fbd901b68faebaf6148db6e18cdbcb50fa1c778` 上のローカル作業ツリー。先行するLIVE表示・固定矢印などを保持。コミット・push・デプロイなし。

## 変更

- `mode-entry-guide.js`: ボタンの中身だけを変更。星と軌道のSVG、小さなHIGHLIGHTS表記、明朝の「見どころへ」、円形の矢印で構成した。既存のボタン属性・読み上げ名・イベント処理・タイマー・保存設定は保持。
- `mode-feature-intro.css`: 細い青緑の光の輪郭、暗いガラス面、上辺の反射、内側の陰影を持つ横長カプセルへ変更。四角いフォーカス枠を丸い輪郭に合わせた。
- ホバーでは縁が明るくなり、星の軌道が少し回転、矢印が右へ3px進む。押下は1px沈む反応。動きを抑える設定では位置・回転の変化とトランジションを停止し、色とフォーカス表示は維持。
- タイトルの文字、背景の80%濃度、約2.4秒後に紹介へ進む処理は変更しない。新規画像・フォント・外部ライブラリなし。既存の共通ボタンの光演出も利用。
- `gaia-mode-loader.js` / `index.html` / `sensors/index.html`: 共用JS/CSSのキャッシュ識別子を更新。新しい回帰テストと `check:entry-highlights:browser` コマンドを追加。

## 実ブラウザー確認

インストール済みChrome、ローカル `http://127.0.0.1:4492`。新規回帰テストは本番相当CSP、外部HTTPS遮断、同梱のFIRMSスナップショット応答。PC/タッチのエミュレーションであり、物理端末・本番配信の確認ではない。

- 変更前: 1440×900・390×844で元のボタンとタイトル画面を保存。
- 最終版: 1440×900・3840×2160・901×768・390×844・320×568・844×390、および動きを抑える設定の1440×900、計7条件合格。
- ボタン・内部の文字/SVGが画面内に収まり、44px以上の操作領域、横はみ出しなし、初期フォーカスと実ヒット領域を確認。タイトル文言・文字の影なし・背景80%は従来どおり。
- 実際のEnter/タップで紹介へ進み、「操作ガイドを見る」→ガイドの次ページ→Escape、ガイド再表示→Tab/Space→開始まで検証。PC/スマホではタイトル途中で閉じても復活しないこと、自動遷移のタイミングも確認。
- 1440pxの通常/抑制設定2条件で、実マウスを押したままの状態を追加検証。通常時は押下変形、抑制設定では変形なし。離すと紹介へ進み、後続のガイド・再表示・自動遷移も合格。
- 1440px全画面とボタン、390pxボタンの実画像を目視確認。文字と図形の間隔、光の輪郭、画面全体とのバランスを確認した。
- 共通JSを使う `/sensors/#map` も390×844で実際に開き、既存の3枚の見どころカード、タイトル用ボタンが非表示であること、開始ボタンで閉じられることを確認。センサー機器・実アカウント・接続データの検証ではない。
- JS構文確認、セキュリティポリシー（6ハッシュ）、入口容量（997,576 / 1,000,000 bytes）、`git diff --check` 合格。ブラウザー例外・新規回帰テストのCSP違反なし。

## 証跡・版

`artifacts/entry-highlights-button-20260912/before/` が変更前、`after/` が最終7条件、`pressed/` が追加の押下2条件。センサーの共用画面はローカルChromeの追加実操作ログに記録。各JSONに対象5ファイルのSHA-256を保存し、試験前後の一致を検証。

| ファイル | 最終SHA-256 |
| --- | --- |
| mode-entry-guide.js | 5e5b485e95224e95fd5ddcc82cee326417d271c4ccb406d1951380d039456ae6 |
| mode-feature-intro.css | 331af2515fe1a6407c97109fe24885b50c708bd2bc3d3e26ec132069490bb19c |
| gaia-mode-loader.js | a26ccf8d1c24eb429e14f71917f04eeddfbb09b8e68215f4d552fe75dfed6aa7 |
| index.html | f87f908ea694e4d000da773d69ec383d792d95bae282fb2a133522c546758778 |
| sensors/index.html | 31022477cb497d5f24a135fd1f8b52a95c6a48c3ac770c254c00fab2e1868466 |

ローカル実装・確認まで完了。公開反映・本番スモーク・物理端末は未実施。保存/書き出し形式や配布物の変更はなく、その全経路は今回再試験していない。既存の未公開作業・残件は過去のQA記録を維持し、このボタン変更で解決したとは扱わない。
