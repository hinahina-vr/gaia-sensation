# 英語・簡体字対応：ローカル作業の区切り（2026-09-13）

## 状態と対象版

ユーザーの「キリねえからそろそろ終わろうぜ」を受け、追加の網羅調査・修正はここで止める。全機能・全利用経路の完全検証済みという意味ではない。変更はワークツリーに保存済み。コミット・プッシュ・デプロイは行っていない。

- 基点：`0034ee39cd906960c458d0cf84f6a48e719c39ab` にローカル変更を加えた版。
- キャッシュ識別子：`i18n-20260913`。最終確認で識別子の追加更新対象は0件。
- 対象ファイルのハッシュ一覧：`artifacts/i18n/final-candidate-manifest.json`。
- 過去の記録と失敗証跡は保持。詳細な経過は `docs/I18N_WORK_IN_PROGRESS_20260912.md`。

## 反映した内容

タイトルの言語設定に応じて、入口・地図・統計・センサー・各モード・設定・本編と後日談・クレジット・ログ書き出しを英語／簡体字に切り替える。最新画像のセンサー入口ガイドも対象。辞書は7,744項目、本編377＋後日談164本文。

原文の「の」を汎用置換して文章が崩れ、「in」が混入する原因を除去。全文と明示した表示フィールド単位で翻訳する。英中の字体は日本語の明朝調に合わせ、言語別の折り返し・余白を調整した。

画像ファイル、原資料の地点・物質・生物の固有名、識別コード、個人の入力値は改変しない。原資料名を残す場合も周囲の説明・数値ラベルは翻訳する。ストーリー原稿のハッシュは不変。

主な変更先：`gaia-i18n*.js/css`、`locales/`、`index.html`、各モードJS、`statistics-*.js`、`src/exploration/` の表示部分、`sensors/`、`concept/`。再現・回帰試験は `scripts/check-i18n-*.mjs`。

## 合格したローカル検証

実Chrome、PC 1440px／スマホ相当390pxの表示と操作を確認。下記は既存証跡を再利用し、全試験の再実行を無制限に増やしてはいない。

| 範囲 | 確認結果・証跡 |
| --- | --- |
| 最終全体チェック | `npm run check`（precheck／postcheck含む）exit 0。最終実行セッション28224。カタログ確認、`git diff --check` も合格 |
| 最新画像の入口ガイド | 地図／センサー×PC／スマホ×英中＝8条件。画像ロード、全文、開始操作、横溢れ。`artifacts/i18n/guides/` |
| 「in」混入の報告画面 | 4条件、修正前再現と修正後の原文復帰・全文表示。`artifacts/i18n/reported-surfaces/` |
| 本編・後日談・ログ | 本編4条件、後日談164段落の全ページを4条件で完走。ログ全544ステップとMarkdown実ダウンロード、Clipboard、別言語読込。`story-browser/`、`true-end-full/`、`log-export/` |
| 71展示 | PC英中とスマホ英中の展示切替。スマホ最終142条件合格。初回試験で不足した1〜14の実ポインタ／タッチ・カード・Canvas文字は別試験で補完。`map-runtime/`、`map-details/` |
| 分析26手法 | 104条件、各5表示＝520表示。数値・dataset/method IDの言語間不変、説明と属性。`statistics-methods/report.json` |
| 年次データの分析 | COD・PRTR・生物の5表示×PC／スマホ×英中は保存再読込試験追加前に12条件合格。追加した保存再読込試験の最新結果は下記の未確認に分ける |
| 概念・キャラクター・宇宙・GX・音楽 | concept 4条件、人物16条件、宇宙／GX72条件、12曲×日英中36条件。`concept/`、`character/`、`secondary-runtime/`、`sound-glyphs/`、`ending-music/` |
| センサー・AIのUI | センサー40条件、詳細24チェック、統計AI4条件。検索・入力保持・履歴・確認キャンセル・試験回答。APIはfixture。`sensor-platform/`、`sensor-detail/`、`statistics-ai/` |
| 直リンク・前後移動・巡回 | 英中のPC／スマホで前後・メニュー・71↔01、直リンクとオープニング経由の入口。巡回は英中それぞれ24代表展示×2画面＝48条件。`artifacts/map-exhibit-*-en/`、`*-zh-CN/`、`map-cruise-en/`、`map-cruise-zh-CN/` |

`map-details/report-poi-complete.json` は27条件成功後の旧失敗を含む。失敗を合格に書き換えず、修正後の `report-poi-1440-zh14.json` 1条件と `report-poi-mobile.json` 28条件で1〜14の英中PC／スマホ56条件を補完している。後から追加した全属性チェックまで56条件全てを再実行した、という意味ではない。

## 残件と検証限界

1. 展示10の年間CO₂排出量グラフに、読み上げ用 `svg@aria-label` の日本語説明が1件残る。通常の可視文言ではないが、アクセシビリティ上の未対応として保持。`artifacts/i18n/map-details/inventory-attrs-inventory.json` に原文あり。今回の区切りで追加修正は行わない。
2. 年次分析の保存→ページ再読込→別言語で適用する追加試験は未合格。最初の非表示ボタン操作は試験側で折りたたみを開くよう修正した。その後PC英語で保存までは進み、再読込後の分析準備待ちが30秒でタイムアウト。原因の追加調査・修正は行わず、製品の保存機能不良と断定もしない。最新失敗は `artifacts/i18n/annual-analysis/report.json` と `failure.png` に保持。
3. 実端末でのタッチ、実センサー機器・実認証・実AIプロバイダー・音声出力・全自然再生時間・本番公開は未検証。ローカルAPI fixtureや時計を加速した巡回を実配信の合格とは扱わない。

公開する場合は、この区切り以降の新たな明示指示と、その時点の対象コミットでの確認が必要。
