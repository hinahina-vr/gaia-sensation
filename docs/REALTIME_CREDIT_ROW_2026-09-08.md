# リアルタイム展示の出典を下部クレジットへ移動（2026-09-08）

## 対象と変更

基点 `b236255db4dc60f748aecfe4327d45d39f8ad33b` に今回のローカル変更を適用。直前の紹介画面の縮小・共通フォント変更は保持した。

- 提示画像1の「地球のいま / 01—05」を削除。
- 提示画像2のプロバイダー名を、画像3の既存 `.japan-credits` へ移動。Natural Earth、気象庁、USGS、OpenStreetMap等の既存クレジットを保持し、横並びに追加した。狭い画面では横方向の並びを維持して折り返す。
- 展示01〜05の選択に合わせて出典と既存の公式リンクを更新。非表示の展示の出典を隠し、通常展示06へ進んだ際に残らないようにした。
- 「リアルタイム展示」、ライブ等の接続状態、観測・配信時刻、モデルの種類、詳細出典ボタンは保持。FIRMSの公開時間差に関する注記も保持した。
- 主な変更は `src/exploration/realtime-exhibit-status.js` と `realtime-exhibits.css`。呼び出し側の `firms-exhibit.js`、`planet-signals-exhibit.js` に既存出典URLを渡し、`live-exhibits.js`、`src/exploration/index.js`、`gaia-mode-loader.js`、ルート `index.html` の関連キャッシュ識別子を更新。

## 再現・回帰試験

WindowsのローカルChrome、`http://127.0.0.1:4447` で実際に表示・操作。外部APIには保存データ・試験用応答を使用した。

1. `node scripts/check-realtime-credit-browser.mjs http://127.0.0.1:4447 --before`
   - 基点コミットの対象8ファイルをブラウザーに供給し、1440×900で展示01〜05の旧見出しとカード内出典を再現。5ケース合格。作業ツリーの書き戻しはしていない。
2. `node scripts/check-realtime-credit-browser.mjs http://127.0.0.1:4447`
   - 3840×2088、2560×1392、1440×900、1024×768、390×844、320×568、844×390の7サイズ、展示01〜05の35ケース合格。記録されたページエラー0。
   - 見出し削除、カード内出典削除、現在の出典が下部に1つだけ表示されること、横方向の並び、横はみ出し0、既存クレジットの画面内配置、時刻維持を確認。1440px以上では全出典が同じ1行に並ぶことも確認。
   - 実際に「次へ」を操作して出典の更新を確認し、展示06でリアルタイム出典が残らないことを確認。
   - 1440×900の展示03で出典をクリックし、正しいURLの別タブが開くことを確認。リンク先の応答はローカル試験HTMLであり、外部サイトの可用性確認ではない。
   - `artifacts/realtime-credit/before/` と `after/` にレポート・スクリーンショットを保存。1440、390および3840幅の実出力を開き、配置を目視確認。最終 `after/report.json` の対象8ファイルのSHA-256と作業ツリーの一致を確認した。
3. `node scripts/check-realtime-exhibits-browser.mjs http://127.0.0.1:4447 artifacts/realtime-credit/exhibits-final`
   - 1440、1920、3840、1024、390、320、844幅で既存の展示01〜05、表示範囲、操作、時刻、展示06除外を確認。35ケース合格、記録エラー0。
   - 1440、390、320幅では保存値・参考値・時刻遅れの3状態も合格。
   - 初回実行で旧スマホ見出しの19px条件が現行コンパクトHUDと不一致だったため、テストを「重複見出し非表示・バッジ表示・状態11px以上・時刻10px以上」に修正し、全7サイズを再実行して合格。表示実装はこの調整では変更していない。
4. `node scripts/check-realtime-status-browser.mjs http://127.0.0.1:4447 artifacts/realtime-credit/freshness`
   - 25時間経過時のライブ解除、非表示タブからの復帰時更新、ブラウザーのオフライン・オンライン切替の3ケース合格。
5. `npm run check`（precheck/postcheckを含む）、リアルタイム状態の純粋関数検査、モードローダー検査、変更JSの構文検査、`git diff --check` は合格。

回帰テストとして `scripts/check-realtime-credit-browser.mjs` と `check:realtime-credit:browser` を追加。鮮度検査には記録先引数を追加し、以前の証跡を上書きせず保存した。

## 未確認・公開状態

ローカルChromeの画面サイズ・タッチ入力エミュレーションによる確認。物理スマホ、Safari、本番配信、今回の実API通信は未確認。保存仕様・書き出し機能・配布ZIPの変更はない。コミット・プッシュ・デプロイはしていない。
