# 2026-09-09 観測展示・描画改善の公開

## 指示と対象

製作者の今回の明示指示「コミット・プッシュ・デプロイ」に基づく。公開基点は `2a4f51385b69a0fda1ef285b0058acae7207640b`、公開先は既存の `https://gaia-senseware.pages.dev`。このタスクで依頼された未公開の実装・素材候補を同じ候補版にまとめる。新規サービス・課金プラン・DB・シークレット・認証設定は変更しない。

- 大気・水質21展示（44〜64）、PRTR3展示と魚類・底生生物2展示（65〜69）。公式原表の年度・単位・欠測・名称と出典を保持。
- 年次展示の字幕・地点出現演出、下部パネルの密度と配置、デモのデータ待機、需要時読込・描画負荷の削減。
- タイトル・エンディング各5枚の候補画像と比較ページ。本編への差し替えは行わない。
- 各機能の取得・生成・照合・回帰テストと検証記録。

ダウンロードした大気・水質の原ZIP・仕様PDFはローカルに保持し、Git管理・配信に追加しない。PRTR・生物の原ZIPも従来どおりローカル保持。再取得URLとSHA-256は各manifest、加工手順はスクリプトを収録する。

## 検証の扱い

既存の同日QA記録はその時点のローカル版に対する証拠であり、現在版・本番の全体合格に読み替えない。公開前に全体検査とコミットの新規展開版で影響機能の実Chrome確認を行い、その同一コミットをプッシュ・配信する。公開後は本番HTTPとChromeで表示・主要操作・保存復元を確認する。

実機スマートフォン、Safari/Firefox、ESP32実機、外部AIへの実送信、実アカウントへのデータ登録・削除・認証変更は対象外。モック・通信障害注入は本番動作の証拠と区別する。

## 素材とデータの出典

追加画像10枚は既存キャラクターを参照したOpenAI Imagegenの成果物。各候補フォルダのREADME・prompts.jsonに制作元、参照、原寸、加工有無、保存確認を記録し、素材台帳へ収録する。既存の権利判断の日時・理由・許諾確認状態は変更せず、今回指示を新たな未確認リスクへの了承に読み替えない。

PRTR・生物の出典と利用範囲は `data/sources/prtr-biology/README.md`。大気・水質の原資料・取得条件は `docs/QA_JAPAN_POLLUTION_EXHIBITS_2026-09-09.md` と `data/sources/japan-pollution/manifest.json`。公的機関の評価・承認・危険度判定として表示しない。

実装コミット：`d60e15d`。基点との機械照合で、既存304素材の台帳全項目、出典6件、既存gaia-signals、GBIF、両lockfile、依存一覧が不変。追加10枚を含め314素材。旧scope `5c397fc2edacd956f1d4aa677c5b65cbec8b3f09275b226f177308e31a29eab1` → 新scope `8ce44c3a2cc79c75e69bfdee11bc7b8dce505cedac8e0fb719d8783111c1d568`。旧判断の内容は変更せず、追加制作物の所在と今回の版を対応付けた。

## 公開準備中の検証

- 全体 `npm run build:release` 合格。precheck/check/postcheck、権利台帳314素材、既知形式の秘密値904ファイル走査、Pages Workerの991,209-byte再現ビルドを含む。Workerは基点から内容変更なし。最初のビルドはWranglerのローカル設定ディレクトリへのアクセス制限で終了し、必要なアクセスを許可した再実行で合格。
- sensor-platformのTypeScript検査と隔離ローカルPages試験に合格。既存の静的配信・Range・API・非公開パス・匿名セッション試験であり、本番DB・実アカウントの変更なし。
- 画像比較ページを本番CSPで表示するとinline scriptが遮断される問題を実Chromeで再現。スクリプトを同一オリジンの外部JSへ移し、両ページ×1440/390幅のCSP・全画像読込・原寸PNG保存・リンクの4条件で修正確認。既存のfile:直接閲覧の代替動作・原寸表示・保存試験も両ページで合格。画像本体の変更なし。

公開前の新規展開版と公開後の検証記録・対象コミット・SHA-256・スクリーンショット・HTTP応答は `artifacts/release-prtr-20260909/` に保存する。そこに記録された実行済み結果のみを公開確認の証拠とする。

## 735d3f5 の新規展開・本番確認

Gitの元blobから新しいディレクトリに1,398ファイル／817,670,675 bytesを展開し、全Gitオブジェクトハッシュ一致、追加ファイル0件を確認した。先行するtar方式ではWindowsの日本語名3件と改行変換が検査で不一致となったため、このtar展開版は公開に使用していない。

`735d3f5587b808b7ea44b4eecda4ac2de362e0c4` を作業ブランチとmainへ通常のfast-forwardでプッシュし、Cloudflare Production/main `8eebb067-39f8-4061-b40d-73f89e0b9064` に配信。同一コミットからの新規展開版と本番の双方で、PRTR・生物5展示×1440/390の10組、画像候補2ページ×2幅の4組、上部の物語入口からの保存・読込・Markdown書き出し2組が合格。本番の公開変更ファイル67件のHTTP本文がコミットと一致し、セキュリティヘッダ・読み取り専用APIヘルス・非公開パス4件も合格。

既知の未修正問題：公開データ末尾から初回にストーリーを開く導線は、PC・スマホ相当／タイトル経由の計4条件で引き続き失敗。基点の時点で検証のみ依頼された既存問題（`QA_ENTRY_ACTIONS_2026-09-09.md`）として記録を維持し、今回の公開依頼に無断の別修正を含めていない。上部の物語入口は動作する。

GitHub Actions `34319538193` ではsensor-extensionが合格し、web-pageの実Chrome試験だけが旧「43展示」待機で失敗した。アプリは69展示を正常登録しており、本番Chromeでも確認済み。CI試験のPC／スマホ2か所の固定数を、各展示カタログから求めた件数へ更新。表示・性能・操作に関する他のアサーションは維持し、同じ実Chrome試験を再実行する。この後続変更は検証スクリプトと公開記録だけで、配信アプリ・データ・画像・Workerを変更しない。

## 遅延読込とガイド一時停止の回帰修正

件数の旧条件を解消して全体画面試験を進めると、ガイド開始直後の一時停止で対象表示が完了しない問題を検出。実Chromeで原JSON応答を1.5秒遅らせて再現すると、停止状態・step 0のまま対象要素がない。停止時の世代更新によって旧非同期準備が無効化され、停止後に準備が引き継がれないことが原因。

`guided-tour.js` では、未表示の対象がある一時停止に限り現在のステップ準備を新しい世代で続行する。経過時間・キューの自動進行は停止を維持し、既に表示された対象・手動操作中のカメラはやり直さない。入口からのキャッシュ識別子も更新。

`scripts/check-tour-early-pause-browser.mjs` の同じ遅延条件で、1280幅／390幅とも、読込中の停止→対象表示→停止時間の維持→手動の次送り2回→再開→終了が合格。原表値の置換なし。遅延は明示的な通信試験であり実配信遅延の測定とは区別する。修正前後の `tour-pause-before/` と `tour-pause-after/` を保持し、全体の実Chrome試験も再実行する。これは今回の遅延読込変更と組み合わせた関連機能の修正であり、上記の既知の末尾入口問題とは別。

`696d66b` の全体実Chrome試験は `contest-browser-network/report.json` で合格。新規展開1,399ファイル／817,678,950 bytesの全Git blob照合も合格し、この展開版のガイド回帰2組と65・68展示×PC／スマホ相当4組が合格。一方、GitHub CI `34320711716` は入口カードのフェード中に固定320 ms待機が終わり、表示判定で失敗した。CSSはカード420 ms＋80/150 msの遅延、親620 msであり固定待機だけでは完了を保証しない。試験を親と2枚のopacityが0.999以上となる状態待機へ変更し、既存の表示・配置・導線アサーションをすべて維持する。アプリ側CSSやアニメーション時間は変更しない。

## 1493fcb の確認とページ終了時の修正

`1493fcb3cf74f68fe462bb0c40987468dd6874c2` は新規展開1,399ファイル／817,680,220 bytesの全Git blob照合、ガイド回帰2組、65・68展示×2幅の4組、全体Chromeに合格。GitHub CI `34321245359` もweb-page／sensor-extensionとも合格し、本番 `09a69f75-6db5-4f30-bb0f-52178aa5dd96` へ反映。本番HTTPの68変更ファイルと6検査、本番PRTR・生物全10条件も合格した。

追加の本番全体Chromeでは表示・全導線・ガイド・復帰のアサーションを通過したが、ページ終了でstoreが通信を中断した際のconsole error 1件により全体は不合格。pageerror／未処理Promise拒否は0件だった。`app.js` のcatchが正常な所有者終了もデータ障害としてログ・UIへ報告していたため、非BFCacheのpagehideで所有者終了を記録し、その後のAbortErrorだけは失敗表示せず処理を終える。稼働中のタイムアウト・HTTP障害の報告は変更しない。

`check-snapshot-lifecycle-browser.mjs` の原JSONを1.5秒遅らせる同一条件で修正前のエラーログを再現。修正後はPC／タッチ相当各3条件（終了、BFCache保存、503注入）が合格し、終了時の誤通知なし、BFCacheの読み込み継続、HTTP障害のエラー通知維持を確認。イベント・503注入を実ユーザーのブラウザ復元や実障害の証拠と混同しない。snapshot-storeの既存タイムアウト・再試行・共有・原数値・終了検査も合格。最終版の新規展開と本番検証は同じartifactsディレクトリ内の別記録へ保存し、1493fcbの本番全体不合格記録を上書きしない。
