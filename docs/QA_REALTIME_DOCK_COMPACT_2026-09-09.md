# 01〜05 下部UIの高さ調整

対象: `2a4f51385b69a0fda1ef285b0058acae7207640b` を基点としたローカル作業ツリー。先行する大気・水質21展示の未公開変更は保持。push・デプロイは行わない。

## 変更

- `realtime-exhibits.css`: 01〜05のデスクトップ用バーを `clamp(142px, 8.5vw, 188px)` から `clamp(104px, 5.6vw, 124px)` に縮小。文字サイズ・ラベル・数値・時刻・操作は維持。
- 幅901〜1250pxで2段になる01の再生操作と04の最大震源表示は126pxを確保。他の展示は同じ小型PCでも104px。
- モバイルは01〜05だけ上下paddingを10→6px、行間を6→4pxにし、状態欄の行間・下余白も縮小。タッチ操作部のサイズは変更しない。
- 共通の高さ変数を使い、出典クレジットもバー上端へ追従。他展示・ストーリー画面の高さ設定は変更しない。
- 出典パネルを開いて戻ると、外側のシーンが横に632pxスクロールする既存の症状を旧CSSで再現。01〜05のシーン祖先に `overflow: clip` を設定し、地図カメラ以外の横スクロールを防止。
- `gaia-mode-loader.js` と `index.html` のキャッシュ識別子を更新。

## 再現・回帰試験

`scripts/check-realtime-exhibits-browser.mjs` に `--compact-height` を追加。旧版の高さとの比較、文字サイズ維持、文字の上下クリップ、出典の実クリックと復帰、再生・停止、キーボードの時刻送り、最大震源への移動、次の展示への実クリックを検査する。

旧版の証跡: `artifacts/realtime-dock-compact-2026-09-09/before/`。幅1920・1440・1024・390・320で01〜05の25ケース。シーンの横スクロール再現は `before-controls/report.json` と `failure.png`。同じChrome・画面幅・操作で再現し、CSSのみを `HEAD` 版へ置換して確認した。

## 最終結果

| 画面幅 | 変更前 | 変更後 |
| --- | --- | --- |
| 1920px | 全5件 163.19px | 全5件 107.52px（約34%減） |
| 1440px | 全5件 142px | 全5件 104px（約27%減） |
| 1024px | 全5件 142px | 01・04は126px、02・03・05は104px |
| 390px / 320px | 01は179.19px、02〜05は125.38px | 01は162.19px、02〜05は110.38px |

検証した最終版の実装ファイルSHA-256:

- `realtime-exhibits.css`: `3da8fef8645251a061a844389159a15be16a9d72373b31bbc05b60ec3613a5a4`
- `gaia-mode-loader.js`: `47fff9f12cfd38af27631ad4e4026e962406ca39c9d3342bc706111fba166022`
- `index.html`: `6e4f2cf104c287a668655649c8be923d43e5c65accf378d4ee3274706aeec8f2`

01〜05の全5件を1440×900、1024×768、390×844、320×568、1920×1080、3840×2088、1250×800、901×768、844×390で確認。合計45ケース。旧版との高さ比較、時刻・状態・数値の表示、文字の上下クリップ防止、出典と復帰、前進操作、01の再生／停止／キーボード時刻送り、04の最大震源への移動が合格。統計分析は元の無効状態を維持。各幅で06への切替とリアルタイム表記解除も確認。

証跡: `artifacts/realtime-dock-compact-2026-09-09/after/report.json` の完了済み6画面・30ケースと、`after-small/report.json` の3画面・15ケース。最初の連続試験は7画面目の起動待ちでタイムアウトしたため、元の報告は `failed` のまま保持。実装を変えず新しいChromeで残る3画面を再実行し、全15ケース合格。タイムアウトの根本原因までは特定していない。途中の起動失敗をUI試験の合格として数えていない。

保存観測・サンプル・時刻遅延表示の15ケース（幅1440・390・320・1920・3840）も合格。外部応答を固定した表示試験であり、ライブ配信での確認ではない。

既存の `check-realtime-dock-alignment-browser.mjs` も幅1251・1799・1800で実行し、見出し・数値・時刻・アクションの行位置、内容の収まり、出典表示、05→04移動が合格。証跡は `alignment/report.json`。最終版の画像もPC 1440／1920、狭いPC 1024、スマホ390で目視確認した。

`node scripts/check-mode-loader-prefetch.mjs`、`node scripts/check-realtime-exhibits.mjs`、変更したJSの構文検証、`git diff --check` は合格。最終ブラウザ試験後に実装ファイルの変更はない。

## 検証範囲

ローカルのインストール済みChrome。PCとタッチエミュレーションでの実DOM・実描画・実クリックを検証。プロバイダー応答は保存データ／合成テストデータによる固定応答で、外部ライブ取得の検証ではない。保存観測・サンプル・時刻遅延表示も確認対象。実機スマートフォン、本番配信、外部AI、ZIP配布は対象外。保存・書き出しの仕様や観測データ自体は変更していない。
