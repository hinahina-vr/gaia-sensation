# MAP数値欄の横配置・はみ出し修正 / 2026-09-12

## 依頼と対象

ユーザー原文: 「右空いてんだからこんな左に詰め詰めにしないではみ出ないようにしてよ」

添付は07の「平均海流速度 / 0.28 m/s / 海流79地点」。解釈: 空いた右側に補足情報を移し、数値を小さくせず、枠の上下・左右に収める。

基点は `9fbd901b68faebaf6148db6e18cdbcb50fa1c778` 上のローカル作業ツリー。先行の展示名1行化・数値強調・座標統一と他の未公開変更を保持。今回のコミット・push・デプロイなし。

## 変更

- `map-unified-dock.css`: 06〜14の共通数値欄を、主数値が左・補足が右の横並びへ変更。CO₂の実測/補完件数も右欄内で必要に応じて折り返す。補足のない展示に空の行を残さない。
- 数字の書体、サイズ、詰めた字間、単位を保持。主数値の行高を0.94→1.3にし、文字の実寸を収める。ラベルの行高と上下余白も確保。通常PCの操作パネルは100〜124px。
- 901〜1400pxでは数値・期間操作を上段、展示名・説明入口・出典・分析を下段に配置。期間操作を押せなくなる横幅不足も防ぐ。展示名は1行のまま。タッチ用配置と本編の寄り道用配置は変更しない。
- 年目盛りは専用の20px行に収め、途中で折り返さない。目盛り欄そのものの幅が260px以下なら中間ラベルを間引き、スライダーへの重なりを防ぐ。
- `scripts/check-map-metric-room-browser.mjs` を追加。文字の実矩形、親枠、縦横オーバーフロー、右側配置、期間スライダーのヒット領域を検証。
- 既存の `scripts/check-map-chapter-width-browser.mjs` の操作試験を通常ドックの展示一覧ボタンと次/前ボタンにも対応させた。以前の試験は独自ドックのボタンだけを対象にしていた。

## 再現と検証

インストール済みChrome＋Playwrightで `http://127.0.0.1:4492` の実画面を操作。本番相当CSPを適用し、外部HTTPSを遮断。同梱の元データを利用したローカル試験であり、本番配信・物理端末の確認ではない。

- 変更前: 3840・1440・901・390px、06〜14の36条件を記録（独自表示の12は共通欄が非表示）。07の縦積みと、他の表示の文字切れ/横幅不足を再現。16条件で数値欄の問題を検出。1440pxの07では、枠56.84pxに対して見出しが63.78pxあり下へはみ出していた。
- 初回修正後: 同じ4幅で期間のHome/Endを操作した72条件が合格（表示される共通数値欄は64条件）。数値と補足が親枠内、数値欄の縦横オーバーフロー0、07の地点数が右側、期間操作が到達可能。
- 関連回帰: `GAIA_FOCUSED=1 node scripts/check-map-unified-dock-browser.mjs` の39条件合格。21との文字書体/サイズの一致、06〜20の表示、15〜20の期間切替と元データの値/時刻一致を確認。
- 境界の追加確認で1281pxのCO₂期間操作の幅不足が判明したため、2段配置の上限を1280→1400px、上段を70→76pxに修正。1401pxでも期間操作の中央に年目盛りが重なることを確認し、実際のヒット要素が折り返した「2004年」であることを特定。上記の目盛り用行高・折り返し禁止・幅に応じたラベル間引きを追加した。
- 最終の全画面幅試験: 3840・1440・1401・1400・1281・1280・1024・901・390px × 06〜14 × Home/Endの162条件が合格。実表示の共通数値欄144条件、非表示の12が18条件。文字/親枠のはみ出し、数値欄の縦横オーバーフロー、07の地点数の配置、期間操作のヒット領域を確認。検出問題0、ページエラー0。
- 変更前と最終版の対応する32表示を機械比較し、主数値のテキストとフォントサイズがすべて同一であることを確認。`app.js` のSHA-256も変更前と同一。
- 最終の展示切替操作試験: 901・1440・3840px × 06/07/10/14/15/21の18条件合格。展示名1行、各操作部が枠内、実ヒット領域、展示一覧の開閉、期間Home/End、次→前の展示切替を確認。14→15で独自のLIVE操作パネルへの切替/復帰も確認。
- 1401pxのCO₂を同じHome操作で再確認。スライダー下端857.5pxに対し、年目盛り上端865.5pxで8pxの間隔があり、中央が入力欄自身にヒットする。目盛りは1958・1989・2019・2050年。`1401-co2-fixed.png` を目視確認。
- `node --check`（上記2試験）、`git -c core.safecrlf=false diff --check`: 合格。

07の主数値サイズは3840pxで46px、1440pxで31.68px、901pxで28px、390pxで23pxのまま。4K/1440pxの07は目盛り修正後の最終版も画像で目視確認。CO₂予測区間・人口の長桁・901pxの数値欄は目盛り修正前後で数値部分の変更なし。

## 証跡・版

- `artifacts/map-metric-room-20260912/before/report.json`: 元の再現36条件と画像。
- `artifacts/map-metric-room-20260912/after/report.json`: 初回修正後72条件。CSS SHA-256 `20580a4a51aab3ead0d01091fc40a156af3b34ff38573add5dcc3b1c36ab86d3`。
- `artifacts/map-unified-dock-20260912/after/report.json`: 同じCSS版の関連回帰39条件。
- `artifacts/map-metric-room-20260912/after-boundaries/report.json`: 1281pxの幅不足を記録した中間試験。合格証跡として扱わない。
- `artifacts/map-metric-room-20260912/after-final-narrow/report.json`: 中間版。901〜1400pxの108条件は合格したが1401pxで目盛りの重なりを検出。最終合格証跡として扱わない。
- `artifacts/map-metric-room-20260912/after-final/report.json`: 目盛り修正を含む最終版の全画面幅試験。
- `artifacts/map-chapter-width-20260912/after-metric-room-final/report.json`: 最終版の展示名・ボタン・期間操作試験。

最終 `map-unified-dock.css` SHA-256: `eee5a17960d2d2360eb2bc22aa5490bd9936efaf67a897c98dabe4b07e9cc7cb`。

## 未確認・残件

今回の表示修正はローカルのみ。公開反映・本番スモーク・物理スマートフォンは未実施。データ構造、計算、保存/読込/書き出しの実装変更や配布ZIPはない。保存/書き出し全経路、本編やSOUNDなどの無関係な機能を今回再試験したとは扱わない。先行作業の証跡と公開待ちの状態は維持。
