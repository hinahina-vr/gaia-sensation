# ストーリーの不自然な改行・改ページ — 2026-09-11

## 依頼と対応

依頼原文: 「変な改行なおして」（添付11枚）。

解釈: 本文を書き換えるのではなく、引用・単語・文節の途中で不自然に切れる行／ページを修正する。同じ表示処理による箇所も確認する。公開の依頼はなく、ローカルのみ反映。

| 添付 | 対象ID | 原因／修正 |
| --- | --- | --- |
| 1 | esp32_pitch_016 | 『身近な街角の1点を測る触角』の途中で折り返す。短い二重鉤括弧内を避けて行を選ぶ。 |
| 2・3 | beyond_02_009 | 固定ページが「見／えない」で分断。「持ち寄って、／1台では見えない…」へ。 |
| 4・5 | beyond_02_020 | 「発掘／なんてしないで」を分断。「できるなら、／地層から発掘…」へ。 |
| 6 | beyond_03_002 | 「氷殻／の下に」を分断。「知覚し、／厚い氷殻…」へ。 |
| 7 | beyond_03_003 | 「根／を張る森」を分断。「読み取り、／星間ガスに根を張る森…」へ。添付6とは別のメッセージ。 |
| 8・9 | beyond_03_009 | 「揺らぎ／を自分の…」を分断。「機械も、／お互いの揺らぎ…」へ。 |
| 10 | beyond_03_040 | 「営／みは」を分断。「という営みは、／二百七十万年後…」へ。 |
| 11 | beyond_03_048 | 「描／き、」を分断。「波紋を描き、／星間分子雲…」へ。「千年単位」も行内で保護。 |

全体照合で、幅の計算が最大0.5pxの超過を許し、予定外の行に「く。」や「いる。」だけを落とすケースも発見。1pxの端数余裕を確保する計算に修正し、活用語尾「く」を前の語とまとめた。該当する本編のケースを含め、孤立した語尾をブラウザー回帰テストで検出する。

## 実装と履歴の保持

- 新しい `story/reading-breaks-20260911.js` に7件の固定ページ区切りの変更前後を記録。`scripts/build-true-end-story.mjs` が適用し、`true-end-data.js` と `story/現行統合台本.md` を既存生成処理で更新。
- 本文、話者、メッセージID、順序、演出、承認台本、過去のLOGコメント・改訂履歴は変更しない。7件以外の固定ページ区切りも保持。検証では今回のページ区切りだけを厳密に逆変換して過去のハッシュ契約も維持する。
- 本編の既存の日本語分割／行選択処理を `dialogue-typography.js` に共通化。本編とAPEIRONCENEが同じ処理を利用。漢字複合語（氷殻、千年単位など）、引用、句読点、文字列の復元を扱う。
- APEIRONCENEは全ページの配置を先に決め、文字送りでは文字の可視性だけを変更。途中で行が動かない。画面が狭くて収まらない場合だけ固定ページを追加分割し、リサイズ後は読んでいた位置を含むページを表示する。観測・フォント・リサイズ処理は終了時に解除。
- `gaia-mode-loader.js` は共通処理を両ランタイムより先にロード。`index.html` と変更アセットのキャッシュキーを更新。
- 新規の静的／ブラウザー回帰テストと既存の共通処理テストを更新。`check-dialogue-page-reveal-browser.mjs` は現在38文字になった旧対象003から、実際に複数ページとなるGXの010へ対象を更新し、人工的なCSS幅変更を削除。

## 対象版

- ベース: `888c26b` / `codex/gaia-copy-motion-polish-20260904` と既存のローカル変更。
- 本編: storyVersion 13、380ステップ（うち通常会話／地の文344件）。
- APEIRONCENE: 164メッセージ、`readingBreakRevisionId: reading-breaks-20260911`。
- 以前の13:00開始、温度画面の自動進行、冒頭の演出、BGM、終了時のフェードなどのローカル変更を保持。コミット・push・デプロイなし。

## 検証結果

実行環境: ローカル `http://127.0.0.1:4492/story`、インストール済みChromeのheadless実行。新規の2ブラウザーテストはローカルナビゲーションに本番CSPを適用し、外部サービスを遮断。スマホはビューポート／タッチエミュレーションで、実機ではない。保存データは隔離したテスト用フィクスチャ。

| 画面 | 本編の全344本文の実DOM計測 | 後半の実操作での通し再生 |
| --- | --- | --- |
| 1920×1080 | PASS | 164メッセージ / 253ページ、最後までPASS |
| 1440×900 | PASS | 164メッセージ / 253ページ、最後までPASS |
| 390×844 | PASS | 164メッセージ / 256ページ、最後までPASS |
| 320×568 | PASS | 164メッセージ / 279ページ、最後までPASS |

本編の全件検査は現在の会話面でのページ割り・行のDOM計測であり、344件すべての背景／演出を順番に操作した試験ではない。ESP32の添付箇所は実際に再生し、各ページのスクリーンショットと、SAVE→進行→LOADを検証。後半は通常のスタッフロール完了ボタンから入り、全1,041ページをクリック／タップして本文の完全一致、改ページ、禁則、画面内の収まり、フィナーレ到達を確認。4サイズでLOGから実際に台本をダウンロードし内容を照合。

- 変更前: `artifacts/story-reading-breaks-2026-09-11/before/report.json`。1920×1080と390×844で添付箇所を実際に再生・撮影し、同じ固定ページの分断と引用の折り返しを再現。
- 最終版: `artifacts/story-reading-breaks-2026-09-11/after/report.json`、全件PASS。添付に対応するPC／スマホのスクリーンショットを保存し、代表画面を目視確認。ページエラー・404・CSP違反なし。
- ライフサイクル: `artifacts/story-reading-breaks-2026-09-11/lifecycle/report.json`、PC／スマホともPASS。本編・後半の実時間の文字送りで、表示済み文字の座標が動かず表示文字数が増えること、Enterで表示完了／次ページ、AUTO、320pxへの縮小と再拡大で読み位置維持、LOG書き出し、ネイティブ章スキップ、フィナーレからの帰還、破棄後リサイズを確認。6検証グループPASS。
- 既存の次ページ文字送り: `artifacts/story-reading-breaks-2026-09-11/main-page-reveal/report.json`、PC／スマホの2件PASS。次ページが全文瞬時表示にならず文字送りを開始し、途中クリックで完了する。
- 終了部の入口: `check-ending-entry-fade-browser.mjs` を再実行し、PC／スマホ通常モーション／軽減モーションの3件PASS。入口のフェード処理に変更なし。最後の共通文字組みの端数調整後は、上記ライフサイクルと通し再生を再実行。
- `npm run check`（precheck・check・postcheck）、`npm run check:story-reading`、ページ改訂の厳密逆変換と過去LOG契約、生成データ／台本書き出し整合、`git diff --check`: PASS。静的チェックだけを実操作検証として数えない。

途中検証では新しい共通処理をMAP側だけにロードしてしまう組み込みミスを検出し、STORYの依存リストへ修正。最終版の直接STORY入口と帰還まで再確認済み。テスト側の旧AUTOセレクター、ロード完了前の待機判定、短くなった旧GX対象も更新してから再実行した。

## 最終ファイルの照合

`after/report.json` と `lifecycle/report.json` が記録したSHA-256を実ファイルと照合済み。主な最終値:

- `dialogue-typography.js`: `efd231131fee05807f0adbd03efe8fad066459fa009fb52e9d5e69ebd760e2f9`
- `novel-mode.js`: `be5cdb212db739ce6ec51f00ce0f72aa96cd0b41081975ec51d7149edd69a367`
- `true-end-data.js`: `f04aa53efc731eb4bef400903fc343afbaa03c92b8a36d74c1def1c7eb912aaa`
- `true-end-mode.js`: `0ec4a8a64c7675071edf83b87168524751b38be9e4e81c714cfb32be60087151`
- `true-end.css`: `cfeed0d9c2f5a8f6312f0a20a713a789b431b2e256f2cff6e212ec927d696e79`
- `gaia-mode-loader.js`: `b825e46e58bfaaa9591278dd2214e778233f7878428b5735bc35a6e47f065417`
- `index.html`: `6048518eb63ac7fe3a2cb2fade45a67815f097d93d8def9018f7ef54a1f93001`

## 未確認・残件

本番には未反映。本番配信・実機iOS/Android・Safari/Firefox・全画面サイズ／文字拡大率は未確認。ZIP配布物は今回作成していない。以前記録した短い横向き画面のVN操作部の重なりは、今回の4サイズの文字組み合格とは分けて扱い、横向きの操作部レイアウトを対象にした再現と調整が引き続き必要。この既存課題が解消したとはしていない。
