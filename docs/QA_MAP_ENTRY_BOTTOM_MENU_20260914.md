# 地図入口と下部メニュー

- 2026-09-14 ローカル変更。先行する入口スクリーンショット変更も未コミットで保持。README等の他作業は変更していない。
- 依頼1: 「地球を見に行く」でメニューを開かず自動再生する。トップから地図を開く際の展示一覧へのフォーカス指定を解除。歓迎ガイドの終了時は一覧にフォーカスを戻さず、一覧を閉じ、戻るボタンに安全にフォーカスを移して再生を開始。
- 依頼2: 下部の展示名からメニューを開いた際は下側に表示する。上側の起点が残ることをChromeで再現（06、上メニュー→下部タイトル、`data-anchor=top`）。下部ボタンの実クリックで起点を更新。別の起点へ切り替える場合は単純な開閉反転ではなく、開いた状態で再配置。置換された展示パネルのタイトルにも適用。
- 下部メニューの下端は下部パネルのすぐ上。上部ボタンからは従来通り上側に表示。
- 15の置換パネルでは、上メニューから移動した時の120ms遅延閉鎖が開き直し後に残る問題も再現。メニューを開く時点で古い閉鎖タイマーを取り消す。

## 回帰テスト

- `node scripts/check-map-entry-bottom-menu.mjs`: ローカルChrome headless、1440×900 / 390×900。トップの地図ボタン→歓迎ガイドの開始ボタンを実クリック、一覧が閉じていることと炎の時間進行を確認。
- 06で上メニュー→下タイトルの実クリック、メニュー下端と起点の距離、マウス移動後も表示されることを検証。12・15の下部パネルでも位置を検証。
- 証跡 `artifacts/map-entry-bottom-menu-20260914/`。06のメニューが下部に表示されることをスクリーンショットで目視確認。
- 構文検査、対象差分の `git diff --check` 合格。
- 全71展示・全画面サイズの実ブラウザ網羅ではない。公開反映・コミット・プッシュ・デプロイは未実施。
