# 提出準備の確認結果

## 現行の容量基準（2026-09-26）

初期読込量の内部上限は2,000,000 bytesです。静的集計と実Chromeのencoded body集計は共通定数を使用し、上限超過を例外扱いする環境変数は廃止しました。境界値・不正値の回帰テストを維持しています。容量条件の変更は他の検査の合格を意味しません。最新の実行結果はGitHub Actionsを参照してください。今回はCI・文書のみの変更で、配信サイトは再デプロイしていません。

## 後続検査の現行化（2026-09-26）

タイトルから地図への直接遷移、15〜20の47都道府県色分け地図、固定ナビゲーション、音楽画面終了時の復帰を現行仕様に合わせてCIで確認します。PC・4K・スマホ幅で6展示、ガイドの操作・回転・自動進行、WebGLフォールバック・コンテキスト喪失・反復開閉までローカル実Chromeで合格しました。実機スマホ検証ではありません。

1280px幅のガイド2枚目と操作パネルの重なりを修正。同じ条件の非重複検査が合格し、資産URLも更新しました。内部上限2MBは維持し、実測1,871,831 bytes、ブラウザ例外・未処理エラー・404は0件です。CIではOpen-Meteo/NOAAの取得不能応答と保存データへの切替を固定し、実APIの疎通確認とは区別しています。公開とCIの確定結果は対象コミットのActionsを参照してください。

## 過去の検証記録

以下は当時の1MB基準での記録です。

確認日: 2026-09-13。基準コミット `cb5e443` に対するローカル修正・概要コメント・文書更新がある作業ツリーを対象にしています。未コミット・未公開であり、GitHub／公開サイトの合格記録ではありません。

## 今回確認したこと

| 項目 | 結果 | 補足 |
|---|---|---|
| `node scripts/check-contest-submission.mjs` | 合格 | README・提出ガイド・構成・出典案内・CIの整合 |
| 同梱のプレビューサーバー | 起動確認 | 4173は既存プロセスが使用中のため4189で起動。既存サーバーは停止していない |
| 実Chromeでの起動手順 | 合格 | 入口の音設定、`#world-08`の表示・71展示の存在、`/concept/`を確認。外部HTTPSは遮断 |
| 文書内のローカルリンク | 合格 | 更新した6文書の66リンク先が存在 |
| `npm run check:contest` | 停止 | 初期画面の保守的な未圧縮合計が1,829,235 bytes。既存の1MB基準超過 |
| `npm run check:rights` | 停止 | `docs/media-rights-ledger.json is stale`。現行素材と生成台帳が不一致 |
| `npm run check` | 途中で停止 | `scripts/check-japan-pollution.mjs:19` のVM実行で `matchMedia is not defined` |

環境: Windows、Node.js 22.17.0。今回 `npm ci` による依存の再導入は行わず、既存インストールを使用しました。CIのNode.js 24や新規cloneからの全手順は未検証です。停止後の後続検査について合格とはしていません。

## 提出前の対応方針

1. 初期転送量: 必須でない資産の遅延読込を検討し、現行の予算基準で再計測する。基準を緩めるだけで合格にしない。
2. 権利台帳: 差分の素材・由来・条件を確認し、必要な記録を補ったうえで生成し直して再検査する。台帳の生成成功は利用許諾の証明ではない。
3. テスト環境: `check-japan-pollution.mjs` のブラウザAPIスタブを現行コードに合わせ、対象チェックと総合チェックを再実行する。実ブラウザでの表示確認とは分ける。
4. 最終的な提出コミットと検証結果を対応させる。非公開リポジトリでは審査者の閲覧権限を確認し、公開サイトも提出するなら版の一致を確認する。

上記はREADME等の見直し時点の記録です。その作業では、上記3件を解消する製品・テストコード変更、権利判断、コミット・push・デプロイは行っていません。

## 既存の画面検証

レスポンシブ修正の18条件・378画面・180操作／個別回帰25件は[提出ガイドの検証状況](CONTEST_2026_SUBMISSION.md#検証状況)を参照。これは上記の総合提出チェックがすべて合格したことを意味しません。ローカルの詳細証跡はGit管理外です。

## 再評価の残件6件を修正（2026-09-13）

後続依頼「じゃそれ直して」を受け、再評価で残った6件（中4・低2）をローカルで修正しました。地図上部の重なり、地点吹き出しの重複、分析グラフの英語軸名の見切れ、低い画面の保存一覧、出典の可読性、選曲後の再生操作が対象です。

- 受入・境界幅・日英中の69ケース、旧9件に関連する25ケース、グラフ・記録参照の6ケース、合計100ケースに合格。
- 地点・年の数値アニメーション完了後も3言語で追加確認。比較画像はこの確定表示を使用し、同じケースの再確認を100ケースへ重複加算していません。
- 最後の小型分析ヘッダーの行分け後は、影響するE03の9ケースとグラフ6ケースを再試験。他画面の通過済み証跡は再利用し、受入結果の各行に証跡フォルダーを記録しています。
- 実Chrome 152.0.7977.83（Windows、headless）とモバイルの画面・touchエミュレーション。保存→再読み込み→LOADを含みます。外部HTTPSを遮断したローカルデータでの検証です。
- 実機・Safari／Firefox・200%ズーム・公開版・新規cloneは未確認。コミット・push・デプロイは未実施です。

比較画像付きレポートはローカルの `artifacts/responsive-followup-20260913/REPORT.html`、Markdown版は同じフォルダーの `REPORT.md`。`manifest.json` に今回変更した実行時ファイル7件の最終SHA-256を記録し、レポート生成時に一致を検査しています。集約した受入結果は `accepted/results.json`。再現用スクリプトは `scripts/check-responsive-followup-20260913.mjs`、`scripts/check-responsive-fixes-20260913.mjs`、`scripts/check-responsive-chart-regression-20260913.mjs` です。

この追加確認は上記の提出チェック3件を解消したものではありません。過去の検証レポート・途中の失敗証跡は保持しています。
