# MAP説明の引き継ぎセリフと立ち絵 — 2026-09-12

## 依頼と修正

- ユーザー原文: 「この立ち絵　セリフとあってないな」。添付は、あめの「ほいじゃ、次お願い」に汗・驚きの立ち絵が表示された画面。
- 対応解釈: このセリフに割り当てた表情を、落ち着いた引き継ぎに合う既存の笑顔へ変更する。
- 原因: `novel-mode.js` の `AMANE_STEP_EXPRESSIONS.map_mode01_016` に `startled` が明示されていた。前後は説明を終えてみずへ目配せし、次の展示を依頼する場面。
- 修正: 当該指定のみ `calm`（`amane-calm-07-v3.png`）へ変更。承認済みセリフ・画像そのもの・他の驚き表情の指定は変更しない。
- `gaia-mode-loader.js` と `index.html` の読込URLを更新。新規回帰試験を `check:story-map-handoff-expression:browser` に登録。既存の表情試験は驚きの対象を `esp32_pitch_011` に移し、今回のセリフを笑顔の検査対象として追加した。

## 検証対象

- 基点: `bd6c1c3c95ab245507293acd7f2f3714714f2606` ＋今回のローカル変更。今回の変更は未コミット・未公開。
- `novel-mode.js` SHA-256: `3a1b6109e73b72cf65cbb54734225af478f7943b576844229288c66e7322eab9`。
- `novel-story-data.js`（変更なし）: `2ff6aac5345ffc56ad2431460291a3dc5f21b18a9b554fa6132acb2430108d57`。
- `gaia-mode-loader.js`: `211e04e0e4716eae40f144a87a641e23ab429bbc9d044234fabadf958c983f2e`。
- `index.html`: `60ba3d6a6823979d4f84f1d9b08c109f8c3e545f59c0836ffe5950af61bd4a8e`。
- 使用画像（変更なし）: `9446b50bdc40129ea6e66366dfe5113f38d09169f8aa3c81e10895c1202eb074`。
- 回帰試験の開始・終了時に上記ファイルのハッシュ一致を検査。

## 再現・回帰結果

Windowsのインストール済みChrome（headless）、ローカル4492、本番相当CSP。ブラウザーの保存領域はユーザーのものとは別に作成。

- 修正前: `map_mode01_013` から進み、対象セリフで `startled` が出ることを再現。PCの画像でも汗と驚きのポーズを確認。記録は `artifacts/story-map-handoff-expression-20260912/before/report.json`。
- 修正後: 1440×900／390×844で、実際に表示されたセリフの座標をクリック／タップして対象まで進行。既存の笑顔画像のデコード、古い表情が残らないこと、セリフが画面下部に収まり祖先要素がスクロールしないことを確認。
- 対象をSAVE → 次のみずのセリフ（teasing） → あめの注意（exasperated） → LOADで対象へ復帰 → ページ再読込。復帰後もcalmで同じセリフと画像になることを確認。
- PCとスマホ幅の対象画面、および再読込後のスマホ画面を目視確認。表情・ポーズが自然な笑顔となり、顔とセリフが表示されている。
- 最終証跡: `artifacts/story-map-handoff-expression-20260912/final-v4/report.json` と `1440-handoff.png`／`390-handoff.png`／`390-restored.png`。4チェック合格、ページ例外・404・CSP違反0件。
- 関連試験: `expression-matrix-final/report.json`（同じ親ディレクトリ）。412×924で9件の表情、1280×800で2件の直接表示を確認。今回のセリフを含め、他の驚き・注意・柔らかい笑顔などが維持され、二重表示なし。対象のスマホ画像も目視確認。
- 試験改善: 初期の要素指定タップは、大きな会話コンテナを自動スクロールしてステージをずらした。表示されたセリフへの座標入力と遷移のヒット判定待ちに変更し、スクロールなしを検査した。初期の `after`／`final`／`final-v2`／`final-v3` および旧 `expression-matrix` の画像は最終見た目合格の証跡にしない。

## 静的確認と未確認範囲

- 変更JSの構文、ローダーprefetch、story-section-entry、承認済みストーリーの整合性、CSPハッシュ、差分の空白チェックに合格。
- 初期読込容量: 997,563／1,000,000 bytesで合格。
- Chromeのモバイル画面・タッチエミュレーションであり、実スマホ端末での試験ではない。外部HTTPSは遮断。本番配信・公開先キャッシュ・他ブラウザーは未確認。
- セリフ／保存形式／書き出し機能／配布物を変更していない。保存・ロードは上記の実UIで確認したが、書き出し・ZIP導入は今回の対象外。
