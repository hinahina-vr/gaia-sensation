# 全体表示ボタンの統合（2026-09-12、ローカル）

依頼: 下部の「全体へ」をズーム操作の近くへ移し、ズーム時に表示。世界は地球儀、日本は日の丸。フォーカス時に短い説明。

変更: app.js / index.html / map-unified-dock.css / map-mobile-shell.js。
共通コントロールで展示15–69を日本、その他を世界として扱い、全体表示からズーム・移動した時だけ戻るボタンを表示する。下部の旧ボタンは非表示。スマホでも右側の操作を表示し、既存操作メニューのアイコンも合わせた。

検証版: 現在のローカル作業ツリー、http://127.0.0.1:4492/。公開なし。

- `node --check app.js` / `node --check map-mobile-shell.js`: 合格。
- `node scripts/check-map-overview-controls-browser.mjs`: 合格。Chrome実ブラウザ、1440×900 / 390×900、展示13・17・21・41・69・70の計12条件。
- 拡大後のアイコン、フォーカス説明、クリック後の全体位置（世界1倍、日本PC6倍／スマホ4.25倍）、戻った後の非表示、再拡大時の再表示、旧下部ボタン非表示を確認。
- スクリーンショットでPC・スマホの説明位置と地球儀／日の丸を目視確認。説明の欠けなし。
- 初回試験で既存スマホCSSが操作欄を隠すことを検出。表示を修正し、12条件を再実行して合格。

証跡: artifacts/map-overview-controls/results.json と各PNG。
外部HTTPSを遮断して保存データで確認。実配信、全71展示の個別操作、スマホ操作メニュー内の代理ボタンの実操作は未検証。
