# MAP展示名の1行表示・座標表記統一 / 2026-09-12

## 依頼

ユーザー原文: 「1行になるように横幅調整して　他のもチェックして」

画像は68「川底の小さな生きもの」の左下ドック。解釈: 文字を小さくしたり名前を省略したりせず、横幅を調整して1行で読めるようにし、他の展示名と周囲の操作欄も確認する。

追加原文: 「じゃ統一しといて」。直前の提案「画面表示は03の『北緯／南緯・東経／西経』を共通ルール」「北緯などを上段、東経などを下段、観測値は右に大きく」を引用した依頼。

基点: `9fbd901b68faebaf6148db6e18cdbcb50fa1c778` 上のローカル作業ツリー。先行する数値強調・横並び・座標の2行化、および他の未公開変更を保持。今回のコミット・push・デプロイなし。

## 実装

- `map-chapter-navigation.css`: 共通の見出し欄を最小360pxへ拡張し、01〜30の折り返し・省略を解除。フォントサイズと展示名自体は変えない。
- `marine-cod-exhibit.css`: 65〜69の固定幅を最小幅に変え、長い名前に合わせて欄が広がるようにした。68は1440px幅で欄300→360px、文字21pxのまま1行。4Kでは欄480px・文字28px。期間ボタンも途中で折り返さない。
- `food-exhibits.css`: 70・71も内容に必要な見出し幅を確保。
- `realtime-exhibits.css`: 901〜1100pxの01〜05を上下の操作段に配置し、見出しを広げても観測状況・値・期間操作・出典が画面外へ出ないようにした。
- `map-unified-dock.css`: 901〜1200pxの15〜20を場所/時刻と見出し/値/操作の2段へ整理。既存の装飾円がドック外へはみ出していたため、右端内に収めた。タッチ用レイアウトは変更していない。
- `src/shared/coordinates.js`（追加）と `app.js`、`firms-exhibit.js`、`planet-signals-exhibit.js`: 座標の日本語整形を共通化。01のPOI・詳細・分析用表示ラベルも北緯/南緯・東経/西経に統一。画面上は既存の2行レンダラーを使用。各データの既存の小数桁数を維持し、符号付き座標・観測値は変更しない。

## 再現と最終確認

インストール済みChrome＋Playwright、`http://127.0.0.1:4492`、本番相当CSPを適用し外部HTTPSを遮断。同梱の元データによる実ブラウザー操作。以下は本番配信・物理スマートフォンでの確認ではない。

- `scripts/check-map-chapter-width-browser.mjs --before`: 901・1440・3840px、全71展示の213条件を実画面で記録。68の折り返し、65/66/67/70などの複数行、01〜30の省略表示を再現。
- 同スクリプトの最終版: 同じ213条件すべて合格。全展示名が1行、名前の変更なし、省略・はみ出しなし、周辺のボタン・入力欄はドック内。別途before/afterを機械比較し、213条件のフォントサイズが同一、次の展示ボタンの余白が保たれることを確認。
- `--interactions --widths=901,1440 --numbers=1,3,16,68`: 8条件合格。各ボタン/入力欄の実ヒット領域、展示一覧の開閉、期間スライダーのHome/End、次の展示→前の展示を実操作。
- `scripts/check-record-dock-compact-browser.mjs`: 1440×900・901×900・390×844・320×568、66/67の8条件合格。実在の0 kg/年度・2届出物質を選択し、値・修飾情報・内訳ダイアログ・選択欄・出典の到達性を確認。31・70への退出も確認。既存試験を分割年次データの読み込みに対応させ、古い「PRTRは2行」の期待値を今回の1行に更新した。
- `scripts/check-map-unified-dock-browser.mjs`（`GAIA_FOCUSED=1`）: 1440・1024・390px、39条件合格。21との書体/サイズ、06〜20の関連表示、15〜20の時間スライダー・元データとの値/時刻一致を再確認。後続の01〜05専用CSS修正はこの試験の対象外であり、上記全展示・操作試験で確認している。
- `scripts/check-poi-value-variants-browser.mjs`: 50項目合格。01は同梱FIRMSの実観測点を北東・北西・南東・南西から選択し、実ホバーで2行の方角表記を検証。分析データのlat/lon/観測値が元データと同一で、表示ラベルのみ変わることも確認。70/71の実観測値 `205.3%` / `-128.2%` とクリック選択も確認。
- 同variants試験の02/03は合成キャッシュによる統合試験、長桁・0・欠測・HTML風ラベル・各方角の細かな行配置は合成レイアウト試験。実観測による01/70/71と区別する。元の数値強調の詳細は `QA_POI_VALUE_EMPHASIS_2026-09-12.md`。
- `node --check`（app/共通座標/POI表示/FIRMS/Planet）、`git -c core.safecrlf=false diff --check`: 合格。

狭いPCの追加境界チェック（1024・1100・1200・1280・1399px、01〜05/16/21/65/68/70の50条件）も合格。`after-narrow-final/report.json` に記録。検証後の追加コード変更なし。

## 証跡・版の識別

- `artifacts/map-chapter-width-20260912/before/`: 同操作の変更前。
- `artifacts/map-chapter-width-20260912/after/`: 最終213条件と68のドック画像。
- `artifacts/map-chapter-width-20260912/after-interactions/`: 最終8条件の実操作・画像。
- `artifacts/map-chapter-width-20260912/after-narrow-final/`: 最終の中間画面幅。
- `artifacts/record-dock-compact-2026-09-09/after/`: 今回更新した8条件。
- `artifacts/map-unified-dock-20260912/after/`: 今回の39条件（以前の広い条件の証跡を今回実施したと数えない）。
- `artifacts/poi-value-emphasis-20260912/variants/`: 50条件。01の南緯/西経の実観測カードを目視確認。

各report.jsonに対象ファイルのSHA-256を記録。最終章幅CSS:

| ファイル | SHA-256 |
| --- | --- |
| map-chapter-navigation.css | f72ee0673483656fec0ca3b3a062f3068d357d01b131c4dbe05afe196d937716 |
| marine-cod-exhibit.css | 36e91aa640cc697f2c30ddb21709a3b37b3cd4b40ee04ce8fb905f0141ff04e8 |
| food-exhibits.css | 822cb6798fc9c9cffb79bc9b066ba6794a6c0a158cdc657a3abcf21383fc4a6e |
| map-unified-dock.css | 4f3f848b615cfb1d64e4e46465a87e5fd4fed46c6dea85d46e43ea51ff64e3f6 |
| realtime-exhibits.css | 1a86c1e76ac3b2ca81f818b05926ab50a692d4c25a008dfc829668be3c2f15e8 |

68の1440pxドック、901pxの01/16ドックを目視確認。狭い画面での2段化は周辺の情報/操作欄であり、展示名そのものは1行。

## 未確認・残件

今回の変更に関するローカル確認のみ。公開反映・本番スモーク・物理端末・全履歴/全保存経路の再試験はしていない。新しい保存/書き出し機能や配布ZIPなし。従来のデータ構造・数値・履歴保存は変更していない。以前のSOUND・本編・OP候補などは今回の範囲に混ぜず保持。
