# 全展示の主要数値カウント

対象：2026-09-12 ローカル作業ツリー。未公開。

## 実装

- 展示22と同じ760ms・32段階のイーズアウトを主要数値欄に適用。
- 01、02–05、06–13、15–20、31–69、70–71は `src/shared/animated-metric.js` を使用。14は地図上の人口表示との同期を保ち、既存カウントを同じ時間・段階に調整。21–30は既存の共通カウントを維持。
- 年・時刻・単位や元データは変更しない。欠測・限界値は即時表示。再描画で同じ値のカウントを再始動しない。途中変更は表示中の値から開始。動きを減らす設定では即時表示。
- 範囲は各展示の主要数値欄。出典・分析表・全POIラベルを文字列監視して書き換える方式ではない。

## 検証

- ローカルChrome、1440px/390pxで全71展示の表示を確認（初回走査の31–69は未選択状態）。
- 追加の1440px走査では31–69の地域・地点を選択し、途中の数値フレームを採取。56/61–64は追加走査で地点を変更。欠測・「未満」等の値は補間しないことを確認。
- 390pxは02/12/15/31/38/44/55/65/69/70/71の選択・途中フレームを追加確認。
- 共通関数の実ブラウザ試験：増加、減少、再描画、途中キャンセル、欠測への復帰、日付維持、小数、桁区切り、reduced-motion。
- 人口の既存ブラウザ回帰試験：1440px/390px合格。
- 最終調整後は12/15/31/70を1440px/390pxで再確認。

証跡：`artifacts/all-metric-count`、`artifacts/all-metric-selected`、`artifacts/metric-selected-mobile`、`artifacts/metric-final-regression`、`artifacts/metric-last-check`。
試験：`scripts/check-all-metric-count-browser.mjs`、`scripts/check-population-count-browser.mjs`。

外部HTTPSは遮断し、保存データ／FIRMS保存スナップショットで実施。実端末・外部ライブAPI・公開環境は未確認。分析・書き出しのデータ生成処理は変更していない。
