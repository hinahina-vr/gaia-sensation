# 日本統計展示・説明文の背景透明化 — 2026-09-09

## 変更内容

添付画面はMAP 30「雨と暮らしの暦」の年間降水日数の説明。説明文は変更せず、その背後の緑色の帯を透明化した。同じ説明行を使うMAP 21〜30にも適用する。

背景は説明文自身ではなく、下部操作欄と共有する `.gaia-estat-readout` に付いていた。また `panel-surfaces.css` の共有80%背景指定が優先適用されていた。

- `map-chapter-navigation.css`: 説明行が表示される1501px以上で親パネルの背景・ぼかし・影・外枠を外し、操作欄の行だけに背景用の疑似要素を配置。説明行の下罫線も除去。疑似要素は絶対配置でグリッドの自動配置に参加せず、クリックを妨げない。
- `panel-surfaces.css`: 共有80%背景を操作欄の疑似要素へ適用し、説明を含む親パネルには適用しない。操作欄の数値・ボタンの背景は維持。
- `gaia-mode-loader.js`、`index.html`: 対応する読み込みURLを `copy-transparent-20260909` に更新。
- 回帰試験とnpmコマンドを追加。既存の説明文レイアウト試験は古い展示数30件固定の準備待ちをアプリ準備完了判定に置換。

1500px以下で説明行を省略し、スマホの「読み方・凡例」に説明を載せる既存動作は変更していない。

## 確認結果

ローカルWindows Chrome、`http://127.0.0.1:4447`、同梱統計データを使用。

- `node scripts/check-estat-copy-transparent-browser.mjs --before`: HEADの旧CSSを配信し、1920×1080で緑の説明帯を再現。
- `npm run check:estat-copy-transparent:browser`: 18件合格。3840×2160、2560×1440、1920×1080、1501×900、1500×900、1024×768、390×844。元の説明文が保持され、操作欄の80%背景が残り、前の展示・出典パネル・スマホ説明が利用できることを確認。
- 広幅4画面では、文字だけを一時的に隠して背後を固定の市松模様にし、説明文字領域の実スクリーンショットをパネル自体を隠した場合と比較。PNGが同一で、地色・ぼかしがないことを確認。比較範囲は説明行の下9pxの余白を除く（そこには維持した操作ボタンの微弱な光彩が届く）。通常の地図表示のスクリーンショットも別途保存し、1920・1501・3840pxのドックを目視確認。
- `check-estat-copy-layout-browser.mjs`: 3840、2560、1920、1600、1501、1500、1440、390pxでMAP 21〜30の計80件合格。長文の折り返し・省略なし、品質注記、操作欄・タイムラインの配置、横はみ出しなし、スマホの説明表示を確認。
- 変更JS／テストの `node --check`、`git diff --check`: 合格。

## 検証版・証拠・未確認

基点コミット `aaa9153116c704f14d82f48969a396b907388451` の上のローカル未コミット作業ツリー。他の依頼による既存変更は維持。

記録: `artifacts/estat-copy-transparent-2026-09-09/{before,after,layout-regression}/report.json`。`after/report.json` に対象CSS2件・読み込みファイル2件のSHA-256を保存。終了時に検証ファイルとディスク上の内容の一致を再確認。

未確認: 物理スマートフォン、本番配信、全ストーリー通し操作。背景・レイアウトの修正であり、保存・書き出し機能は変更対象外。ZIP作成、push、デプロイは行っていない。
