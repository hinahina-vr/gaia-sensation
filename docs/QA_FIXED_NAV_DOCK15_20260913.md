# 展示15基準の下部枠・左上固定ナビ（2026-09-13、ローカル）

## 依頼と対応

原文：「下のUI、全部15に合わせてほしい」「2枚目みたいなのは左上にもってきゃええやん」「2枚目のボタンの表示位置固定してほしい 今ガコガコ動いてかなわん」。

- 下部パネルと注記の高さを毎回計測してナビを移動する処理を撤去。前／展示メニュー／次の実ボタンは、PCで左16px・上76px、スマホで左12px・上70pxを基本とする固定位置。safe-areaに対応。表示中の展示・注記・下部パネルの高さには追従しない。
- メニューは固定ナビの下へ展開。スクロール可能な一覧の上端も世界／日本で共通。既存のホバー／フォーカス入口、1.2秒のZ方向発光、Escと選択後の閉じ方を維持。
- PCの全プロバイダーの下部操作枠を展示15基準へ統一。通常100px、大画面124px、幅901〜1200pxでは161px・2段配置。画面下端、上側の角丸、見出し・数値の書体とサイズ、出典／分析の位置を共通化。各展示固有の地点選択・時間スライダー等は実要素を維持。
- 長い年間データの注記と比較値を「解説」の開閉欄へ収め、操作枠を押し上げないようにした。内容を削除していない。元のノードを再利用するため、年・地点による更新も維持。
- スマホは既存のコンパクトなデータ欄／モーダルを維持し、ナビを固定。タイトル行をその下へ移して重なりを避けた。

## ファイルと版

- `map-stable-navigation.js`：高さ追従・位置ロック・関連監視の撤去、注記と比較欄の開閉表示。
- `map-stable-navigation.css`：左上の固定位置、一覧の上端、スマホのタイトル位置。
- `map-ui-grid-polish.js`：一覧の配置を下部パネル基準から固定ナビ基準へ変更。
- `map-unified-dock.css`：展示15基準の下部枠・各データ欄・狭幅2段・解説の配置。
- `gaia-mode-loader.js`、`index.html`：`fixed-nav-dock15-20260913` のキャッシュ参照。
- `scripts/check-fixed-map-navigation-browser.mjs`：再現操作の回帰試験。

基点 `0034ee39cd906960c458d0cf84f6a48e719c39ab` と既存ローカル作業に今回の差分を追加。未コミット・未プッシュ・未デプロイ。古いQA記録や他のローカル作業は保持する。

## 実ブラウザー確認

実Chromeとローカル4492番の画面、保存データを使用。実際の矩形、elementFromPointのヒット対象、ネイティブマウス／タッチ／キー操作とスクリーンショットを確認。

- 変更前：展示15でナビ上端695px、展示1で680px、展示21・27・8で728px（1440×900）。画面と計測で高さ追従による差を再現。`artifacts/fixed-nav-ui-20260913/before-*.png`。
- 新回帰試験：1440×900で全71展示、390×844・1024×900・2560×1440で各10展示、計101条件が合格。遷移中のナビ座標不変、横方向のはみ出しなし、PC下部枠の高さ・下端一致、ナビと出典／分析の実ヒット対象、世界／日本一覧と展示27への選択、解説の展開を確認。`final/report.json` と各PNG。
- モバイル見出しのCSSレイヤー優先順位を修正後、390×844の10展示を再確認。タイトルと固定ボタンが重ならないことを新しいアサーションと実画面で確認。`mobile-final/report.json` と各PNG。
- `check-map-stable-next-interactions-browser.mjs`：PC／スマホ14チェック合格。固定座標での連打（21→33、33→20、69→3、5→8、14→17、63→67）、キーボード、既存の別メニュー入口。各連打のX/Y位置幅はともに0px、実ヒット対象は全サンプルで有効。`interactions/report.json`。
- `check-map-picker-cascade-browser.mjs`：9チェック合格。ホバー・Tab・上下キー・クリック・Esc・外側クリック、世界／日本、1.2秒の実アニメーション、reduced-motion、表示途中のキャンセル、展示15へのURL遷移。証跡は既存の `artifacts/map-picker-depth-20260913/` に更新。
- `node --check`、`check-mode-loader-prefetch.mjs`、対象ファイルの `git diff --check` 合格。

初回の高さだけ揃えた段階で火災の文字や狭幅の下部行が切れることを実画像から検出し、情報欄の配分・2段配置・行高を調整して再検証した。出典・分析の矩形とヒット可否も回帰条件へ追加した。

## 境界・残件

今回の固定位置・下部枠はローカル実装と上記QAまで完了。本番公開、実機スマホ、全言語・全画面幅の組み合わせを確認したという意味ではない。データの保存・書き出し形式、観測計算、WebGLエフェクトは変更していない。

直前の「クルージング中に火柱が出ない理由」は原因調査済み・未修正。クルージングのスライダー更新が手動スクラブと扱われるため、火柱描画の条件から外れる。今回のUI変更へ修正を紛れ込ませていない。過去の翻訳等の残件・検証範囲は各既存QA記録を維持する。
