# コンセプト02章の指定文・削除・主語整理

## 依頼と実装

2026-09-10の「02 DEEP CONCEPT」導入・講義紹介の差し替え、指定段落と装飾説明の削除、追加の「全体的に 作者が というのは自明なのでいらない」と橋渡し文の削除に対応。基準コミットは `888c26b623151205f6b8ecf9f77951dbc9a425ae`、ローカル版 `concept-19-learning-20260910`。先行する未公開変更は保持。コミット・push・デプロイは行っていない。

- 導入見出しを「惑星の片隅、私たちが立っているここから。」にし、本文と「4つの講義と、物語への接続」の前書きを指定原文で差し替えた。句読点・語句・原文内の空白を保持。
- 装飾イラストのキャプションのみ削除し、画像自体は保持。4講義の指定段落を各1つ削除し、講義名・残る2段落ずつ・4つのシラバスリンクは保持。統計学の削除指示に重複していた文章は、実装上の該当段落全体として削除。
- 「データから何が言えるか。」から「作者自身のAI構想へ進みます。」までの橋渡し段落をDOMから削除。見出し、深層へのリンク、明暗のグラデーションは保持。
- 本文中の「作者は」「作者が」を省き、「作者の構想へ」を「構想へ」、「本作は作者個人による」を「本作は個人による」に整理。非公式作品・大学の公認ではないこと・フィクションの注意書きは保持。本文の主語重複とは区別して、奥付の氏名表記「作者：ひなひな」とプロフィール・noteリンクは残した。
- 長くなった講義の前書きを本文と同じ14px以上の読みやすいサイズへ変更。位置づけの末尾「結びつけています。」を語句単位で折り返す。

変更の中心は `concept/index.html` と `concept/concept.css`。指定文の全文・残した8段落・削除禁止の回帰データは `scripts/lib/concept-editorial-copy.mjs`。既存の静的・editorial・author-oracle・linebreak試験を今回の指示に合わせて更新した。画像、構想図、JavaScriptの動作は変更していない。

## 実検証と対象版

証跡は `artifacts/concept-learning-20260910/`。インストール済みChrome、localhost:4492、本番CSP適用、分離コンテキスト。スマホは画面寸法・タッチのエミュレーションであり、物理端末や本番での試験ではない。

- `editorial/report.json`: 8ケース合格（1440×900、2274×1100、3840×2160、768×1024、390×844、320×568、280×653、844×390）。指定文と残した段落の全文照合、削除要素の不在、装飾画像保持、14px以上の前書き、A/B/Cと画像配置、ページ内リンクのクリック／タップ、ヘッダー、横はみ出し、奥付・注意書きの語句折り返し、CSPを確認。PC・スマホの学びと橋渡しの実画像を目視。この試験は末尾の改行修正直前のHTML（`5abef852fcae80569d96de97970faf6fca2da29c36a3d53472d3b5ee29b09a10`）に対する結果。
- `linebreak/report.json`: 文言変更後、390px幅で位置づけの末尾「す。」だけが改行される症状を文字単位の実描画で検出。語句の折り返しを修正した。
- `linebreak-final/report.json`: 最終HTMLで9幅合格（280、320、360、390、412、480、768、900、1440px）。同じ操作で橋渡しの段落削除、見出し、短くした位置づけの描画、語尾の孤立・句読点・はみ出し、深層へのリンクとヘッダー下への到達、明暗テーマを再検証。390pxの位置づけの画像も目視し、語尾全体が1行に収まることを確認。
- `reader-final/report.json`: 最終HTMLで4ケース合格（1440×900、390×844、320×568、768×1024）。4講義と残した8段落の全文、シラバスURL、作者奥付、フッターの注意書き、本文のはみ出し、図を開く・拡大・Escape・フォーカス復帰、深層と先頭へ移動、noteリンク実クリックを検証。外部ページは読み込みを遮断し、noteの遷移要求URLまでを確認した。シラバスは保存済みのリンク属性の確認であり、外部サイトの再閲覧ではない。
- 最終HTMLで `npm run check` 全体合格（終了コード0）。静的な全文・削除照合も含む。テストが意図的に発生させる外部データ503フォールバックのログは、実配信の失敗として扱っていない。
- `git diff --check` 合格。最終2ブラウザ試験の3ファイルハッシュが作業ツリーと一致することを照合。

最終ハッシュ:

- `concept/index.html`: `32e8363ba093c0bc82279ee77ce2a1bc8c6543c7ff4c4f0f618d6807ec4d0120`
- `concept/concept.css`: `bd3a377fff0228240979015055a3c15844662f3bf93f5905e0dfb57c759452a9`
- `concept/concept.js`（未変更）: `4696cfa83134cef43cce1dcf47feb3eec05aeb00316d0927aae1bbc9c9eac78a`

## 引き継ぎと未確認範囲

今回の文言・削除・関連表示のローカル対応は完了。公開は未指示のため未反映。物理スマホ、Safari/Firefox、公開配信、実印刷は今回未確認。新規ZIP・PDFや保存・書き出し機能は追加していない。前回の本編入口・JavaScriptなし・印刷CSS等の証跡は `docs/QA_CONCEPT_EDITORIAL_2026-09-10.md` を参照し、今回その全範囲を再実行したとは扱わない。本文のみの更新で本編の動作は変更していない。

本編の通常会話にある短い横画面での顔と会話領域の重なりは、コンセプト本文の変更では解消しない既知の未対応項目として引き続き保持。次に本編の横画面レイアウトを扱う際に、会話中の実操作と配置を検証する必要がある。
