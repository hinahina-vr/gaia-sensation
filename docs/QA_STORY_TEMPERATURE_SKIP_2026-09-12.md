# 気温偏差のスキップ表示 / 2026-09-12

依頼原文: 「1枚目　スキップ▶　にして」

添付の「物語へ戻る ↗」は、本編内のNASA気温偏差画面の右上ボタン。`story-temperature.js` の表示を「スキップ▶」に変更し、読み上げ名は「スキップして物語へ戻る」とした。矢印を▶に変更し、`story-temperature.css` の間隔を6pxに統一。`gaia-mode-loader.js` の該当CSS/JSのキャッシュ識別子も更新した。スキップ先と保存処理は変更していない。

## 確認済み

インストール済みChrome、ローカル `http://127.0.0.1:4492`、本番相当CSP、外部HTTPS遮断。既存の直前の場面を保存データで準備し、以降は実際のボタン・キー・タッチ操作を使用。

- `scripts/check-story-temperature-browser.mjs --only=pc`: 1440×900、合格。
- 同 `--only=mobile`: 390×844、合格。
- 可視テキスト「スキップ▶」と読み上げ名を検査。実クリック/タップで `map_mode01_024` に戻り、古いCO₂画面が途中で露出しないことを確認。
- 年の切替・前後・地点選択の値と同梱バイナリの一致、欠測表示、出典の開閉、SAVE/LOAD、台本Markdownの実ダウンロードと内容、本編の次の台詞まで確認。
- 1440pxの実画面PNGを目視確認。JS構文検査と `git -c core.safecrlf=false diff --check` は合格。

証跡: `artifacts/story-temperature-skip-20260912/pc/` と `mobile/`。各 `report.json` に対象ファイルのSHA-256、画面PNG、ダウンロードした台本を保存。

## 未対応・確認待ち

同じ依頼の「2枚目　スライダーの動きがぎこちなくてGIFアニメみたいなので滑らかにして　ヒートマップもクロスフェードで滑らかにして　あとUIの不透明度は　80%」は未対応。画像は1枚しか届いていないため、対象画面を特定する2枚目の再送を依頼している。

候補の気温偏差画面には260msごとに整数年を切り替えて地図を即時再描画する処理があるが、2枚目と同一であるとは断定していない。今回の変更にはスライダー平滑化・地図クロスフェード・不透明度変更を含めない。

ローカル反映のみ。コミット・push・デプロイなし。本番・物理端末で確認したとは扱わない。
