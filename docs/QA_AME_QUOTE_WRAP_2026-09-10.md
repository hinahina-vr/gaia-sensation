# あめの引用文・改行位置（2026-09-10）

依頼: 添付の「呼吸して／るリズム」を「呼吸してる／リズム」に調整。

対応: `index.html` のあめのオープニング台詞だけを変更。既存の `gaia-vn-quote-keep` で「呼吸してる」を分割不可にし、その直後に `<br>` を追加。台詞の文言・文字サイズ・演出・みずの台詞は変更なし。CSSおよびJSの変更なし。

対象版: 基準HEAD `cdc82c8ffd4fee607bd2d838ec3157b7095952d3` 上の既存ローカル変更を保持した作業ツリー。対象HTML/CSSのSHA-256は `artifacts/ame-quote-wrap-2026-09-10/after/report.json`。

ローカルChromeで通常のサウンドオフ選択からオープニングを実再生。あめの場面が表示された時点でアニメーションを一時停止し、実際の文字矩形・画像を検査した。

- 修正前1440×900: 「呼吸して」で行が終わり、次行が「るリズム」から始まる症状を再現。
- 修正後1440×900、3840×2160、390×844、320×568: 全4件合格。いずれも1行目末尾が「呼吸してる」、2行目先頭が「リズム」。横方向の文字切れなし、全文一致。
- PC・スマホ相当の引用文スクリーンショットを目視確認。全画面と引用部分の画像を上記artifacts内に保存。
- 既存のみずの語句保持、JSページエラーなし、CSP違反なし、`check-app-content.mjs`、対象ファイルの `git diff --check` 合格。

再実行: `node scripts/check-ame-quote-wrap-browser.mjs`。修正前証跡は同スクリプトの `--before` で収録済み。

ローカル反映・検証完了。push・デプロイなし。スマホはChromeの画面サイズ・タッチエミュレーションで、実機/Safari/Firefoxは未検証。テキストの改行のみのため、保存・書出し処理や配布ZIPの変更なし。
