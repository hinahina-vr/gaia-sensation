# 経過日数スライダーの連続移動

連続時間軸でsignalTimePositionを描画フレームごとに更新し、rangeの刻みを0.01に変更。重い説明パネル更新は既存ステップ頻度を維持。国別選択（09・12・13）や地震の年別スケジュール、別モジュールの年度スライダーはこの連続時間処理とは別で変更していない。

check-smooth-slider-browser.mjs: ローカルChrome1440/390幅、07の経過日数で40msごとに25回スライダー実値を取得し18以上の異なる値、小数値、順方向の小刻みな移動、停止後の位置保持を検証。証跡artifacts/smooth-slider。外部API遮断・保存データ使用。本番・実機未確認、未公開。
