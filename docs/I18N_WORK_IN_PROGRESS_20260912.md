# 英語・簡体字翻訳 — 作業中（未完了・未公開）

## 依頼と範囲

- 原文「英語　中国語　翻訳して　タイトルの設定に応じて表示して　画像は翻訳不要です」
- 確認回答「画面全体＋ストーリーの台詞も翻訳」。全 UI、展示・説明・設定・ストーリー・後日談・ログを対象とする。画像ファイルは変更しない。
- 追加依頼：元の日本語明朝に合うタイプフェース。上部の時刻・場所、オープニングの見出し・説明もすべて翻訳。
- 日本語の承認済み台本・数値・ID・保存キーは維持。今回は公開指示なし。

## 実装済み、検証途中

- `gaia-i18n.js` / `gaia-i18n-catalog.js` / `gaia-i18n.css`: 既存 `gaia:language:v1` と `gaia:language-change` に連動。保存・直リンク・別タブの変更に対応。既知の表示文言のテキストノードと表示用属性のみ翻訳。画像、フォーム入力、ユーザー入力、数値ルート、文字送り中の DOM を対象外にする。
- `locales/story-*.js`: 本編 6 場面・後日談 3 場面の本文を英中に翻訳。原文データを改変しない読み取り専用 view を物語ランタイムへ渡す。
- 後日談の翻訳は全文からページを再計測する実装。実ブラウザで後半・保存復帰を確認する必要がある。
- 展示ナビゲーションの 3 ボタンと全件数・読み上げラベルを翻訳。
- 追加フォント依頼：英語は Yu Mincho / Baskerville / Times 系、中国語は Songti / SimSun 系。タイトルの太さ・字間を調整。
- 日付・場所・冒頭パネルを辞書に追加済み。動的な複合文言を、明示的に登録したテンプレートと UI 区切り単位で翻訳。入力値・技術 ID は変更しない。入力欄の placeholder は翻訳。
- `locales/ui-*.js`、`exhibit-titles.js`、`metric-labels.js` を追加。index の全テンプレートの既知固定文言（592件の初回採集）は登録済み。noscript と native 言語ボタンの注記3件は除外が必要。
- 国名は Intl.DisplayNames、47都道府県と代表都市は対応辞書。日本の中国地方が China と誤訳されないよう、地域選択の原文を「中国地方」とした（観測IDや値は不変）。
- 本編は言語変更時に同じ step/session のまま再描画。言語ごとにページ長が違うため現在段落の1ページ目へ戻す。保存一覧も step ID から現言語の見出し・抜粋を表示。後日談にも listener を追加したが後日談での実動作は未確認。
- 古い「今後実装予定」は利用規約と参加説明の残り2箇所から除去（以前のユーザー削除依頼の残件）。

## 確認できたこと（限定的な途中証跡）

- 修正前再現：`GaiaLanguagePreference.set('en')` 後も sound title が「サウンド設定」。
- 修正後、実 Chrome でタイトルの英中切替、CDP が報告する使用フォントを確認：日本語 Yu Mincho Demibold、英語 Yu Mincho Regular、中国語 SimSun。
- `artifacts/i18n/title-font-{ja,en,zh-CN}.png` を撮影し英中を目視確認。
- `GaiaNovel.open(null,{autoStartFresh:true})` の英語 1 ページ目を実描画。元の日本語データ不変。上部の時刻・場所英訳も確認。`artifacts/i18n/story-en-rendered.png`。
- `story-en-initial.png` は読み込み途中の画像であり本編確認証跡ではない。
- `node scripts/check-i18n-story-browser.mjs` 合格：1440×900 / 390×844、英語・簡体字の4条件。サウンド画面の native 言語ボタン、実オープニング、時刻・場所、本編本文、各場面の長い台詞（各3段落）の全文ページ連結と実測fit、言語変更で step/session 維持、保存・別言語のプレビュー・読込。
- 同試験で英語冒頭見出しの横溢れ、wbr 境界の `facefor` を再現→修正。`artifacts/i18n/story-browser/report.json` 合格。`390-en-opening.png` / `390-zh-CN-story.png` を目視確認。地図辞書はこの合格後も拡充中なので、最終版で影響範囲を再確認する。
- `scripts/inventory-i18n-modes-browser.mjs maps` は全71展示の実切替・readyまで完走し、元日本語のDOM/属性と GaiaAppContent を3,760件採集。これは翻訳合格ではない。
- `scripts/filter-i18n-inventory-browser.mjs` は実ブラウザ辞書で全採集文言を再評価し、部分翻訳にも残る日本語を検知。現在 `artifacts/i18n/maps-pending.json` に1,913件が残る（観測値等の複合文言・固有地点名を含む）。
- `node scripts/check-i18n-catalog.mjs`：3,742登録項目（動的テンプレート含む）、本編377本文＋後日談164本文・全メタデータの登録を確認。原文2ファイルのSHAは不変。全UI合格ではない。

## 次に継続する作業（完了報告禁止）

1. `node scripts/check-i18n-catalog.mjs` で全ストーリー・メタデータの登録漏れを確認。
2. 全画面の翻訳辞書を拡充。`scripts/inventory-i18n-ui-browser.mjs` で index の全テンプレートを採集し `artifacts/i18n/ui-untranslated.json` を出力。展示 71 件、POI、ガイド、統計、BYOK、各モード、センサー・concept ページ、クレジットも継続対象。
3. キャンバスに描く文字は DOM 翻訳だけでは適用されない。明示的に表示時翻訳を追加すること。ユーザーの「画像は不要」は生成済み画像素材であり、キャンバス上の動的な文字の除外承認ではない。
4. 後日談の全ページ・場面境界・最終部、ログ全台本・チャット・書き出しを実動作確認。字幕や保存プレビューの追加対応は済んだが、各経路の最終確認は継続。
5. 文字送り、全本文のページ割り、後日談、チャット、ログ、保存・読込、全71展示・ナビ・URL・巡回を PC/スマホで回帰確認。日本語へ戻したときの復元と画像未変更も確認。
6. 全体 npm check と差分チェック。対象版・実測証跡・未確認範囲を最終 QA に記録。

既存のクルージング・展示 URL・ナビの変更は前タスクでローカル QA 済み。今回に関係する回帰は必要だが、過去の成果物や作業履歴は削除しない。公開は新しい明示依頼がない限り行わない。

## 採集IDと継続ポイント

- 原本 `artifacts/i18n/maps-untranslated.json` は3,760件の固定インベントリ。各行の source を `locales/` に翻訳している。今後の追加チェックで原本を上書きしないこと。
- 原本 `other-untranslated.json` は109件。ui-gallery-space と ui-story-runtime で大半を登録。モードをloadしただけの採集で、全モードを開いて操作した検証ではない。
- 元の index 初回592件 `ui-untranslated.json` は再採集済みで今は除外対象3件のみ。既存の ui-common/intro/sources/map-lab/story-space が592件の主辞書。
- map source ID 0–273 は ui-map-controls / geography / metric-labels が対応（一部数値テンプレート）。274–279の長い案内、286以降の操作ラベル、509以降の GaiaAppContent の説明は未登録が多い。大量の複合値はテンプレート化し、実際の数値を個別翻訳登録しない。
- `gaia-i18n.js` は templateList を literal 部の長さで降順にし、広いテンプレートより特定文言を優先。英語の日本語残り検知は「t(source)===source」だけでは不十分（部分翻訳を見逃す）。filter script のCJK検知を使用する。
- 注意：単語「再生」は再資源化タイルと再生操作で意味が違う。metric-labels の単語訳は Recycling だが、一般のプレイヤーUIは Play にすべき。属性／コンテキストで分離すること。
- 古い入口説明には現行展示番号と不一致の記載が残る。旧MAP26 FIRMS等の source diagram、旧ストーリー概要など。翻訳登録は原文通りだが、現行UIで見える経路を確認して照合すること。

## 追補：追加スクリーンショットの修正・再検証（2026-09-12時点）

### 原因と修正

- ユーザーの追加画像：入口のフォーカス説明、地図・センサーのイラストガイド、出典の展示名、凡例、展示26の統計解説、白いプロローグ、クレジット、最後のボタン、音楽紹介。全て進行中の翻訳範囲に含む。
- 白いプロローグの「in」混入は `{pref}の{metric}` の汎用置換が未知の文章まで並べ替えたのが原因。実ブラウザで修正前を再現してから削除した。明示的な全文辞書と表示フィールド用テンプレートに限定し、翻訳できない捕捉値を含むテンプレートは採用しない。未知の文が順序不変で残る回帰テストを追加。深い再帰で得た未翻訳値をトップレベルのキャッシュに混ぜない。
- opening のフォーカスガイドは原稿上の改行ごとの断片ではなく、段落全体を GaiaI18n.bind に渡す。白いプロローグ8行・クレジット役割/名前・締め文・最後のボタン・12曲の紹介を全文登録。
- 音楽選択時の「Missing constellation glyph」を英中で実際に再現。ASCIIコロンを追加し、手描き文字にない翻訳文字は言語に合うフォントで描く。日本語の既存手描きパスは維持。言語切替時にアクティブな曲名エフェクトを再計測。
- 地図ツールチップは訳文を反映してから位置を計測。英中は出典と地点を上下に分け、画面外にはみ出すのを防ぐ。英語の年スライダー見出しは必要時に2段で重なりを防ぐ。
- sensors/index.html に翻訳基盤と共通・ガイド辞書を追加。`data-description` による説明も翻訳する。センサー本体の全機能はまだ翻訳途中。
- 展示21・22の旧「月ごと」を現行の年次データと照合して「年ごと」に訂正。過去fixtureは書き換えず `docs/design/annual-picker-copy-20260912.json` を追加。テストでも元データの期間が年単位であることと catalog.frequency を照合。

### この段階で合格したテスト（未公開）

- `check-i18n-reported-surfaces-browser.mjs`：修正前再現 `artifacts/i18n/reported-surfaces/before/report.json`。修正後は1440×900 / 390×844 × en/zh-CN全4条件、実タイトル→白いプロローグ・ガイド全文・ボタン・日本語復帰・横溢れ。最新再実行も合格。
- `check-i18n-guides-browser.mjs`：地図／センサー × PC／スマホ × en/zh-CN、全8条件合格。画像ロード、ガイド全テキスト、日本語混入、横溢れ、開始ボタンを確認。センサーAPIのみ未ログイン・空データのread-only fixture。本番/実機センサーの検証ではない。
- `check-i18n-ending-music-browser.mjs`：全12曲の選択と紹介、クレジット役割と名前、締め文・ボタン、後日談3場面の入口と最終画面、日本語復帰。PC/スマホ×2言語の全4条件合格、pageerrorなし。専用保存データからクレジットに入り、reduced-motion表示とnative場面スキップを使用。後日談の全本文・自然再生タイミングを検証したという意味ではない。
- `check-i18n-statistics-reading-browser.mjs`：実際の展示26のデータから、5タブ全てをPC/スマホ×2言語で確認。実ツールチップの位置、スライダー見出しの重なり、全文の日本語混入、言語変更後のdataset/method ID維持。AI送信は行っていない。
- `check-i18n-core-browser.mjs`：未知文章の誤変換防止、属性・フォーム入力保持、原文復帰、キャンバスのネイティブ描画、全て合格。
- `check-i18n-story-browser.mjs` を再実行して4条件合格。本編・長文改ページ・保存/別言語読込に回帰なし。
- `npm run check`：初回は古い月次fixture不一致で失敗。年次データ確認を伴う改訂fixture追加後、全体 check / postcheck が exit 0。これはUI全体の翻訳合格ではなく、既存のデータ・数学・台本・構文等のチェック。
- `check-i18n-catalog.mjs` 現在5,740項目、本編377+後日談164本文、原文SHA不変。`git diff --check` 合格（既存のLF/CRLF警告のみ）。
- スクリーンショット目視済み：英中センサーガイドPCと英語スマホ、英語クレジットPCと最終画面スマホ、展示26英語チャート、白いプロローグスマホ。証跡は各 `artifacts/i18n/` サブディレクトリ。

### 辞書・インベントリの現在地

- 地図原本3,760件の固定IDは保持。`maps-pending.json` 現在334件。主に河川調査の固有地点名/原資料IDを含む複合ラベル（IDは改変しない）、少数の動的文言、および訂正前21/22の2件。原本の採集は翻訳合格試験ではない。
- 31〜71の水質・気象・大気・PRTR・生物・食料の主要指標説明と出典は、`ui-map-water-weather`、`ui-map-air-water-indicators`、`ui-map-prtr-biology`、`ui-map-food` に全文登録。実POI/年変更での追加採集・検証は残る。
- 統計原本 `statistics-untranslated.json` 1,020件、`statistics-pending.json` 688件。全26手法を実行した採集。展示26の報告画面と分布の初期部分は登録済み。次の辞書作業は元ID 280以降、残りの手法の解説・条件・計算表示。個別数値をハードコードせず明示的な表示テンプレート化。
- センサー原本 `sensor-untranslated.json` 348件を採集。全HTMLのhiddenパネル/属性も含むが、未ログイン空データfixture。実ログイン/接続/履歴/登録エラー等の全ランタイム文言は未採集。`inventory-i18n-page-browser.mjs` で追加採集可能。固有入力値、キー、画像素材は翻訳しない。
- 全画面完了の報告はまだ不可。後日談全本文・ログ/書き出し、センサー全設定/ガイド、conceptと各モードの詳細、全71展示の翻訳後の実操作・URL/クルージング回帰、最終キャッシュ識別子更新が残る。公開は新しい明示指示が必要。

## 追補：2026-09-13、最新の途中状態

- 最新追加画像のセンサー入口ガイドも、既存の全画面翻訳依頼の対象。3枚の画像はそのまま、見出し・本文・開始操作・再表示案内を英中化。上記 guides の8条件で実表示を確認済み。
- センサー静的原本348件の未訳は0件。`ui-sensor-platform` / `ui-sensor-measurements` を追加。参加、利用規約、接続・書込ガイド、計測69項目、登録、プロフィール、アカウント削除を対象とする。英中で計測項目を検索でき、コード/クリップボードの5種の接続依頼も全文翻訳。技術値・固定URL・個人入力は維持する。
- 明示 bind の値を再度DOM翻訳しない修正。観測点名・参加者名などの個人入力を維持。削除確認語は JA「削除」、EN「DELETE」、ZH「删除」だがAPI契約は従来の `DELETE_MY_ACCOUNT`。実削除は実行しない。
- `check-i18n-sensor-platform-browser.mjs` 最新40条件合格（PC/390px × en/zh-CN、各10画面/操作）。実ChromeのUI、Clipboard API、検索・入力保持・確認キャンセルを検証。センサーAPIはローカルfixtureであり実認証・サーバ登録・ハードウェア連携の検証ではない。スマホ英語の規約見出しが切れる問題を再現して折り返しを修正、前後画像を目視確認。
- センサー追加ランタイム原本245件は `sensor-runtime-original-sources.json` にsource順を復元保存（初回viewsメタデータは再採集時に失われた）。今後の再採集は `sensor-runtime-latest.json` のみ更新。現在この試験範囲の未訳は0件。
- `check-i18n-sound-glyphs-browser.mjs` 全12曲×英中日、36条件合格。選択中の言語切替、中央キャンバスの実描画ピクセル、日本語の手描きパス復帰を確認。簡体字フォントによる曲名を目視確認。音声出力/全自然アニメーション時間の検証ではない。
- 統計 `ui-statistics-inference` / `ui-statistics-categories` / `ui-statistics-models` を追加。原本1,020件の未訳0件。説明は全文・明示パラメーターで翻訳し、計算/原データは変更しない。
- `check-i18n-statistics-methods-browser.mjs` 104ケース合格（26手法×PC/390px×en/zh-CN、各5タブ＝520表示）。ネイティブ分析変更メニューで選択、表示/読み上げラベル、日本語残り、数値結果とdataset/method IDの言語間不変を確認。図と解説を撮影。実際の公開データスナップショットを使用、AI送信なし。
- `check-i18n-core-browser.mjs` 再合格。カタログ7,118件、541本文、原文SHA不変。カタログ件数は全利用経路の合格を意味しない。
- 次の残件：concept 151採集文言、各モード詳細、センサー履歴/AI等の追加状態、全後日談/ログ/書き出し、翻訳後71展示のPOI・時間変更、ナビ/直リンク/巡回回帰、キャッシュ識別子と最終全体check。ローカル実装を継続、公開は未実施・新規指示待ち。

### 2026-09-13 続き（上記から進んだ範囲）

- conceptページを追加対応。断片の機械的な並べ替えを避け、26個の文章ブロックは全文訳を表示し、日本語へ戻すと元のDOMノード・改行・強調を復元。画像ファイル/リンクは不変、alt説明・図の操作は翻訳。
- `check-i18n-concept-browser.mjs` PC/390px×英中の4条件合格。6セクション、図拡大/閉じる/フォーカス復帰、原文DOM復元、画像・リンクの不変、入口リンク後の言語保持。目視でスマホヘッダーの見切れを発見して折り返しを修正し、リンク座標の回帰を追加して再合格。
- 統計26手法の全画面と代表画像を再確認。スマホ英語の3補助タブのラベルが隣と近すぎたため、英中のみ自然な折り返しを許可。`check-i18n-statistics-reading-browser.mjs` PC/スマホ英中4条件を再実行して合格。
- ログ本文だけでなくMarkdownのタイトル、話者、区分、日時、見出し、操作通知、ネイティブ確認文を翻訳。入力コメント・技術ID/メタデータは維持。未訳の匿名話者4行、コピー通知、「全台本」見出しを実出力から発見し修正。
- `check-i18n-log-export-browser.mjs` 4条件合格。全544ステップの実ログDOM、英中Markdownを実ダウンロードして全本文・ID・BOM確認、実Clipboard API、JA復帰、ユーザーコメント保持。Windowsのclipboard CRLFは検査時のみ正規化。本篇の380ステップには本文なしの演出3件を含む。
- `check-i18n-true-end-full-browser.mjs` 4条件合格。通常のENDINGジャンプ→クレジット終了ボタン→後日談入口から、全164段落をネイティブクリックで完走。PC英171/中165ページ、390px英247/中176ページ。各段落の全ページ連結が訳文と一致、実枠内fit、途中言語切替、最終画面、pageerrorなし。reduced-motion試験であり音声・自然タイミングの保証ではない。
- キャラクター紹介も分割グリフを翻訳せず、全文翻訳→英語単語/グリフ化するよう修正。プロフィールは4人それぞれ全文訳。言語変更でも選択中の人物/表情は維持。`check-i18n-character-browser.mjs` PC/390px×英中×4人=16条件合格。画像内容は変更しない。目視確認は引き続き行う。
- 宇宙・GXの初回実表示とJSONから149文言、キャラクターグリフ等111文言を `secondary-modes-original.json` に採集。`ui-secondary-modes` で149文言を明示全文/表示テンプレート化。再評価で残る103件は旧キャラクター描画の単字であり、単字辞書は作らない。全文描画は上記16条件で確認。宇宙10・GX8の実操作後の追加採集はまだ途中。
- 全体 `npm run check` はexit 0。ただしモードローダーの単体モックに新しいルート依存がなく、非同期TypeErrorが出たまま成功判定になっていた。fixtureで実 `map-exhibit-route.js` を先に読み込むよう修正し、単体再実行でエラーなしの合格。後続の追加辞書/character修正後は最終checkを再実行する。
- 最新残件：宇宙/GX操作後の追加文言・表示確認、センサー履歴/AI/詳細等の追加状態、翻訳後71展示のPOI/時間変更（河川の原資料IDと固有地点表示の区別を含む）、ナビ/直リンク/巡回の回帰、キャッシュ識別子更新と最終check。公開未実施。

### 2026-09-13 続き：操作後の文言と実表示

- 宇宙10・GX8の動的説明、操作フィードバック、出典、数値テンプレートを `ui-space-runtime` / `ui-gx-runtime` に追加。宇宙のnull観測値が0と表示されていた症状を再現し、`space-mode.js` / `space-scenes.js` で欠測は「—」に修正。原データJSONは不変。
- `check-i18n-secondary-modes-browser.mjs` 72条件合格。実ポインタ操作でフィードバック変更を確認。宇宙はネイティブモードボタン、GXは公開setPhaseで準備して実ジェスチャー。スマホ宇宙の出典ボタン/説明は元のCSSで非表示のため、非表示要素を強制クリックして確認済みとはしない。PCの出典・スマホGXの出典は実画面で確認。パネル背景透過による背後の文字混入と小さすぎる文字を英中CSSで改善し再合格。
- キャラクター16条件は英語ハイフン語のまとまり修正後に再合格。宇宙/センサー出典の可読性CSSと関連スクリーンショットも再確認。
- `ui-sensor-detail-ai` / `ui-ai-runtime` を追加。センサー履歴、統計、AIエラー・送信先確認・状態、統計AIの6質問例を翻訳。質問例は言語変更に追従するが、自分で編集した質問は維持する。送信先/キー/個人名は文章置換しない。AIには設定言語で応答する指示を渡す。
- `check-i18n-sensor-detail-browser.mjs` 24チェック合格。PC/390px×英中で実詳細・履歴再取得・分析・キー未入力・送信先キャンセル・試験回答、削除のネイティブ確認キャンセル。API/AIは明示したローカルfixture。実APIキー、実認証、センサー機器、本番送信ではない。
- `check-i18n-statistics-ai-browser.mjs` 4条件合格。6質問例、未編集/編集後の言語変更、送信プレビューJSONと送信bodyの一致、送信先キャンセル/同意、試験回答、キー削除・終了・再表示で空を確認。外部AIサービスには送信していない。
- `bump-i18n-asset-versions.mjs` で変更したJS/CSSと親importの識別子を `i18n-20260913` に機械更新。新たな変更後も再確認する。`npm run check` は更新後にexit 0、以前のモードローダーfixtureのTypeErrorもなし。core browserも再合格。ただし本節末尾の後続修正後は最終checkを再実行する。
- 地図 `ui-map-runtime` を追加。実POI、年度の先頭/終点、データ欠測状態、凡例、AIヘッダー等の追加文言を全文/数値テンプレート化。地図ラベルは合成した文章の再翻訳ではなく、意味のあるフィールドを個別に翻訳する。
- 当初のruntimeテストは2〜5の実データ読込完了前に進み、6〜14にはgetCruisePoints APIがなく、POI件数0だった。これを「全71 POI合格」と扱わない。2〜5の実データ待ちを追加、年度変更の読込完了を待機。1〜14の実ポイント/タッチ・詳細カードは別 `check-i18n-map-details-browser.mjs` で補完している。
- 地点、原資料の物質/生物名、地点コードは推測で別名に改変しない。表示要素を明示的な原資料フィールドとして分離し、原表記であることを案内。地域/件数/欠測など周囲のUIは翻訳。ユーザーへの進捗でもこの区別を説明済み。ネイティブoptionは原名とコードだけを保持して別フィールドをbindし、言語変更・年変更でも選択IDは不変。統計タイトルにも表示用メタデータを追加し、原名を含むだけで周囲の説明まで未訳にならないよう修正中。
- ナビの英中PC/390pxは各2条件合格（メニュー/前後/71↔01/心拍アニメーション/画面内位置/reduced-motion）。英語cruiseは24展示×2画面=48条件合格。巡回時計のみ加速した試験であり全展示実時間放置ではない。日本語実時間5地点×5秒/終点3秒の既存証跡も維持。
- 直リンク英中試験はPC71切替まで通ったが、試験コードがabout:blankにlocalStorageを書きエラーを出していた。HTTP(S)文書だけで言語を設定するよう試験側を修正し、再実行中。未修正試験のエラーをアプリ合格に含めない。
- この時点の残件：最終スマホ71展示、1〜14の実カード/キャンバス文字、原資料の内訳ダイアログ/統計の追加確認、英中直リンクと中国語巡回の結果回収、影響範囲の最終check・キャッシュ・SHA一覧。全画面完了の報告はまだしない。コミット/プッシュ/デプロイは未実施、過去の公開依頼を今回の公開許可として扱わない。

## 2026-09-13：ユーザー指示による区切り

「キリねえからそろそろ終わろうぜ」を受け、追加の網羅調査は停止。上記の後続結果と残件を `docs/I18N_LOCAL_QA_20260913.md` に集約した。本書の途中状態を現在の残件一覧として扱わない。

- スマホ71展示×英中142条件、1〜14の可視カード・Canvas表示の不足分、原資料ダイアログ、英中直リンク・巡回は結果回収済み。統計26手法104条件と最新入口ガイド8条件も再合格。
- 最終 `npm run check` はexit 0（セッション28224）。カタログ7,744項目、541本文、原文SHA不変、キャッシュ更新の残り0件、diff whitespace check合格。
- 残件は展示10グラフの読み上げ説明1件、および年次分析の保存→再読込追加試験のタイムアウト。原因を推測して製品不良や合格と断定しない。
- 変更はローカル保存済み。未コミット・未プッシュ・未デプロイ。未確認の利用経路まで完了したとは報告しない。
