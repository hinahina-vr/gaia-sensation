# 展示17の背景を41に統一

2026-09-12 ローカル作業ツリー。未公開。

- パターン・色・速度は元から共通。17のseedを41と一致させ、陸地に雨を重ねないマスクも共通化。
- 海上限定の判定をテーマの `oceanOnly` にまとめ、17も41同様にマスク非対応のCSSフォールバックを抑制。
- 設定の一致を検査。Chrome 1440/390pxで17/41のWebGL実描画・時間変化を検査、17の陸地点サンプルの透明度0も確認。
- 試験：`scripts/check-map-theme-background-browser.mjs` の `QA_NUMBERS=17,41`、追加の `QA_NUMBERS=17`。合格。
- 証跡：`artifacts/rain17-match41`、`artifacts/rain17-land-mask`。保存データによるローカル試験。公開環境・実端末は未確認。
