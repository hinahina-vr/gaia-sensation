# 17・41 雨の向き

## 再修正：真上から真下

上向きの水面リボンが残っていたため、雨テーマから水面成分を削除。回転も除去し、雨を真下へ落とす。完全な雨シェーダー出力を比較し、0.3秒で横0px・下10pxの移動を確認。PC/スマホの17・41はartifacts/rain-downward-finalで再検査。以前の雨成分だけの検査では背景全体の方向を確認できていなかった。

緯度座標は上向きが正のため、雨のテクスチャ座標の時間オフセットを負から正へ変更。雨の見かけの移動が下向きになる。

check-rain-direction-browser.mjs: ローカルChromeの実WebGLで雨成分を分離して方向を比較（独立に動く水面成分だけ検査時に除外）。旧処理は0.3秒で上へ9px、新処理は下へ9pxの相関が最大。方向回帰テスト合格。

check-map-theme-background-browser.mjs: QA_NUMBERS=17,41、1440/390幅で実画面の描画と時間変化・切替を検査。証跡artifacts/rain-direction。外部API遮断、保存データ使用。実機・本番未検証、未公開。
