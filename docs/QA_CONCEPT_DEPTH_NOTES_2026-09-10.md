# コンセプト本文・原風景・奥付の追加校正

## 依頼への対応

2026-09-10の深層説明の差し替え以降、同じ作業中に追加された文言修正・削除を統合。基準コミット `888c26b623151205f6b8ecf9f77951dbc9a425ae` 上の未公開作業ツリー、ローカル版 `concept-20-notes-20260910`。既存の変更を保持し、コミット・push・デプロイは行っていない。

- 深層の導入を、指定された「すべてがデータ化され…時代。」「神話製作機械は、決定論的で予定調和なデータの輪廻の環から…」「確実な未来を消費するのではなく…」の3文へ差し替え。各文は別段落。語句ごとの折り返しを使い、スマホで「最適解」や語尾だけが分断されないようにした。
- 結びの「選んだ記憶は記録へと戻り…」「だが、過去の選択を、未来の義務にはしない。」の段落を削除。「人生まで『おすすめ順』に…」と最後の段落は保持。
- 奥付から「法学部・SIerを経て、事業会社で社内AI導入に携わっています。」を削除。「BMS」のみを削除して「映像制作と個人サイトづくり…」は保持。名前・所属・noteリンクも保持。
- フッターの `GAIA SENSEWARE` を、明示された綴りどおり `GANA SENSATION` に変更。別の表記であるヘッダー等の `GAIA SENSATION` は、この置換の対象としていない。
- 構想図の見出しを「神話製作機械の原風景（初期ノート）」へ変更。ページ内ナビの「構想図」を「原風景」へ変更し、リンク先 `#mechanism` は保持。
- 添付画像の数字は「神託が守る3つの原則」のCSSカウンターと特定し、01/02/03の生成と番号用の見出し余白を削除。3原則の見出し・本文、章番号やA/B/Cは変更していない。
- 図の下の「自己情報を複数のAIが解釈し…」「記録は次の応答の素材となり…」を削除。図・画像内の呼称・代替テキストは変更せず、金色と青紫の凡例を保持。
- 6項目を指定の平易な説明文へ全面差し替え。その後の追加指示を上書き反映し、完全自己情報の但し書きを削除、深層知性群を「完全自己情報をもとに…私（原型）の観察と更新を続けます。」へ変更、神格アーキテクチャを「考えうる可能性の極限までシミュレーションします。」で結ぶ形へ短縮した。
- 神託の冒頭「システムから返されるのは」と末尾の「過去のデータから未来が自動的に決まってしまう世界…」を削除し、「最後に意味を与える権利を本人の心に残す。」を「自己決定権を留保します。」へ置換。現在の全文は「『従うべき命令』ではなく…」に始まる2文。原文中の鉤括弧・空白は実ファイルと照合用データで保持。
- 体験Cの後半を「ただ日常を消費するのではなく、『何を調べ、どう生きるか』を思考していく。」へ差し替え。作品概要の太字は維持し、文末を「展示参加型ビジュアルノベルです。」へ変更。

文言の正本は `scripts/lib/concept-editorial-copy.mjs`。この記録中の要約・引用符表記ではなく、そこに保存した指定原文とDOMの `textContent` を全文照合した。主な実装変更は `concept/index.html` と `concept/concept.css`。静的契約、editorial、author-oracle、ページ試験の該当期待値を更新した。今回はJavaScript、画像、データ、ストーリー本編、保存・書き出し機能を変更していない。

## 最終版の検証

最終証跡: `artifacts/concept-depth-agency-20260910/final-combined/report.json`。インストール済みChrome、localhost:4492、本番CSPを適用した分離コンテキスト。モバイルは画面寸法とタッチのエミュレーションであり、実機・本番ではない。

- **1440×900、390×844、320×568、768×1024の4ケース合格**。深層3文、6項目全文、保持した結び2段落、作品概要、体験C、短縮した奥付、フッター名、原風景の見出しとナビ表示を照合。
- CSSの `::before` が番号を生成しないこと、リストマーカーなし、番号用余白なしを実ブラウザで確認。3原則の見出しも保持確認。番号削除前の比較画像は同じ証跡親ディレクトリの `final-all/`、削除後は `final-combined/`。
- 本文・見出しの実描画範囲が画面内に収まり、ページの横はみ出しがないことを確認。深層の語句と6項目の末尾は文字範囲の行数で確認し、語尾を1行に保つ回帰検査を残した。
- 学びの4講義と残した8段落、シラバスURL、注意書き、深層と先頭への移動、明暗ヘッダー、図を開く・拡大・Escape・フォーカス復帰も合格。noteリンクは実クリックと遷移要求URLまでを検証し、外部ページ読込は遮断した。
- PC・スマホの深層本文、原風景見出し、番号なしの3原則、6項目、奥付、体験C、作品概要の画像を確認。最終の6項目ではPCで「す。」だけが残る問題も解消。
- 最終版で `npm run check:concept`、`git diff --check` 合格。4ケースの報告ハッシュと最終作業ツリーの3ファイルが一致。
- `npm run check` 全体は6項目の最初の差し替えまでの版で合格。その後の個別の文章短縮・体験C・概要文末・ナビ変更は、上記の最終全文照合とブラウザ4ケースで影響範囲を再検証した。途中の各ディレクトリの合格を最終版の合格として混同しない。

最終ハッシュ:

- `concept/index.html`: `9fdc0a20d74a0f7447241488f0db4a5e7edb3b78c492efe9908cdcf0c88feb50`
- `concept/concept.css`: `c214c2e945554e83c0acbdf290e5ee75fc1d7056e13ae84328c048444a0d53df`
- `concept/concept.js`（未変更）: `4696cfa83134cef43cce1dcf47feb3eec05aeb00316d0927aae1bbc9c9eac78a`
- 構想図（未変更）: `8d1ccc1cbf148317f03c285dcc9399c29f4433c1e8ee2e10b31e064d33cc68b5`

## 未確認範囲・引き継ぎ

今回の指示はローカルで反映・確認済み。公開は未指示のため未反映。物理スマホ、Safari/Firefox、本番配信、実印刷は未確認。新しいZIPやPDFは作成していない。本編入口・印刷CSS・JavaScriptなし等の前回証跡は `QA_CONCEPT_EDITORIAL_2026-09-10.md` と `QA_CONCEPT_LEARNING_2026-09-10.md` を参照し、今回すべてを再実行したとは扱わない。

以前から保持している本編の短い横画面での顔と会話領域の重なりは、今回の静的コンセプト本文修正では解消しない未対応項目。次に本編レイアウトを扱う際に、その操作条件で再現・修正・回帰確認が必要。
