# 3枚の紹介画面：高さに収める・サイト共通フォント（2026-09-08）

## 対象と変更

基点 `b236255db4dc60f748aecfe4327d45d39f8ad33b`。提示された地図の初回紹介画面を対象に、PCの3列表示で画像・説明・API条件・操作ボタンまでスクロールなしで表示するよう調整した。

- `mode-feature-intro.css`：みず・あめの紹介画面をサイト共通の `--font-ja`（游明朝などの明朝体）へ統一。見出し・カード本文・番号・ボタンも同じフォントを使用する。
- PC幅901px以上は、画面高から上下の余白を引いてパネルの高さを決め、画像の表示領域を残りの高さに合わせる。文字・操作ボタンは必要な高さを確保。画像は `object-fit: contain` を維持し、人物を切らない。見出し・本文・余白も画面高に応じて調整する。
- `overflow: hidden` でスクロールバーや説明を隠す処理は追加していない。非常に低いウィンドウや大幅な文字拡大では内容へ到達できるよう、スクロールのフォールバックを残す。
- スマホ・幅900px以下の縦積み表示は、本文を読めるサイズに保ち、既存の縦スクロールを維持。これはPCの3列表示を対象とした縮小であり、全スマホ画面の長い紹介を無理に1画面へ押し込める変更ではない。
- `gaia-mode-loader.js`、`index.html`、`sensors/index.html`：共有CSSとローダーのキャッシュ識別子を更新。センサー側の紹介デザインや本番APIの処理は変更していない。
- 画像ファイル、文言、保存仕様、分析結果、外部送信先、権利判断に変更はない。今回は画像生成・画像の描き換えをしていない。

## 再現・回帰試験

ローカル `http://127.0.0.1:4447/?exhibit=1&live=1#world`、Windows Chrome。APIには保存データ・試験用応答を使用。

1. `node scripts/check-feature-fit-browser.mjs http://127.0.0.1:4447 --before`
   - 基点コミットのCSS・HTML・ローダーをブラウザーへ供給し、2560×1272で151px、1440×720で163pxの縦はみ出しを再現。
   - 旧コミットを固定しているため、修正後も再現可能。作業ツリーや履歴は書き戻していない。
2. `node scripts/check-feature-fit-browser.mjs http://127.0.0.1:4447`
   - PC 8条件：3840×1908、2560×1272、1920×954、1536×760、1440×720、1366×650、1366×600、1024×768。縦・横はみ出し0px、画像のdecode、全説明とAPI条件が表示範囲内、フォントのサイト共通値との一致、44px以上の操作領域を確認。
   - 開いたまま2560×1272から1440×720へリサイズしても収まることを確認。
   - スマホ相当390×844、320×568、横持ち844×390はフォント一致、画像と操作ボタンの表示、下部説明へのスクロール、開始動作を確認。PCと合わせ11ケース合格。
   - 再現・修正後のレポートとスクリーンショットは `artifacts/feature-fit/before/` と `artifacts/feature-fit/after/`。2560×1272と1440×720の実出力を開き、フォント・画像・説明・ボタン・スクロールバー不在を目視確認した。
3. `check-feature-intro-browser.mjs`（`GAIA_FEATURE_OUTPUT=artifacts/feature-fit/lifecycle`）
   - PC/スマホ6サイズで地図・センサーの初回表示、開始、閉じる、再表示、スポットライトガイド、フォーカス循環、保存済みガイド状態を確認。センサーのログイン・デバイス入口も含め14ケース合格、記録エラー0。
4. `check-feature-bright-browser.mjs`（`GAIA_FEATURE_BRIGHT_OUTPUT=artifacts/feature-fit/bright-regression`）
   - 1440/2560/3840の3ケース合格。旧暗色UIとの比較基点を `3a14ec5` へ固定し、今回の縮小後の文字サイズに合わせた検査へ更新。元の大きさ・文字拡大要求を永久に固定して今回の縮小要求と衝突させない。画像3枚、明るさ、80%の背景、切り抜きなし、操作性は引き続き検査する。

`package.json` に `check:feature-fit:browser` を追加。`npm run check`（precheck/postcheckを含む）、変更した試験の `node --check`、`git diff --check` は合格。最終レポートの対象4ランタイムファイルのSHA-256と現物の一致を確認した。

## 未確認・公開状態

ローカルChromeの画面サイズ・タッチ入力エミュレーションによる確認。物理スマホ、Safari、本番配信、実API送信、文字だけを大幅に拡大した全設定は未確認。書き出し機能・配布ZIPの変更はない。本変更はローカルのみで、コミット・プッシュ・デプロイはしていない。
