# 31〜64の文字・下部UIの統一

対象は `2a4f51385b69a0fda1ef285b0058acae7207640b` を基点とするローカル作業ツリー。先行する公害関連21展示と01〜05の高さ調整は保持。push・デプロイは行わない。

## 再現と変更

添付された31番の見た目をローカルで確認。31以降の展示名は14px固定、番号は24px固定で、共通の観測UIの書体指定からも漏れていた。3840px幅では30番の展示名28pxに対して半分の大きさだった。

- `map-chapter-navigation.css`: 31〜64の番号を26〜32px、展示名を21〜28pxにし、隣接する30番と同じ拡大率・ベースラインに揃えた。長い展示名は他の展示と同様に省略表示し、上部見出し・一覧で全文を確認できる。
- `map-observation-typography.css`: 年次展示の表示と凡例も共通の明朝体・標準ウェイトに統一。01〜30の既存ルールは変更していない。
- `marine-cod-exhibit.css`: 左から「展示」「地点選択」「観測値」「年度操作」「出典・分析」の5区画に整理。地点選択欄の左端を揃え、選択文字14〜18px、数値24〜34px、補助ラベル11〜15pxへ拡大。
- 観測値は「指標名→値→原資料の補足」を縦に並べる。PCの重複する地点名は隣の測定地点選択欄で確認できる構成にした。データ・選択肢・座標・分析内容は変更していない。
- PCの通常バーは01〜05と同じ104〜124pxの範囲。幅901〜1200pxでは、地点・年度操作を上段、展示・数値・出典／分析を下段とし、文字を小さくせず収める。
- スマホは既存の下部配置を維持し、選択文字を12→14px、補助文字を1px拡大。表示しないPC用の章番号・章名のサイズはスマホの操作に影響しない。
- `gaia-mode-loader.js` と `index.html` のCSSキャッシュ識別子を更新。

旧版の証跡: `artifacts/annual-dock-style-2026-09-09/before/`。幅3840・1440・390で30・31・44・64を比較した12ケース。

## 検証

`scripts/check-annual-dock-style-browser.mjs` は30番と実際の計算済み文字サイズ・書体・ウェイトを比較する。31〜64の数値は原JSONと照合し、地点選択欄、年度、前後移動、出典、分析などのクリック位置が画面とバーの範囲内にあることを検査する。通常PCでは5区画の順序と地点選択欄の左揃えも検査する。

最終ブラウザ検証は合格。証跡は `artifacts/annual-dock-style-2026-09-09/after/report.json` と同フォルダのPNG。

- 幅1440×900・390×844では31〜64の全34展示をそれぞれ確認（68ケース）。
- 3840×2088・1920×1080・1024×768・320×568・844×390では31・32・38・43・44・54・55・63・64を確認（45ケース）。
- 同じ7画面で30番を比較対象として確認。年次展示113ケース＋比較7ケース＝計120ケース、未処理のブラウザ例外0件。
- 13ケースで出典表示・分析画面・キーボード年度送り・自動送り開始／停止・次の展示を実際に操作。PCでは章名から展示一覧を開閉した。出典・分析から戻ってもシーンの横スクロールが0であることを確認。
- 通常PCは5区画の順序、地点選択欄の左端、文字サイズ・書体・ウェイトを検査。小型PC／スマホは全操作部の実際のクリック位置とバー内への収まりを確認。測定値は表示中の年度・地点の原JSONと一致。

`check-marine-cod-ui-browser.mjs` も幅1440・390で再実行し合格。全地点の表示範囲、出典・統計・年度操作、512倍までの拡大、密集地点の実クリック／タップ、ホイール、ドラッグ、2本指ピンチ、CODの実際の点の色、別展示へ戻った際の拡大制限を確認。証跡は `map-regression/report.json` とPNG。

最終版の画像も目視確認した。特に3840pxの31番の章名、1440pxの31・63の下部バー、1024pxの2段配置、390pxの31番、320pxの長いLASの指標名・地点名・微量数値を確認。

最終版のSHA-256（両ブラウザ試験の報告と現ファイルが一致、試験後の実装変更なし）:

- `marine-cod-exhibit.css`: `7bab15ff08c8a042033067fa26d45bc9b21e75eea95af7b071ec18c0ca8c1f82`
- `map-chapter-navigation.css`: `8ce636ec13737879f81dbf7738974648c0831f0d53da44438559cb97236d90ce`
- `map-observation-typography.css`: `3b760cbd1c6076cc74cea88e3bcd4f8e792a936b8141332eca236c6ea582480f`
- `gaia-mode-loader.js`: `6ce00a0440b9c84c68e2e21369cebf73b530f2ea96e3de83dbdd7eb3fabc0c5c`
- `index.html`: `1dc72b92393fe5fa30e257245e7d76e2cccb405a69af677e4aae5a3635b6f861`

静的確認: `node scripts/check-mode-loader-prefetch.mjs`、`node scripts/check-japan-pollution.mjs`、`node scripts/check-japan-sensor-open.mjs`、追加テストの構文確認、`git diff --check` は合格。

## 範囲と未確認事項

ローカルのインストール済みChromeで描画・マウス・タッチエミュレーションを確認。新規・既存の年次展示は同梱の原資料に由来するJSONをそのまま使用し、外部通信は遮断。実機スマートフォンや本番配信で確認したものではない。保存・書き出し機能の変更、外部AI、ESP32、ZIP配布は対象外。
