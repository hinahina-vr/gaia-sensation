# 展示巡回の非表示と展示再生の初期オン

PC・スマホの展示巡回ボタンを表示経路から除外し、巡回の自動開始を無効化。展示番号が変わると展示再生の要求をオンにする。地図の再入場時もオン。ガイド・読込・非表示時の一時停止と物語側の再生所有権は維持。再生データのない展示は従来どおり非対応と説明する。

`node scripts/check-map-default-play-browser.mjs` 合格。ローカルChrome 1440 / 390幅で21・15・31・38・70・71・06・01の初期再生と手動停止、巡回ボタン不在・巡回オフ、21の実際の期間進行を確認。前展示を停止してから次展示へ移っても再生がオンになる。PCの操作欄を目視確認。

証跡: artifacts/map-default-play-20260912/report.json とスクリーンショット。外部API遮断、FIRMSは保存JSONを使用。実機・本番・全展示の個別再生は未検証。未公開。

過去の「巡回が初期オン」を前提にした巡回テストは旧仕様。本変更の回帰テストは上記スクリプト。
