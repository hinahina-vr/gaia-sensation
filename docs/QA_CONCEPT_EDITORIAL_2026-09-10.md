# コンセプト本文校正・削除・奥付・語句の折り返し

## 依頼と対応

2026-09-10の公開URL `/concept/#depth` を対象にした本文差し替え依頼、および同じ作業中の追加指示をまとめて扱った。基準コミットは `888c26b623151205f6b8ecf9f77951dbc9a425ae`。先行する未公開変更を保持したローカル作業ツリーであり、コミット・push・デプロイは行っていない。

- ヘッダーを「放課後のある風景」「GAIA SENSATION CONCEPT BOOK」、作品見出しを「『惑星の放課後』とは」へ変更。作品概要、深層の問いと説明、世界観の6項目、結びを指定原文で差し替えた。全文の照合用データは `scripts/lib/concept-editorial-copy.mjs`。
- `SURFACE CONCEPT 01`、`02 DEEP CONCEPT`、`03 CORE CONCEPT`、`GIS MODE`／`STORY MODE`、「世界を、直視する」「出会い、変わる」へ変更。
- 「／ ローカル実装の画面」「この作品のもとになったアイデア。」「図で仕組みを見る」と下矢印、再度明示された「ここで見せないのは…」の段落を削除。削除指定に続けて全文が示された04章を丸ごと取り除き、05の番号見出しも取り除いた。世界観の構想図と6項目の説明は保持。
- その後の「殺風景」「01だと段落番号とかぶるので他のにして」に対応し、体験見出しの装飾を **A / B / C** とモード名で復元した。旧01/02/03の装飾は戻していない。
- 作者紹介の長文を本文から外し、末尾の奥付に2つの短い行へ要約。PCでは約2行、狭い画面では語句単位で自然に折り返す。名前、所属、仕事と創作の背景、noteリンクを残した。注意書きは内容を一字も変えず、3つの文と語句のまとまりで折り返す。
- 380px以下では長くなったヘッダー表記とナビを2段にし、文字を潰さずに配置。作者紹介の移動後も学びの章に空の枠を残さないよう再配置した。

変更の中心は `concept/index.html` と `concept/concept.css`。構想図の画像、画像内の旧呼称、図の拡大・スクロール処理は変更していない。現行の静的契約、ページ／作者・神託／キャラクター入口のブラウザ試験を更新し、`check:concept-editorial:browser` を追加。

## 検証結果

証跡: `artifacts/concept-editorial-20260910/`。インストール済みChrome、localhost:4492、本番と同じCSP、分離したブラウザを使用。物理スマホ・公開サイトでの検証とは区別する。

- `before/report.json`: PC 1440px、スマホ幅390pxで注意書きの「フィクション」が2行に分断される症状を、実描画の文字範囲から再現。変更前画像も保存。
- `final/report.json`: **最終A/B/C版8ケース合格**。1440×900、2274×1100、3840×2160、768×1024、390×844、320×568、280×653、844×390。指定文の全文、削除要素の不在、A/B/C、画像と見出しの交互配置、本文がその下に収まること、奥付、ヘッダー、本文・語句のはみ出し、ページ内リンクを実操作で確認。注意書きの語句は全件1行内で保持。エラー・HTTP 400以上・CSP違反なし。
- `author-reader-regression/report.json`: **最終版4ケース合格**。PC・390・320・768px幅。作者奥付、4講義とシラバスの保持、神託の3原則、図を開く・拡大・Escape・フォーカス復帰、明暗テーマ、先頭へ戻る、noteリンクの実クリックを確認。noteの外部読込は遮断しており、遷移要求URLの検証までである。
- `character-entry/report.json`: **最終版4ケース合格**。PC・390・320・844px幅。先行変更済みのキャラクター紹介からコンセプトへ実移動し、新しい作品見出しを確認。ブラウザで戻る、紹介を閉じる、4人の選択とCGも合格。
- `reader-regression/report.json`: **A/B/C復元直前の版で7ケース合格**。5画面幅の読書画面、フォント・コントラスト、画像デコード、図の通常／拡大・左右上下スクロール、キーボード・ダイアログ・背景クリック、印刷CSS、動き低減、JavaScriptなしの閲覧、本編入口を確認。最後の3ラベル復元後は影響する体験部の配置・折り返し・ページ長とアンカーを上記最終8ケースで再検証した。
- `npm-check-final.log`: `npm run check` 全体合格。`git diff --check` と最終3ファイルのSHA-256照合も合格。
- PCのA/B/C付き体験部、深層、世界観、奥付、スマホ／280px幅の注意書きを実画像で確認。PCの奥付は約2行で、スマホ幅では切れ目の悪い単語分割を起こさず折り返す。

最終対象ハッシュ:

- `concept/index.html`: `f25cb8d20f0af2a8312475146baaaa08b09bd45340b16eaf66f5401dd6c6ac7f`
- `concept/concept.css`: `de8e45598fd52f41fe85382dfdfb596d7de4b296da834de339078a096168279d`
- `concept/concept.js`（未変更）: `4696cfa83134cef43cce1dcf47feb3eec05aeb00316d0927aae1bbc9c9eac78a`

## 試験記録・未確認範囲

新規試験の初回は長距離のスムーズスクロールが完了する前に固定800msで位置を判定して停止した。実際に対象がヘッダー下へ到達するのを待つ検査に修正し、同じネイティブクリックで合格した（`after/` を保持、`after-v2/` はA/B/C復元前の8ケース）。公開URLは閲覧環境の制約で直接開けず、本番確認には数えていない。

依頼に従って、確認後に `http://127.0.0.1:4492/concept/#depth` をローカルChromeで開いた。ブラウザ操作で同URLが選択中のタブになり、新しいページタイトルが表示されていることも確認した。物理スマホ・Safari/Firefox・本番は未確認。新規ZIPやPDFは作成していない。印刷はブラウザの印刷CSSの検証であり、実プリンター出力の検証ではない。既知の本編通常会話の短い横画面での顔と会話領域の重なりは今回のコンセプトページ外にある未対応項目として保持。
