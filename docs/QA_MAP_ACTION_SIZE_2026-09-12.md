# MAPの出典・統計分析ボタンを06基準で固定

依頼原文: 「このボタンはすべて06と同じサイズで固定して　バラバラやねん」

解釈: 同じ画面サイズなら、展示を切り替えても「データの出典」「統計分析」の幅・高さ・内部の文字とアイコン配置が06と一致すること。スマホの既存の操作パネルは維持する。

対象は `9fbd901b68faebaf6148db6e18cdbcb50fa1c778` 上のローカル候補。既存の未公開変更は保持。コミット・push・デプロイ・ZIP配布は行っていない。

## 再現と修正

変更前の実Chromeで、1440×900の06は142.625×67pxだったのに対し、15は高さ44.844px、31は71px、65は83px、70は110×44pxだった。3840×2160では06が182×91px、70が180×44px。火災などのボタンには測定値用の大きい行ボックスが適用され、狭いPC画面の06はラベル末尾が省略されていた。

- `map-unified-dock.css`: 06の実寸を共通変数にし、両系統のボタンの幅・最小幅・最大幅・高さ・最小高さ・最大高さを固定。展示側のドック高さに連動させない。低いドックにも必要な高さを確保。アイコン、書体、行間、字間、内部余白を統一。
- 901〜1400px幅では06と同じ100×58pxを維持し、内部のアイコンと余白を調整して「データの出典」を省略せず表示。それより広い画面は従来の06の画面幅・高さに応じた寸法を維持。
- `realtime-exhibits.css` / `map-observation-typography.css`: 測定値の行高・書体指定が共通アクションへ流入しないよう除外。測定値自体の書体は変更しない。
- `food-exhibits.css`: 独自の240/380pxアクション列を共通幅に置換。
- `gaia-mode-loader.js` / `index.html`: 変更したCSS・JSの読込キーを更新。
- `scripts/check-map-action-size-browser.mjs` / `package.json`: 再現と画面別回帰テストを追加。
- 物語内のMAP小窓は専用の144×66pxを維持し、通常MAP用の新指定から除外。`scripts/check-story-map-dock-browser.mjs` にボタン限定・画面限定・変更前CSS比較の検査オプションを追加。既存の年目盛り読み取りも「年」接尾辞に対応。

## 切替確認で発見した表示残り

ボタン変更前の `before/1440-70-page.png` でも、「街と森」の比較パネルが食料展示の背後に残っていた。12を経由して海洋・記録・食料展示へ移ると、内部の基底モードを維持したまま別レンダラーへ切り替わるため、従来の除外判定に含まれない海洋・食料で古いパネルが再表示されていた。

`ecologies-exhibit.js` の表示判定と切替監視に海洋・食料のクラスを加えた。今回再現した経路の修正であり、以前報告された12→13の複雑な連続操作すべてについて同じ原因と断定するものではない。

## 検証

合格した確認:

- `npm run check:map-action-size:browser`: 3840×2160で全71展示。1440×900、1920×1080、1400×900、1280×900、1024×768、901×768では各14展示を確認。計155画面条件・310ボタンで、06との幅・高さの差は0px。文字・アイコンの相対位置／書体／行高一致、文字の欠けなし、画面内への収まり、中央のヒット判定、横スクロールなしを確認。
- 寸法は1440×900で142.625×67px、1920×1080で182×67px、3840×2160で182×91px。901〜1400px幅では100×58px。
- 出典の開閉、06のEnter操作、06・21・31・70の実グラフ表示とMAPへの復帰。31は地域・測定地点、70は日本を実際の選択欄で指定。01・15では分析対象外の説明と起動抑止を維持。
- 390×844と900×768では各06・15・21・70を確認。デスクトップボタンは非表示のまま、操作パネル経由の出典・分析、対象外の無効状態を確認。PC分と合わせて14件の入口操作テスト。
- 12の比較パネルは12でのみ表示。12を経由して海洋・記録・食料へ切り替えても、旧パネルが残らないことを同じ画面走査で確認。
- 物語内小窓を新指定から除外した最終調整後、1440×900・3840×2160で各9展示（06・12・01・15・21・31・65・70・71）と入口6件を再確認。`final-scope/report.json` は全対象ファイルのSHA-256が最終ファイルと一致。全71走査の途中に行った変更はこの物語除外のみで、MAPの寸法・書体・挙動は変更していない。
- `check-story-map-dock-browser.mjs ... --focused --actions-only`: 実際の物語途中の保存状態から起動した2176×1072・901×768で、専用144×66px、ボタンの文字欠けなし、地図小窓内の位置、出典・分析の開閉、物語への復帰が合格。
- 既存 `check-map-unified-dock-browser.mjs` の最終スモーク12条件（1440・390・844幅、21・06・16・20）が合格。これは物語除外前のMAP用指定に対する結果で、調整後の影響箇所は上記最終スコープ試験で再確認した。
- `check-live-timeline.mjs`、`check-mode-loader-prefetch.mjs`、変更JSの構文確認、`git diff --check` が合格。初期読込予算は996,304 / 1,000,000 bytes。

MAPのブラウザ検査はCSP違反・ページ例外・404なし。新しいMAPマトリクスはモーション抑制設定で実施。物語小窓検査は通常モーション。

物語ドック全体の既存テストでは、年送り側コンテナの内部スクロール幅が2px大きく、旧来の1px以内という判定に不合格。ボタンは幅144px・内部はみ出し0px。今回追加したCSSブロックを配信時だけ除いた比較でも同じ2pxを再現しており、今回のボタン変更前からある状態として記録する。全ドック合格とは扱わず、ボタン限定の実操作検査のみ合格。証跡は `story/full-dock-failure.json` と `story-before-action-style/report.json`。この微小な年送り側のはみ出しは未修正。

証跡: `artifacts/map-action-size-20260912/before/report.json`、`after/report.json`、`final-scope/report.json`、`story/report.json` と、各画面幅・展示番号のボタン切抜き／全画面PNG。最終スコープレポートには対象CSS・JS・HTMLのSHA-256を記録。

範囲はローカル配信した実アプリ、インストール済みChrome、本番CSP、同梱の保存データ。外部HTTPSを遮断し、火災APIはリポジトリの保存スナップショットを返す。スマホはタッチ・画面サイズのエミュレーションで、実機・本番・外部配信元への接続確認ではない。

保存・読込・書き出し処理や統計計算は変更していない。今回の関連操作は、出典を開閉し、選択地点／国の統計グラフを表示し、MAPへ戻るところまで実操作で確認。保存・出力の既存証跡は `QA_STATISTICS_OBSERVATORY_2026-09-12.md` を参照し、今回すべて再実行したとは扱わない。
