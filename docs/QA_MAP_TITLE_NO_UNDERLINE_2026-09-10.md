# 展示見出しの下線撤去 — 2026-09-10

## 依頼と対応

ユーザー原文: 「このシリーズ下線出さないで」。添付画像は展示64「ノニルフェノール・微量を追う」の見出し。

同じ見出し部品の共通CSSで、ホバー時・展示一覧展開中に付く下線を撤去した。通常時を含め `text-decoration: none` とし、クリック動作とキーボードのフォーカス枠は維持。出典リンクや前後移動ボタンのスタイルは変更していない。

今回の変更: `map-chapter-navigation.css`、そのキャッシュキーを持つ `gaia-mode-loader.js` / `index.html`、回帰テスト `scripts/check-map-title-no-underline-browser.mjs`、`package.json` の実行コマンド。

## 対象版と再現

- ローカル作業ツリー、親HEAD `cdc82c8ffd4fee607bd2d838ec3157b7095952d3`。以前の未コミット変更は維持。
- `http://127.0.0.1:4492/?exhibit=64#world` で案内から展示開始後、実ブラウザのホバーと Enter による一覧展開で修正前の下線を再現した。
- 修正前: 1440×900、展示64の1ケース合格（期待値 `underline`）。修正後: 同じ操作で `none`。
- 証跡: `artifacts/map-title-no-underline-2026-09-10/{before,after}/report.json`。両版のSHA256を記録済み。

## 修正後の検証

`npm run check:title-no-underline:browser` 相当のスクリプト実行で24ケース合格。

- 1440×900: 展示64、31、38、44、55、65、66、69、70、71、01〜05の15ケース。
- 3840×2160: 展示64、66、71の3ケース。
- 各PCケースでホバー／一覧展開時の下線なし、キーボードフォーカスの1px枠、Enterで一覧が開くこと、Escapeで閉じることを確認。
- 390×844・320×568: 各幅で展示64、66、71の計6ケース。上部タイトルと一覧内見出しの下線なし、ネイティブタップによる一覧開閉、現在展示の地理区分が初期選択されることを確認。
- 修正前後の1440幅見出し、修正後3840幅見出し、390／320幅の画面スクリーンショットを目視確認。
- JavaScript構文確認、`git -c core.safecrlf=false diff --check` 合格。ページ例外・CSP違反なし。
- 最終ソースと修正後レポートのSHA256一致を確認。

## 環境と未確認範囲

インストール済みChromeを使用し、ローカルの実データと本番CSPを適用。NOAA／FIRMSは保存済み応答、USGSは空応答に固定。画面幅とタッチはエミュレーションで、物理端末・実配信データの確認ではない。

表示CSSのみの変更で、保存・読込・書き出し処理は変更していないため、その経路は今回再試験していない。配布ZIPの作成なし。コミット・push・本番公開は未実施で、今回の公開指示なし。
