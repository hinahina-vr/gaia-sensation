# 海洋COD展示・入口UIの公開候補

2026-09-08、製作者の「コミット・プッシュ・デプロイ」に基づく公開。基点は `fc510bf5a84517788e8227e3757634cab5f9e171`。同じ作業ツリーで依頼され、ローカル検証を終えた次の変更を一つのコミットにまとめる。

- 展示31「沿岸に残る、水の記録」: 環境省の2020〜2024年度・海域CODデータ、地点と年度の選択、出典、実測年度値の統計表示。
- スマホのデータ入口で人物の顔に文字が重ならない配置。
- FIRMS観測パネルの80%背景に二重の塗りを重ねない修正。
- タイトルの開始ボタン下中央に「このサイトについて」を配置。

個別の実装・検証記録は `MARINE_COD_EXHIBIT_2026-09-08.md`、`INTRO_FACE_CLEARANCE_2026-09-08.md`、`FIRMS_SURFACE_OPACITY_2026-09-08.md`、`TITLE_ABOUT_BELOW_2026-09-08.md`。各文書の「未公開」は実装完了時点の記録として保持する。

## 公開前チェック

`npm run build:release` 内の権利台帳・依存関係・全体静的検証（`npm run check` のprecheck/postcheckを含む）・数理／既存データ・CSP・秘密情報の限定形式チェックが合格。最後のWorker生成はローカル実行環境の権限制限で止まったため、同じ固定Wrangler 4.121.0のビルドを必要な権限で再実行し、991,209バイトを生成。Worker内容に差分なし。データベース・秘密情報・配信設定に変更なし。

追加の `check:marine-cod`、`check:statistics-data-insights`、原CSVに対する10,186地点年度の独立照合、保存ZIPからのJSON再生成照合、差分検査も合格。直前の画面試験ではPC・スマホ・小型・横画面と、CODの実測値／年度グラフ／出典、タイトルリンクの実操作を確認した。最終ファイルのハッシュは、後続の影響範囲再試験を含めて確認した。

保存原資料と新規CODデータの出典・加工方法・利用条件は `data/sources/japan-marine-cod/README.md` に記録。過去のデータの公開判断を新規資料の許可として流用していない。

## 配信と最終確認

コミットから新しいディレクトリへGit blobを展開し、配信対象をコミットと照合する。新規展開した版のローカル画面を確認してから、既存の `gaia-senseware` Pagesの本番 `main` へ直接配信する。公開後は本番URLのファイルハッシュとPC・スマホ相当Chromeで実際の表示・操作を確認する。

公開ID、コミットSHA、リモートブランチ、最終スモーク結果はGit管理外の `artifacts/release-coastal-about-20260908/` に保存する。この公開前文書を本番検証済みという意味では扱わない。

## 既知の制限

前版の全体ContestブラウザーCIには旧UIへの参照が残り、別の既知の動作軽減切替アニメーション試験も未解決。今回の変更箇所の専用回帰試験とは分けて扱い、全体CI合格とは表現しない。物理端末、Safari/Firefox、センサー／AIへの実送信、物語全編の保存・読込は今回の公開検証対象外。配布用ZIPや新しい書き出しUIは作成していない。
