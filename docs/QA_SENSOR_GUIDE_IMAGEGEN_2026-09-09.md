# センサー紹介イラスト・操作案内：ローカル検証記録

## 対象

- 2026-09-09。ベースコミット `aaa9153116c704f14d82f48969a396b907388451` に本依頼を反映したローカル作業ツリー。既存の未コミット変更は保持。
- センサー紹介・操作ガイド `v3`、新しいイラスト3枚 `v1`。キャッシュ識別子 `sensor-20260909` / `guide-clearance-20260909`。コミット・push・本番デプロイは未実施。
- 制作方式・保存先・参照画像・全プロンプトは [ImageGen制作記録](SENSOR_GUIDE_IMAGEGEN_2026-09-09.md)。配信用WebPは1536×1024、3枚計901,758 bytes。PNG原稿も作業ツリーの成果物ディレクトリへ保存。

## 変更

- `sensors/sensor-platform.js`：既存の世界地図紹介と同じ明色イラストカードを使用。公開観測・自分の機器・分析の3場面を、みずとあめが紹介する。
- 操作案内は現在の画面に留まる。公開地図は拡大・移動、検索、観測点、更新、分析・AI質問、参加、接続ガイドの最大7段階。ログイン／マイセンサーは該当する3段階。表示されていない操作対象は既存の共通ガイド機構で除外する。
- `mode-feature-intro.css`：センサーの2枚目も暖色にし、見出しの折り返しを調整。`sensors/sensor-map-ui.css`：ヘッダーに再表示・BGMボタン両方の余白を確保。
- 参照更新：`sensors/index.html`、`gaia-mode-loader.js`、`index.html`。画像をメディア台帳の生成処理へ登録し、台帳304件を更新。
- 回帰テストを追加し、既存テストの「ガイドを既読にする」設定をv3へ更新。初回紹介を確認するテストだけは旧v2の既読状態を使い、更新後の初回表示を確認する。

## 再現と修正

- 変更前の1440×900／390×844で、暗いSVG紹介画面と、公開地図から操作ガイドを開くとログインへ移る動作を実際に再現。`artifacts/sensor-feature-imagegen-2026-09-09/tour-before/` に2ケースのレポートと画面を保存。
- 変更後は同じ操作で公開地図に留まり、実際の地図操作を案内することを確認。
- 小画面の全手順試験で、案内カードが操作対象に重なる問題を検出。拡大ボタン／ログインボタン単位の強調と短い説明へ調整し、320×568でも対象との重なりがないことを回帰テストで確認。
- 1440×900で、再表示ボタンが「接続ガイド」リンクを塞いでいた問題を実クリックで検出。ヘッダー余白を修正し、リンクから接続ガイドへ移動できることを確認。強制クリックは使用していない。
- 初期の既存4447サーバー試験では、以前の模擬ログイン状態が残り、ログイン画面への着地判定が失敗した。専用4458／4459サーバーへ分離し、模擬セッションをリセットした最終試験では合格。実認証・本番の不具合と混同しない。

## 最終合格試験

ローカルChromeと画面サイズ／タッチのエミュレーション。ネットワークはローカルのセンサーQA fixture、地図の保存データを使用。ブラウザーの例外は0件。

| 試験 | ケース | 確認内容 |
| --- | ---: | --- |
| `check-sensor-feature-browser.mjs` | 15 | 1440×900、390×844、320×568、844×390、3840×2160の各公開地図・ログイン・マイセンサー。全手順、戻る、完了、Escape、再表示、フォーカス復帰、44px操作領域、対象との非重複、画面内配置。 |
| `check-feature-intro-browser.mjs` | 14 | 地図／センサー各6画面サイズとセンサーのログイン・マイセンサー着地。トップからの実クリック、3画像のデコード、スクロール、固定フッター、フォーカス循環、開始、任意ガイド、同一タブの再読み込み後の既読保持。 |
| `check-sensor-guide-browser.mjs` | 4 | 1440、1024、390、320px幅。接続案内のアンカー、計測項目一覧・検索、展開状態、取得エラー、実クリップボードへのコピーと拒否時の代替動作。 |
| `check-sensor-onboarding-browser.mjs` | 4 | 同4幅。模擬登録、1〜16項目、下書き、ペアリング案内、期限切れ・再試行、ポーリングの成功／失敗、詳細編集・保存、接続ガイドとの往復、重複登録・削除なし。 |

- 計37ケース合格。機器登録・保存の確認はローカルfixtureに対するもので、物理ESP32や実アカウントへの登録ではない。
- 公開地図の1440／390px幅では、案内後に拡大・表示リセット、検索の該当なし／解除、観測点選択、最新値24.7の表示、APIキーなしの数値分析、更新、接続ガイドへの移動と復帰を実操作で確認。ガイド操作によるAPIへの登録・公開等の書き込みは0件。
- 画像全体、スマホ縦表示、320pxの末尾説明と固定ボタン、横向きの分析案内、4Kの3枚表示をスクリーンショットで目視確認。顔や手を切り抜かず、文字説明はHTMLで表示。
- 最後の紹介見出しの短縮後に、紹介ライフサイクル・全ガイド・登録フローを再実行して合格。接続案内本文・動作には影響なし。
- JavaScript構文、メディア台帳304件、ブラウザーセキュリティポリシー、アプリ内容、モード先読み、地図デモ、`git diff --check` に合格。

## 証跡と再実行

証跡ルート：`artifacts/sensor-feature-imagegen-2026-09-09/`。

- `tour-after/report.json`：15ケースと対象ファイル・3画像のSHA-256、全段階のスクリーンショット。
- `lifecycle-after/report.json`：14ケース、世界地図／センサーの画像・共通ファイルを含むSHA-256。
- `onboarding/regression-report.json`：4ケースと機器登録関連ファイルのSHA-256。
- `connection-guide/report.json`：4ケース。接続案内の画面とクリップボード試験。
- 上記3つのSHA-256付きレポートを検証後の現ファイルと再照合し、全件一致を確認。

専用のローカルQAサーバーを `node scripts/serve-sensor-platform-qa.mjs 4458` で起動し、別ターミナルで `npm run check:sensor-feature:browser -- http://127.0.0.1:4458` を実行できる。初回の再現試験 `--before` は変更前のツリー専用であり、完成版に対する合格条件ではない。

## 未確認

物理スマートフォン、Safari等の別ブラウザー、物理ESP32への書き込み・計測・接続、実認証、ライブプロバイダーへのAI質問、本番配信は未確認。本変更にはZIP等の配布物はない。これらを実機・実配信確認済みとは扱わない。
