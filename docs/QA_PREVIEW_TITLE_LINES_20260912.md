# 地点プレビューの区切り改行

対象は現在のローカル作業ツリー。未公開。
共通renderPoiPreviewTitleで半角・全角スラッシュを改行に変換し、各行の地名は折り返さない。PCのプレビューを内容幅に合わせ、スマホは一列配置で地名の幅を確保。

`node scripts/check-preview-title-lines-browser.mjs` 合格。
Chrome 1440×900 / 390×900で、若狭湾東部海域 / 立石岬地先、北海道 / 札幌、緯度 / 経度の3例を確認。2行への分割、行のはみ出しなし、画面幅以内を検査し、若狭湾のスクリーンショットを目視確認。

実ページ上で共通レンダラーに検証用データを渡す表示テスト。実地点へのホバー操作や全地点名の網羅試験ではない。外部HTTPSは遮断。証跡はartifacts/preview-title-lines/。
