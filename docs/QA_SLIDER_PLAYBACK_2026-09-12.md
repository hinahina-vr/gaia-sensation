# スライダー再生・09国別比較

09は国別選択スライダーがある一方、時間系列でないため再生非対応かつアニメーション処理から除外されていた。再生を国順の自動選択として実装し、4秒ごとに国・スライダー・数値を同じ選択状態から更新する。手動停止・シークでは停止し、再開時は選択国から進める。国の順序を時間の変化とは説明しない。再生状態の「手動選択」固定表示も修正。

check-map-default-play-browser.mjs に可視スライダーの実値変化の検査を追加。PC1440/スマホ390幅、21・09・15・31・38・70・71・06・01で再生開始とスライダー進行、手動停止を確認。09は停止後4.5秒の位置保持も専用再実行で確認。

ローカルChrome、外部API遮断、FIRMS保存JSON使用。本番・実機・全71展示個別検証ではない。結果はartifacts/map-default-play-20260912。未公開。
