# 「世界を観測する」背景を80%に変更

ユーザー原文：「透明度80%にして」。添付の「GAIA / OBSERVATION — 世界を観測する」画面を対象に、作業開始時に「背景の不透明度80%（20%透過）、文字とボタンは維持」と解釈を明示して実装した。

## 変更

- `mode-feature-intro.css`：`data-mode="map"` かつ `data-phase="title"` の背景色だけを `rgba(10, 20, 30, .8)` に変更する規則を追加。従来は `#0a141ef5`（約96%）。
- `gaia-mode-loader.js`、`index.html`、`sensors/index.html`：共有CSSとローダーのキャッシュキーを更新。
- 文字・ボタン・タイトル要素自体の不透明度、色、配置、12pxの背景ぼかし、遷移処理は変更なし。後続の見どころ画面やセンサー画面には新しい規則が適用されない。

対象版はHEAD `9fbd901b68faebaf6148db6e18cdbcb50fa1c778` 上の未公開作業ツリー。既存の未コミット変更を保持。コミット・push・デプロイはしていない。

## 検証：PASS

`node scripts/check-map-title-opacity-browser.mjs`

- 1440×900、添付と同寸の3573×1621、390×844、844×390、320×568（動きを減らす設定）の5条件。
- 実際に `/#world` からタイトルを表示し、背景alphaが0.8、タイトル・文字・ボタン自体のopacityが1であることを確認。色・ぼかし・ボタンの到達性・画面内配置・横はみ出しなしも確認。
- 自動遷移とタッチでの「見どころへ」を実操作。後続パネルの背景不透明度0.8と外側背景約0.612が未変更であること、閉じて地図へ進めることを確認。
- 計10記録、ページ例外・404・CSP違反なし。PC・添付同寸・スマホの実描画を目視確認。
- JS/MJS構文、`check-mode-loader-prefetch`、`check-contest-entry-budget`（996,085 bytes / 1MB）、`git diff --check`：PASS。

証跡は `artifacts/map-title-opacity-20260912/before/report.json` と `after/report.json`。変更前後の実画面画像と実装5ファイルのSHA-256を保存し、最終作業ツリーとの一致を確認した。

インストール済みChromeに本番CSPを適用したローカル試験。外部APIは遮断またはリポジトリのスナップショットを使用。画面サイズ・タッチはエミュレーションであり、実機・他ブラウザー・本番公開後の検証ではない。

保存形式・書き出し・配布物は変更なし。新規ページ読込で反映を確認し、ZIPは作成していない。先行するスライダー等の対象画像待ちは、本画面だけの変更とは区別して保持する。
