# 楽曲解説 TRACK 02〜12 の変更

## 依頼と反映

ユーザー原文：「音楽の解説を変更したい」に続くTRACK 02〜12の指定文章を、句読点を含めそのまま採用。対応表は `scripts/sound-description-fixture.mjs` に保持。

- `sound-mode.js`：11曲の `description` のみ変更。TRACK 01の解説、全12曲の曲名・曲順・ジャケット・音源・再生処理は維持。
- `gaia-mode-loader.js` / `index.html`：変更した解説が再読込されるよう、`copy-20260912` / `sound-copy-20260912` のキャッシュキーを追加。その他の行は変更していない。
- 専用ブラウザー試験を追加。従来の楽曲切替試験も、新しい全文の期待値を参照するよう更新。

変更前の3ファイルは `artifacts/sound-descriptions-20260912/before/` に保存。変更前後を正規化して照合し、`sound-mode.js` は解説11件以外が一致することを確認した。既存の未コミット変更は保持。

対象版はHEAD `9fbd901b68faebaf6148db6e18cdbcb50fa1c778` 上の未公開作業ツリー。コミット・push・デプロイはしていない。

## 検証結果：PASS

`node scripts/check-sound-descriptions-browser.mjs`

- Chromeの1440×900、390×844、320×568。実際の曲ボタンをクリック／タップし、各条件で全12曲を確認（変更11曲＋未変更TRACK 01、計36件）。
- 表示される解説が指定全文と一致し、曲名・番号・曲順が維持されることを検査。ジャケットの実デコード、楽曲の再生状態・再生時刻の進行も確認。
- 解説全文の文字矩形が表示枠内に収まり、再生操作部と重ならず、切替時に表示枠の高さが動かないことを検査。PC／スマホのスクリーンショットを主担当が目視確認。
- 各条件で消音／再開、ページ再読込→TRACK 02の再選択を実操作し、新しい解説の再表示を確認。全39チェック、ページ例外・404・CSP違反なし。
- `node --check`：変更した実行JSとテストJS/MJS。`check-app-content`（9モード・15カタログ）、`check-mode-loader-prefetch`、`check-contest-entry-budget`（990,635 bytes / 1MB）、`git diff --check`：PASS。

正本は `artifacts/sound-descriptions-20260912/after/report.json`。実装5ファイルのSHA-256を記録し、完了前に作業ツリーとの一致を再確認した。全36曲表示の画像も同フォルダに保存。従来の楽曲切替試験は期待値更新と構文確認のみで、今回は専用の実操作試験を正本とする。

## 確認の境界

ローカルのインストール済みChromeに本番CSPを適用し、外部APIを遮断。端末サイズ・タッチ操作はエミュレーションで、実機・Safari/Firefox・本番表示・主観的な試聴ではない。

保存形式・台本の保存／読込／書き出し・音源ファイル・配布形式は変更していないため再試験対象にしていない。ページの再読込と再選択は今回実施。ZIPは作成していない。スライダー等の先行依頼は対象画像待ちの状態を継続。
