import fs from 'node:fs';
import assert from 'node:assert/strict';
import {createHash} from 'node:crypto';
import {execFileSync} from 'node:child_process';
const out='artifacts/responsive-fixes-20260913';
const read=p=>JSON.parse(fs.readFileSync(`${out}/${p}`,'utf8'));
const profiles=fs.readdirSync(`${out}/final`).filter(f=>f.endsWith('.json')).map(f=>read(`final/${f}`));
const regression=read('regression/results.json'),matrix=read('map-matrix/results.json');
assert.equal(profiles.length,18);assert(profiles.every(r=>r.states.length===21&&!r.failure&&r.actions.every(a=>a.passed)&&r.errors.length===0));
assert(regression.length>=25&&regression.every(r=>r.passed));assert.equal(matrix.results.filter(r=>r.cruiseAndResize).length,6);assert(!matrix.results.some(r=>r.failure));assert.equal(matrix.errors.length,0);
const files=['index.html','gaia-mode-loader.js','map-responsive-layout.js','responsive-audit-fixes.css','map-mobile-shell.js','statistics-lab.js','character-mode.js'];
const manifest={base:execFileSync('git',['rev-parse','HEAD'],{encoding:'utf8'}).trim(),created:new Date().toISOString(),browser:'Installed Google Chrome, headless Playwright viewport/touch emulation, DPR 1, reduced motion',files:Object.fromEntries(files.map(f=>[f,createHash('sha256').update(fs.readFileSync(f)).digest('hex')]))};
fs.writeFileSync(`${out}/manifest.json`,JSON.stringify(manifest,null,2));
const issues=[
 ['R01','高','ズーム操作が凡例・観測パネルに隠れる','ズームを左側の専用位置へ移動。見出しの下端を避け、translate の残存オフセットも除去。320×568 / 375×667 / 844×390 / 1024×768 / 1366×768 のクリックを確認。','final/small-phone-map40.png','regression/R01-zoom-320-40.png'],
 ['R02','高','英語版・食料展示の出典／分析ボタンが画面外','901〜1399px の食料展示を2段・161pxのドックに統一。1366×768 英語で出典・分析ボタンの画面内表示とヒット領域を確認。','final/laptop-en-map70.png','regression/R02-food-en.png'],
 ['R03','中','凡例が展示見出し・矢印に重なる','901〜1199px の見出しと凡例を分離。凡例2枚も上下に分離。モバイルは凡例を「読み方・凡例」に集約し、観測パネルと重複する地点ツールチップを省略。','final/boundary901-map15.png','final/boundary901-map15.png'],
 ['R04','中','人物紹介の説明・立ち絵・台詞が重なる','PCは専用グリッド、タブレットは2列、スマホは縦積み。引用と肖像を同じ画像領域に収め、人物選択も折り返し可能に。4解像度の人物切替とスクロールを確認。','character-scrolled-1024.png','regression/R04-character-1024.png'],
 ['R05','中','低い画面で地図を見る領域がほぼない','幅900px以下かつ高さ700px以下は観測データを開閉式へ。元のスライダーとイベントを維持。表示サイズを戻すと元のドックへ復帰。','final/phone-landscape-map40.png','final/phone-landscape-map40.png'],
 ['R06','中','分析画面のグラフ表示領域が小さい','小画面はヘッダーのみ固定し、本文を一体スクロール化。グラフを説明より前へ移動し、高さ300pxを確保。タブ切替・閉じる操作も確認。','final/small-phone-analysis.png','regression/R06-chart-320.png'],
 ['R07','中','画面遷移後も分析ダイアログが残る','ハッシュ遷移で分析を閉じ、旧画面へのフォーカス復帰を抑止。人物紹介から戻る際も古い操作禁止状態で新画面を上書きしない。人物・音楽・ストーリーへの連続遷移を確認。','small-phone-failure-character-select.png','regression/R07-route-character.png'],
 ['R08','中','地図クレジットのリンクがドック・解説に隠れる','クレジットと解説の配置を分離。768〜1366pxでリンクの到達性を確認。コンパクト表示では「読み方・凡例」内に地図出典リンクを用意。','final/laptop-small-map26.png','regression/R08-credit-1280.png'],
 ['R09','低','横向きスマホで音楽操作が初期表示外','844×390では再生中情報・再生操作・音量を上へ集約。曲一覧は下へスクロール可能。スクロールなしで再生ボタンを操作できることを確認。','final/phone-landscape-sound.png','regression/R09-sound.png'],
];
const esc=s=>String(s).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('"','&quot;');
const shots=profiles.reduce((n,r)=>n+r.states.length,0),actions=profiles.reduce((n,r)=>n+r.actions.length,0);
let md=`# レスポンシブ修正・再検証レポート\n\n対象: ${manifest.base.slice(0,7)} + ローカル修正（manifest.json に SHA-256）。コミット・公開は未実施。\n\n元レポートの9件を修正し、18条件・${shots}画面・通常導線${actions}操作と、個別回帰${regression.length}件を確認。小画面で71展示の操作領域、6系統でクルージング開始・停止と実時間進行、375×667 ↔ 1366×768 のリサイズ復帰を確認。\n\n## 修正一覧\n\n| ID | 重要度 | 問題 | 修正・確認 | 状態 |\n|---|---|---|---|---|\n`;
md+=issues.map(([id,severity,title,fix])=>`| ${id} | ${severity} | ${title} | ${fix} | ローカル確認済み |`).join('\n');
md+='\n\n## 検証条件・未確認範囲\n\nローカル実Chromeのviewport/touch設定（DPR 1、動きを抑える設定）。320×568から2560×1440、境界幅900/901px、日英中。全71展示を全18条件で網羅した試験ではなく、18条件では代表5展示と各モード、71展示では375×667の操作領域を確認。外部HTTPSは遮断し保存データを使用。センサーはローカルfixture API。実機Safari / Firefox / Android・iOSのブラウザー固有挙動、端末性能、実配信、実AI通信、全曲再生・全ストーリー進行、全周クルージングの完走は未検証。\n\n元の監査証跡は変更せず保持。今回変更していない既存機能をすべて保証するものではありません。\n';
md+='\n## 追加で確認した残件\n\n低: 英語の「読み方・凡例」に既存の注意文「数値はこのパネルを開いた時点の表示です。」が日本語で残る。今回追加した観測データ見出し・出典見出しは日英中で確認済み。既存注意文は翻訳側で対応が必要。regression/source-credits-en.png に記録。\n';
md+='\n## 修正前・修正後\n';
let sections='<aside class="notice"><strong>追加確認・翻訳の残件（低）</strong><p>英語の「読み方・凡例」に、既存の注意文「数値はこのパネルを開いた時点の表示です。」が日本語で残ります。今回追加した見出しは日英中を確認済み。既存注意文は翻訳側で対応が必要です。</p><a href="regression/source-credits-en.png">確認画像</a></aside>';
for(const [id,severity,title,fix,before,after] of issues){const src=`../responsive-audit-20260913/${before}`;assert(fs.existsSync(`${out}/${after}`));assert(fs.existsSync(`${out}/${src}`));md+=`\n### ${id} ${title}\n\n${fix}\n\n![修正前](${src})\n\n![修正後](${after})\n`;sections+=`<section id="${id}"><h2>${id} · ${esc(title)}</h2><p>${esc(fix)}</p><div class="pair">${[[src,'修正前'],[after,'修正後']].map(([url,label])=>`<figure><a href="${url}"><img loading="lazy" src="${url}" alt="${id} ${label}"></a><figcaption>${label}</figcaption></figure>`).join('')}</div></section>`;}
fs.writeFileSync(`${out}/REPORT.md`,md);
let gallery='';for(const r of profiles)gallery+=`<details><summary>${esc(r.id)} · ${r.width}×${r.height} · ${esc(r.lang)}</summary><a href="final/${r.id}.json">測定・操作結果</a><div class="gallery">${r.states.map(s=>`<figure><a href="final/${s.file}"><img loading="lazy" src="final/${s.file}" alt="${esc(s.name)}"></a><figcaption>${esc(s.name)}</figcaption></figure>`).join('')}</div></details>`;
fs.writeFileSync(`${out}/REPORT.html`,`<!doctype html><html lang="ja"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>レスポンシブ修正・再検証</title><style>body{margin:0;background:#edf3f5;color:#193340;font:16px/1.8 system-ui,sans-serif}header{padding:32px max(20px,calc((100vw - 1120px)/2));background:#113542;color:white}main{max-width:1120px;margin:auto;padding:24px;background:white}h1{font-size:30px}h2{font-size:23px;border-bottom:2px solid #b2d5da;padding-bottom:8px;margin-top:42px}a{color:#00717b}header a{color:#a7e5e3}table{border-collapse:collapse;width:100%;font-size:14px}td,th{padding:12px;border:1px solid #bdd3da;text-align:left;vertical-align:top}.table{overflow:auto}.pair{display:grid;grid-template-columns:1fr 1fr;gap:16px}figure{margin:10px 0;padding:10px;background:#e9f0f4;border-radius:8px}img{display:block;width:100%;height:450px;object-fit:contain}figcaption{text-align:center;font-size:13px}.gallery{display:grid;grid-template-columns:repeat(auto-fit,minmax(200px,1fr));gap:12px}.gallery img{height:160px}details{padding:14px;border:1px solid #c7dbe0;margin:14px 0}summary{cursor:pointer;font-weight:bold}.notice{padding:18px;background:#eff7f5;border-left:4px solid #278a85}@media(max-width:650px){.pair{grid-template-columns:1fr}h1{font-size:24px}img{height:380px}}</style><header><p>GAIA SENSEWARE / 2026-09-13 / LOCAL QA</p><h1>レスポンシブ修正・再検証</h1><p>9件修正 / 18条件 / ${shots}画面 / ${actions}操作 / 個別回帰${regression.length}件</p><a href="REPORT.md">Markdown</a> · <a href="manifest.json">対象ファイル SHA-256</a> · <a href="regression/results.json">回帰結果</a> · <a href="map-matrix/results.json">71展示・クルージング・リサイズ結果</a></header><main><p class="notice">${manifest.base.slice(0,7)} + ローカル修正を確認。未コミット・未公開。実Chromeの画面サイズ／タッチ設定による検証であり、実機Safari等の合格を示すものではありません。</p><div class="table"><table><tr><th>ID</th><th>重要度</th><th>問題</th><th>状態</th></tr>${issues.map(([id,s,t])=>`<tr><td><a href="#${id}">${id}</a></td><td>${s}</td><td>${esc(t)}</td><td>ローカル確認済み</td></tr>`).join('')}</table></div><h2>確認範囲</h2><p>320×568〜2560×1440、900/901px境界、日英中。18条件では代表5展示と各モード。375×667では全71展示の操作領域。6系統でクルージングの実時間進行・開始停止、375×667 ↔ 1366×768 の配置復帰を確認しました。</p><p>外部HTTPSを遮断し保存データを使用。センサーはローカルfixture API。実機・Safari・Firefox、実配信、実AI通信、端末性能、全曲／全ストーリー、全周クルージング完走は未検証です。</p>${sections}<h2>全${shots}画面</h2>${gallery}</main></html>`);
console.log('REPORT PASS',profiles.length,shots,actions,regression.length);
